# Banco di Niccolo — Build Progress

---

## Phase checklist (Section 12)

| Phase | Title | Status |
|-------|-------|--------|
| **0** | Audit, monorepo, empty shell | ✅ Complete |
| 1 | Clock, 12-city map, ship/courier movement, save/load | ✅ Complete |
| 2 | Trade core — goods, prices, buy/sell, cargo, drift | ✅ Complete |
| 3 | Information layer — news latency, stale prices, couriers | ✅ Complete |
| 4 | Credit — bills, maturity ladder, deposits, loans, insolvency | ✅ Complete |
| 5 | Characters and assignments | ✅ Complete |
| 6 | Event engine — triggers, choices, effects, flags | ✅ Complete |
| 7 | Chapter 1 content pack — full Niccolo Rising arc | ✅ Complete |
| 8 | AI houses v1 — Medici, St Pol, Genoese; agents | ✅ Complete |
| 9 | Chapter 2 content pack — The Spring of the Ram | ✅ Complete |
| 10 | Chapter 3 content pack — Race of Scorpions | ✅ Complete |
| 11 | Trade risk & insurance, Chapters 1-3 quality pass (Section 14 redirect) | ✅ Complete |
| 12 | Chapter 0 prologue ("Claes begins") + expanded guided tour | ✅ Complete |
| 13+ | Chapter 4+ content — paused per Section 14 scope control | ⬜ |

---

## Phase 0 — Done

**Audit**
- Classified every Thrash Margin module as REUSE / ADAPT / IGNORE. See `AUDIT.md`.
- Verdict: build Niccolo as a clean sibling app. The TM simulation core (`GameState`, `Territory`, all action types, combat, AI) is fully entangled with territory-conquest mechanics and shares nothing with Niccolo's ledger/trade/character model.
- Extracted one genuinely theme-agnostic utility: `getNeighbours` + `shortestPath` into `packages/engine/src/graph.ts`.

**Monorepo restructure**
- Moved `thrash-margin/` → `packages/thrash-margin/` via `git mv` (history preserved).
- Moved `banco-di-niccolo-design.md` to repo root.
- Created root `package.json` with npm workspaces covering `packages/*`.
- Created root `.gitignore`.
- Confirmed Thrash Margin client builds cleanly after the move (`tsc && vite build` ✓).

**Niccolo shell**
- Created `packages/niccolo/` — Vite + React + TypeScript, identical stack to TM.
- Builds clean (`tsc && vite build` ✓), runs on port 5174.
- Placeholder screen: dark manuscript aesthetic, title + subtitle + phase note.

**Engine stub**
- Created `packages/engine/src/graph.ts` with `getNeighbours` and `shortestPath`.
- Neither app imports it yet; it is ready to use in Phase 1.

---

## Phase 1 — Done

Deliverable met: **sail Bruges to Venice and watch weeks pass.** Verified live in browser — dispatched the ship from Bruges, advanced the clock week by week, watched the marker travel the Bruges–Venice sea route, and confirmed arrival at "Week of 9 May 1460" (8 weeks after the 14 March 1460 campaign start, matching the route's `distanceWeeks`). Reloaded the page and confirmed the save survived.

1. **Clock** — `dateForWeek`, `formatWeekDate`, `advanceWeek` added to `packages/engine/src/clock.ts` (pure, theme-agnostic — takes a `startDate` rather than hard-coding one) and re-exported from `packages/engine/src/index.ts`. Niccolo supplies its own campaign start (14 March 1460) from `packages/niccolo/src/sim/content.ts`.

2. **12-city map** — authored `packages/niccolo/src/content/cities/chapter1.json`: London, Calais, Bruges, Ghent, Antwerp, Dijon, Geneva, Lyon, Milan, Genoa, Florence, Venice, each with `id/name/region/power/x/y/port`. Rendered as an SVG node graph in `packages/niccolo/src/components/MapView.tsx`.

3. **Routes** — authored `packages/niccolo/src/content/routes/chapter1.json`: 13 edges (land + sea), including a direct Bruges–Venice sea route (8 weeks, seasonal, the Flanders galley line) and an overland alternative via Dijon–Geneva–Milan–Florence (the Alpine pass at Geneva–Milan is marked seasonal).

4. **One ship** ("The Charetty ship") and **5. one courier** ("The dispatch rider") — both start docked at Bruges. Sim core lives in `packages/niccolo/src/sim/`: `types.ts` (GameState/City/Route/Vessel/GameAction), `state.ts` (`createInitialState`), `actions.ts` (pure `processAction` handling `ADVANCE_WEEK` and `DISPATCH_VESSEL`, mirroring TM's reducer pattern). The courier is restricted to land routes only — verified it cannot be sent across the Bruges–Venice sea leg, unlike the ship.

6. **Save/load** — `packages/niccolo/src/hooks/useGameLocal.ts`, single `localStorage` slot (`niccolo_save`), same shape as TM's `useGameLocal.ts` but simplified (no multi-save index — that's TM-specific complexity Phase 1 doesn't need).

Also required, not originally itemised: added `@repo/engine` as a real npm-workspace dependency of `packages/niccolo/package.json` (it wasn't wired up in Phase 0) and ran `npm install` at the repo root so the workspace symlink exists.

No trade, no credit, no characters, no events. Those are Phase 2+.

---

## Phase 2 — Done

Deliverable met: **profitable triangle run.** Verified live in browser — with the ship starting docked at Bruges with 40f cash: sailed Bruges → Calais → London (2 weeks), bought 4 wool at 10-11f/unit (40f spent, cash to 0), sailed back to Bruges (2 weeks), sold the wool at 18-20f/unit (80f received, cash to 80f — a 40f profit on the wool leg alone), then bought 3 Flemish cloth at 24-26f/unit (72f spent, cash to 8f), ready to carry it back to London where cloth base-prices at 45f. Confirmed the save survives a full page reload mid-run (cash, week, and cargo all persisted).

1. **Goods** — authored `packages/niccolo/src/content/goods/chapter1.json`: 8 goods (wool, Flemish cloth, madder, wine, salt, silk, spices, glass), each with an `id/name/unit`.

2. **Local prices** — extended each city in `packages/niccolo/src/content/cities/chapter1.json` with a `market` map (`goodId -> { base }`), following the node data model in Section 3 of the design doc. Prices are deliberately uneven across the map to make arbitrage real: e.g. wool is 10f in London (produced) and 20f in Bruges (imported); cloth is the reverse (24f in Bruges where it's woven, 45f in London where it's a luxury import).

3. **Buy/sell** — new `BUY_GOOD` / `SELL_GOOD` actions in `packages/niccolo/src/sim/actions.ts`. Both require the vessel to be docked (not under way) and to have a cargo hold (couriers don't trade); buying additionally checks cargo space and cash on hand, selling checks the vessel is actually carrying that much.

4. **Cargo** — `Vessel` gained `cargo: Record<goodId, quantity>` and a `capacity` (the Charetty ship: 20 units; the dispatch rider: 0, so it's mechanically excluded from trade). `cargoTotal()` in the new `packages/niccolo/src/sim/market.ts` sums a manifest against capacity.

5. **Price drift** — each city-good pair tracks a `scarcity` multiplier (1.0 = base price) in `GameState.scarcity`. Buying nudges it up (price rises as local supply is depleted), selling nudges it down; every `ADVANCE_WEEK` it decays 10% of the way back toward 1.0 (`driftScarcity` in `sim/market.ts`), so a market a player has been trading in gradually normalises if left alone. `eventModifier` and true random `noise` from the design doc's price formula are deferred — there's no event system yet (Phase 6) and Phase 3 explicitly inverts the pricing problem (stale received-news prices vs. live prices), so it isn't worth building noise into a price the player can currently see live.

Also required, not originally itemised: a defensive check in `useGameLocal.ts`'s `readSave()` that discards a pre-Phase-2 save missing `cash`/`scarcity`/`cargo` rather than crashing on it (there's only one local save slot, so this just means a stale save quietly restarts a fresh campaign instead of throwing).

Starting cash is 40f — small on purpose, per the design pillar that cash-on-hand should be "a small, dangerous number." A `MarketPanel` component (`packages/niccolo/src/components/MarketPanel.tsx`) renders when a cargo-capable vessel is selected and docked: current city's goods, live price, a quantity input, and Buy/Sell buttons; cash and hold usage are shown in both the panel and the vessel list.

No news/latency layer (prices are all live, not stale-by-report), no insurance, no multi-currency, no characters, no events. Those remain Phase 3+.

---

## Phase 3 — Done

Deliverable met: **lose money on stale information, then fix it with couriers.** Verified live in browser.

1. **News items** — `NewsItem` (`packages/niccolo/src/sim/types.ts`): `cityId`, `trueAsOfWeek`, `receivedOnWeek`, and a `prices` snapshot. New `packages/niccolo/src/sim/news.ts` generates one for every city with a market, every week, snapshotting that week's true prices.

2. **Latency** — `shortestReportWeeks()` (Dijkstra over all 12 cities) gives each city a base correspondence time from Bruges, over *every* route, land or sea — a letter rides whatever ship or rider is already making a crossing, unlike Phase 1's single dispatch-rider unit, which stays land-only for its own physical travel. Base latencies from Bruges: Ghent/Antwerp/Calais 1 week, London 2, Dijon 3, Geneva 5, Lyon 6, Milan 7, Venice 8 (via the direct Flanders galley route — the fastest path for both cargo and mail), Genoa 8, Florence 9. Bruges itself is always latency 0 — it's home, the player has first-hand knowledge there always.

3. **Stale prices** — `GameState` gained `pendingNews` (in transit) and `knownPrices` (cityId → newest arrived report). Every `ADVANCE_WEEK`, new snapshots are generated and any whose `receivedOnWeek` has passed move into `knownPrices`. A vessel actually docked at a city still trades at the true live price (unchanged from Phase 2) — the risk is purely in planning a voyage on a report that's gone stale by the time you arrive, not in the trade itself.

4. **Background market flows** — added `applyBackgroundFlows()` (`packages/niccolo/src/sim/market.ts`): a small random nudge (±0.08) to every city-good's scarcity, every week, independent of the player's own trades. This was necessary, not just itemised: without it, nothing in the sim ever moved a price except the player's own buying and selling, so a stale report about a city the player hadn't touched would always still be exactly true when it arrived — there'd be no actual staleness risk to play around. This represents the wider economy the player can't see directly (design doc §3/§4, "scarcity drifts with simulated supply flows between nodes").

5. **Courier investment** — new `INVEST_COURIER` action. Costs `15f × (current level + 1)` and shaves one week off a city's latency, down to a floor of 1 week (a report can never beat a courier's actual travel time, however much is invested). Bruges can't be invested in — it's already instant.

Also required, not originally itemised: extended the save-guard in `useGameLocal.ts` to discard pre-Phase-3 saves missing `knownPrices`/`pendingNews`/`courierInvestment`.

New `DispatchesPanel` component (design doc's UI screen 4): lists every city, its last-known report and age, when the next report is due, current vs. base latency, and an invest button; shows "live" instead of a stale report for any city a vessel is currently docked at. `MapView` now fades city markers by information age (screen 1's "fog by information age") — solid for live or fresh reports, dimmer for stale or unreported cities.

Verified live in browser: reset to a fresh campaign and confirmed only Bruges was live at week 0, with all 11 other cities showing "no report yet". Advanced a week and watched the 1-week-latency neighbours (Ghent, Antwerp, Calais) report in while London (2 weeks) and everything further stayed dark. Invested 15f in London's courier line, watched its latency drop from 2 to 1 week and its button switch to a disabled "Fastest". Dispatched the ship to Dijon (3-week land route, no report yet), advanced 3 weeks, and on arrival found the true docked price (wine 12f, salt 17f) had drifted from the 11f/18f base under background flows the player had no visibility into until physically arriving — the stale-information risk the phase exists to demonstrate. Confirmed the save (including the news queue and courier investment) survives a page reload, then reset the campaign to a clean state. `tsc --noEmit` and `vite build` both clean.

No agents, secrets, or ciphers (design doc ties those to Phase 8/counter-intelligence and the event system), no insurance or multi-currency (Phase 4), no `eventModifier` term in the price formula (Phase 6, event engine). Those remain Phase 4+. **Do not start Phase 4 — stop here per instructions.**

---

## Phase 4 — Done

Deliverable met: **leverage a venture and survive the ladder.** Verified live in browser.

1. **Multi-currency** — `sim/currency.ts` defines 5 currencies grouped by `City.power` (confirmed 2026-07-19): florin (home ledger + Florence), groot (Burgundy/Flanders: Bruges/Ghent/Antwerp/Dijon), pound (England: London/Calais), écu (Savoy/France/Milan/Genoa: Geneva/Lyon/Milan/Genoa), ducat (Venice). Every city in `content/cities/chapter1.json` gained a `currency` field. Cash itself stays a single florin balance — foreign-currency exposure lives entirely in the credit instruments below, not in a multi-wallet system, so Phase 2/3's trade code needed no changes.
2. **Exchange rates** — `initialExchangeRates()`/`driftExchangeRates()` in `sim/currency.ts` give each foreign currency a simplified peg against the florin (not literal historical mint weights) and a weekly random walk that mean-reverts toward par, mirroring the existing scarcity-drift pattern. Verified live: a bill written at 1.30f/pound settled eight weeks later at 1.27f/pound — real FX risk, not just flavour text.
3. **Bills of exchange** — `WRITE_BILL` (`sim/credit.ts`) lets the player borrow florins now against a future repayment, denominated in the destination city's currency, term 2–26 weeks, cost hidden in the exchange spread (0.6%/week). No branch gating (confirmed 2026-07-19) — bills can be written at any city on the map right now; Phase 5 tightens this once branches/managers exist.
4. **Deposits** — `TAKE_DEPOSIT` is the cheap-capital instrument: always florin-denominated, always at home, lowest rate (0.15%/week) — "a merchant deposits with the bank."
5. **Loans out** — `WRITE_LOAN` offers merchant loans (safe, 0.4%/week) and prince loans (risky, 0.9%/week, 20% default chance at maturity) — the design doc's "high margin, may default politically" vs. "secured on cargo" distinction, without named counterparties (Phase 5/8 content).
6. **Discounting** — `DISCOUNT_OBLIGATION` lets the player sell a not-yet-matured receivable for immediate cash at a present-value haircut (1%/week) — a real financial trade-off, not free money. Verified live: a 102f loan receivable discounted early for 98f.
7. **Maturity ladder & insolvency** — `resolveMaturingObligations` (`sim/credit.ts`) runs every `ADVANCE_WEEK`: receivables pay in (prince loans may default), payables draw from cash first, then from a forced sale of any *docked* vessel's cargo at a 70% haircut (cargo under way is illiquid and can't be reached). If that still doesn't cover it, `insolvent` is set and the campaign ends — binary, no stopgap rescue (confirmed 2026-07-19): the design doc's rescue options (Medici support, marriage capital, branch sale) all depend on systems that don't exist until Phase 5/8, so inventing a stand-in would contradict the scope discipline already established in Phase 3. `LedgerPanel` (design doc's Ledger + Counting House screens, consolidated) shows cash, exchange rates, the next-12-weeks maturity ladder, and forms for all three instruments.

Also required, not originally itemised: extended the save-guard in `useGameLocal.ts` to discard pre-Phase-4 saves missing `exchangeRates`/`obligations`/`insolvent`; rounded all cash displays (`Math.round`) since interest math now produces fractional florins; `processAction` short-circuits to a no-op once `insolvent` is true, so the only way forward from the game-over screen is the existing reset flow.

Verified live in browser: borrowed 50f via a bill at London (8-week term, pound-denominated), sailed to London, bought 9 wool at 9f (leveraged past the starting 40f), sailed home to Bruges, sold the wool at 21f for 189f, watched the bill auto-settle in full from cash at week 8 with the ledger clearing to "Nothing due soon." Separately confirmed a merchant loan and its early discount, then deliberately over-leveraged (a 200f bill with all cash lent away and no cargo to liquidate) to trigger the insolvency screen, and confirmed "Start a new campaign" resets cleanly to 40f/week 0. `tsc --noEmit` and `vite build` both clean.

No characters, no branch network, no condotta contracts, no reputation-gated deposit runs, no AI houses. Those remain Phase 5+. **Do not start Phase 5 — stop here per instructions.**

---

## Phase 5 — Done

Deliverable: **assign officers to postings and watch loyalty, Conscience, and the assignments themselves materially change trade, credit, and information outcomes — then neglect the household and watch it cost you.** Verified live in browser.

Section 12 gives Phase 5 no explicit "Playable:" sentence the way Phases 1-4 have one; the deliverable above was written to match that pattern, scoped tightly to §7's own feature list (roster, skills, postings, loyalty, Conscience) rather than inventing scope Phase 12 doesn't ask for. The three judgement calls behind it are recorded as implementation notes in `banco-di-niccolo-design.md` (Section 7): the Chapter 1 roster is seeded at campaign start rather than staggered by chapter-script join dates (that scripting is Phase 6+ content); the assignment loop's four postings map onto systems that already exist (aboard/negotiate/investigate) and skip the one that doesn't yet (manage branch); and loyalty-driven departure is a new generic mechanic, explicitly distinct from the scripted, unpreventable deaths (Marian, Katelina, Godscalc, Umar) this section already promises for later chapters.

1. **Roster** — authored `packages/niccolo/src/content/characters/chapter1.json`: the six officers already with the Charetty company in the earliest chapters of Book 1 — Marian de Charetty (owner), Julius (notary), Tobias Beventini (physician), Father Godscalc (chaplain), Gregorio of Asti (lawyer), Astorre (condottiere) — each with the `law/trade/combat/intrigue` skills from §7's own data model, a starting loyalty, a weekly salary (0f for Marian, the owner; 2-6f for the rest), and `status: 'active'`. Total starting upkeep is 19f/week against a 40f starting stake — deliberately real pressure, per the same "cash is small and dangerous" pillar Phase 2 established.
2. **Assignments** — new `CharacterAssignment` union (`idle | aboard | negotiate | investigate`) and `ASSIGN_CHARACTER` action. `sim/characters.ts` computes each assignment's effect from the assigned officer's skill: **aboard** a vessel gives that vessel's `BUY_GOOD`/`SELL_GOOD` trades a trade-skill discount (cheaper buys, dearer sells, capped at 5%); **negotiate** at a city gives a law-skill discount to the per-week rate on any bill, deposit, or loan written there (capped at 50% off, verified live: Julius negotiating at Bruges is baked into the bill/loan/deposit rate at that city); **investigate** at a city gives an intrigue-skill cut to that city's news latency (`news.ts`'s `currentLatencyFor` now takes `characters` and stacks the cut with paid courier investment) — verified live: assigning Tobie (intrigue 3) to investigate London cut its latency from 2 weeks to 1 and correctly flipped its courier-investment button to "Fastest". `manage branch` is not implemented — no branch network exists yet (§5, gated behind Phase 8+).
3. **Loyalty and weekly upkeep** — `resolveWeeklyUpkeep()` runs every `ADVANCE_WEEK` alongside `resolveMaturingObligations`: if cash covers the whole active roster's salaries, everyone is paid and loyalty ticks up by 1; if it doesn't, nobody is paid (no partial pay, no back-pay debt on the maturity ladder) and loyalty drops by 4 for everyone active. A character whose loyalty reaches zero leaves the company permanently and their assignment reverts to idle. Verified live: with cash at 5f against 19f in wages due, advancing a week dropped every active officer's loyalty by exactly 4 (e.g. Marian 90→86, Julius 70→66) and left cash untouched.
4. **Conscience** — a single `conscience: number` (0-100, starts at 100) on `GameState`. The one wired trigger in Phase 5 is `WRITE_LOAN` with `kind: 'prince'`: it costs 5 Conscience and 3 loyalty each for Godscalc and Tobie by name (matching §7's own "affects Godscalc/Tobie loyalty" sentence), regardless of whether the prince later defaults. Verified live: writing an 85f prince loan dropped Conscience from 100 to 95 and Godscalc/Tobie's loyalty from 85/75 to 82/72, while Marian/Julius/Gregorio/Astorre were untouched.
5. **Household screen** — new `HouseholdPanel` (design doc's UI screen 5): roster with skills, loyalty (colour-coded), salary, current assignment in plain English, and a single combined dropdown to reassign (idle / aboard a vessel / negotiate at a city / investigate at a city — investigate is hidden for Bruges, since its latency is already 0). Header shows a running Conscience total; the panel shows total wages due next week and whether they're currently covered. `DispatchesPanel` now names whichever officer is investigating a city inline with its latency figure.

Also required, not originally itemised: extended the save-guard in `useGameLocal.ts` to discard pre-Phase-5 saves missing `characters`/`conscience`; threaded `characters` through `generateNews`/`currentLatencyFor`/`canInvestFurther` (all default to `[]` so nothing else calling them needed updating); `credit.ts`'s `writeBill`/`takeDeposit`/`writeLoan` all now apply the negotiate discount at their respective city (bills at their destination city, deposits/loans always at home) before computing the obligation amount.

Verified live in browser: assigned Julius to negotiate at Bruges and confirmed a bill written there executes without error under the discounted spread; assigned Tobie to investigate London and watched its report latency drop from 2 weeks to 1 and its courier-invest button switch to "Fastest"; assigned Marian aboard the Charetty ship and confirmed a wool purchase's cost dropped from an undiscounted 20f to a discounted-and-rounded 19f (read directly off the "not enough cash" error message); wrote an 85f prince loan and watched Conscience fall 100→95 with Godscalc and Tobie's loyalty falling 3 points each while the rest of the roster was untouched; then, with cash too low to cover the 19f weekly payroll, advanced a week and watched every active officer's loyalty drop by 4 uniformly. Reset the campaign to a clean state (40f, week 0, conscience 100) afterward. `tsc --noEmit` and `vite build` both clean.

No branch network, no condotta contracts as a live asset, no chapter-scripted join dates or scripted deaths, no AI houses. Those remain Phase 6+ (event engine), Phase 7 (Chapter 1 content pack), and Phase 8 (AI houses). **Do not start Phase 6 — stop here per instructions.**

---

## Phase 6 — Done

Section 12 gives Phase 6 no explicit "Playable:" sentence either (its own line is "Author 10 test events"), so the deliverable is scoped tightly to that and to §8's own feature list, following the same pattern Phase 5 used: **data-driven events fire on schedule, chain off each other's flags, and interrupt play with a real choice that changes cash, Conscience, or the flag state — proven with 10 authored test events, not narrated Chapter 1 content.** Verified live in browser.

1. **Event data model** — `ScriptedEvent`/`EventTrigger`/`EventChoice`/`EventEffects` (`packages/niccolo/src/sim/types.ts`) match §8's own JSON shape: `id`, `chapter`, `trigger` (`dateAfter`/`location`/`flag`, all optional and AND-combined), `title`, `body`, `choices[].{text, effects}`. Content lives entirely in `packages/niccolo/src/content/events/chapter1.json` — logic never inspects a hardcoded event id beyond the engine's own bookkeeping.
2. **Triggers** — `sim/events.ts`'s `triggerMatches` checks the in-game clock (`dateForWeek` against `trigger.dateAfter`), whether any vessel is docked at `trigger.location`, and whether `trigger.flag` is already set in `GameState.flags`. `checkTriggers` runs once at campaign start and once after every `ADVANCE_WEEK`, queuing any newly-satisfied, not-yet-fired-or-pending event onto `pendingEvents`.
3. **Choices and effects** — `resolveEvent` (new `RESOLVE_EVENT` action) pops the front of `pendingEvents`, applies the chosen effect (`flag` sets a flag permanently, `cash` deltas the florin balance, `conscience` deltas the meter, clamped 0-100), and records the event id in `firedEvents` so it can never fire again.
4. **Flags** — `GameState.flags: Record<string, boolean>`, set only by event effects, readable only by later event triggers — the chaining mechanism. Two of the 10 test events demonstrate a chain: refusing a shortcut (`refused_shortcut`) unlocks a follow-up reward the week after (`reputation_probity`); paying for a dockside tip (`dockside_tip`) unlocks its payoff (`tip_paid_off`).
5. **Interruption** — while `pendingEvents` is non-empty, `processAction` blocks every action except `RESOLVE_EVENT` (mirroring the existing `insolvent` short-circuit), and only the front of the queue can be resolved — one scripted scene at a time, per pillar 3 ("victories cost more than defeats... interrupts commercial positions").
6. **10 test events** — authored in `content/events/chapter1.json` (`ev_c1_001` through `ev_c1_010`), spanning March–October 1460: pure date triggers, date+location triggers (Bruges, London, Venice), and the two flag-gated chains above, exercising all three trigger fields and all three effect fields the engine supports.

Also required, not originally itemised: extended the save-guard in `useGameLocal.ts` to discard pre-Phase-6 saves missing `flags`/`firedEvents`/`pendingEvents`; built `EventOverlay` (design doc's UI screen 8) as a full-screen modal card (title, body, one button per choice) and wired it into `App.tsx` above everything else when `state.pendingEvents[0]` resolves to a known event; created `.claude/launch.json` so the niccolo dev server can be previewed (`npm run dev --workspace niccolo`, port 5174) — it didn't exist yet.

No `rep.*` house-reputation effects (no AI houses to hold a reputation with — Phase 8), no scripted deaths/departures, no Chapter 1 narrative content — those remain Phase 7 (content pack) and Phase 8 (AI houses). The scope judgement calls (effects limited to flag/cash/conscience; trigger evaluation on the weekly tick, not instantly on flag-set) are recorded as an implementation note in `banco-di-niccolo-design.md` (Section 8).

Verified live in browser: on a fresh campaign, "A ledger left open" (dateAfter 1460-03-01, location Bruges) fired immediately at week 0 and blocked the rest of the UI until resolved; chose "Take his word for it" (conscience +1, clamped at the 100 ceiling). Advanced three weeks to 4 April 1460 and watched "The dyeworks apprentice" fire on schedule; chose "Refuse — the vats are done properly" (sets `refused_shortcut`). Advanced one more week and confirmed the chained "Word gets around" fired only now (not the same tick the flag was set), paying out +10f on the queue's single choice, cash rising from 2f to 12f. Confirmed normal play (buy/sell, dispatch, household, ledger) was inert while an event card was up and resumed the instant it was resolved. Reset the campaign afterward. `tsc --noEmit` and `vite build` both clean.

---

## Phase 7 — Done

Deliverable met: **Chapter 1 content pack complete — full Niccolo Rising arc, Bruges start to partnership end. First real playthrough.** Verified via a scripted end-to-end simulation of the whole event chain (not hand-clicked — a 32-event, ~90-week campaign is impractical to click through turn-by-turn for verification, so a driver script exercised `processAction` directly the same way the UI does) plus live-browser checks of every new UI surface. See "Verification" below for both.

1. **32 scripted events** (`packages/niccolo/src/content/events/chapter1.json`) — the existing 10 Phase 6 test events plus 22 new ones, spanning March 1460 to November 1461, covering every named beat in §9's Chapter 1 outline: dye jobs and pawnbroking for the Charetty widow, the courier round's first arrivals at Geneva and Milan, Astorre's Naples condotta (broker, run, and wrap-up), the cannon shipment logistics set piece, the ostrich errand, the Medici relationship, discovery of two alum-adjacent secrets, marriage to Marian, an escalating St Pol/Ribérac vendetta reaching "first blood," Felix's scripted death, and a finale that converts the partnership. Full list and rationale recorded as an implementation note in `banco-di-niccolo-design.md` (Section 9).

2. **Secrets** (design doc §6, previously unbuilt) — `Secret` type and `GameState.secrets`, granted by a new `effects.secret` event field (`sim/secrets.ts`). A new `USE_SECRET` action exploits-or-sells a held secret for its florin value in one move (no named buyers yet — Phase 8 content, same abstraction precedent as Phase 4/5's uncounterpartied bills and loans). Secrets can expire: past `expiresWeek` and unused, they flip to `expired` on the next `ADVANCE_WEEK` and become worthless. Chapter 1 ships two — the Phocea alum interest (modest, no urgency) and the Tolfa alum discovery (large, time-limited) — not three; recorded as an implementation note in Section 6.

3. **Condotta contracts** (design doc §5, previously unbuilt) — `CondottaContract` on `GameState.condotta`, started by a new `effects.condotta` event field and resolved every `ADVANCE_WEEK` (`sim/condotta.ts`): pays a weekly retainer, counts down, and on completion pays a campaign-bonus lump sum and sets a flag for the wrap-up event. No sub-brokering (the design doc's own "can be," not "is"). Recorded as an implementation note in Section 5.

4. **The cannon shipment** — Chapter 1's "1 extraction or logistics set piece" (§9's own preamble). Built on the *real* cargo system rather than a flavour-only event chain: a new `cannon` good sellable only at Milan, a new Naples city (13th on the map, no market — added solely as the delivery point) reached by a new Genoa–Naples sea route, and two new event-trigger fields to make a genuine delivery deadline possible — `cargoAtLeast` (a docked vessel at a city must be carrying at least a quantity of a good) and `flagAbsent` (a flag must *not* be set, the mechanism guarding the success and deadline-miss events against ever both firing). A third trigger addition, `flags` (an array, all must be set), gates the chapter finale on five independent threads at once. All three are additive — no existing event or save shape changed. Recorded as an implementation note in Section 8.

5. **Medici favor** — the one relationship in Chapter 1 with a mechanical payoff: a `medici_favor` flag, earned via a short event chain, shaves an extra 15% off a bill's spread specifically at Florence (`writeBill` in `sim/credit.ts`).

6. **Chapter exit** — `chapter1_complete` is a flag, not an actual transition into Chapter 2 (that's Phase 9+). Set by a finale event gated on `married_marian`, `felix_lost`, `condotta_naples_resolved`, `cannon_shipment_resolved`, and `stpol_first_blood` all being true, plus a floor date. `processAction` freezes once it's set, mirroring the existing `insolvent` short-circuit, and `App.tsx` renders a completion screen (weeks elapsed, final cash and conscience, secrets used/expired, any officers lost) in place of the normal UI — the same treatment the insolvency screen already gets. Every one of the five threads resolves regardless of which choices the player makes, including a `simon_ignored` branch (ev_c1_004's other option) that had no path to the vendetta's `stpol_grudge` flag until this phase added `ev_c1_031` to bridge it — without that, choosing "say nothing" to Simon on the first encounter would have permanently soft-locked the campaign out of ever reaching the finale.

7. **New UI** — `SecretsPanel` (list, value, status, an Exploit/Sell button per active secret; renders nothing when no secrets are held) and a one-line condotta status added to `HouseholdPanel` ("Astorre's company: on campaign at Naples, N weeks left · Xf/wk retainer" or "no active contract").

Also required, not originally itemised: extended the save-guard in `useGameLocal.ts` to discard pre-Phase-7 saves missing `secrets`/`condotta` (the latter nullable, so checked by key presence rather than truthiness).

**Verification.** Two whole-campaign scripted playthroughs (success and failure branches of the cannon shipment, and the humiliated/ignored branches of the Simon encounter) both reached `chapter1_complete: true` cleanly at week 86, with all five finale-gating flags correctly set and no stuck or double-fired events. A separate isolated check confirmed `cargoAtLeast` fires the shipment's success event only once 6+ cannon are actually docked at Naples (not before, not with fewer), and that its `flagAbsent` guard prevents the success and deadline-miss events from ever both firing. Direct checks confirmed `USE_SECRET` pays out and can't be reused, expired secrets can't be used, and the Medici-favor discount applies at Florence only. Live in the browser: campaign loads clean with no console errors, the week-0 event fires and resolves normally, Naples appears correctly everywhere a city belongs (map, negotiate/investigate dropdowns, bill-city selector), and the Household panel's new condotta line renders. `tsc --noEmit` and `vite build` both clean.

One pre-existing cosmetic note, not introduced by this phase: the map SVG's height doesn't stay bounded to its pane on tall viewports (the sidebar's long household dropdown lists push the whole page taller, and the map stretches with it), so reaching cities near the bottom of the map — Naples included — needs a page scroll rather than fitting on one screen. This CSS is unchanged from Phase 1; it isn't a Phase 7 regression, and fixing it is a UI polish item outside this phase's content-pack scope.

No AI houses, agents, or counter-intelligence (Phase 8); no branch network or sub-brokered condotta; no Chapter 2 content or an actual chapter transition (Phase 9+). Those remain later phases. **Do not start Phase 8 — stop here per instructions.**

---

## Phase 8 — Done

Section 12 gives Phase 8 no explicit "Playable:" sentence either — its own line is "AI houses v1 (Medici, St Pol interests, one Genoese) plus agents and counter-intelligence." Following the same pattern Phases 5/6 used, the deliverable is scoped tightly to that line and to §6/§10's own feature lists: **three AI houses run a light weekly trade footprint and a dynamic relation standing with the player; a placed agent shields a city's reports from a hostile house's counter-intelligence or has a chance of surfacing that house's insider secret; and a hostile house's counter-intelligence can silently plant a false report in an unshielded city, indistinguishable from a true one until it's checked against a physical visit or a later arrival.** Verified via a scripted driver (probabilistic paths, mirroring Phase 7's own driver-verification approach for anything impractical to click through by hand) plus live-browser checks of the new UI. See "Verification" below for both.

1. **Three AI houses** (design doc §10, scoped to Section 12's own Phase 8 line, not the fuller §10 roster) — `content/houses/chapter1.json`: the Medici Bank (ally, seat Florence, baseline relation 65 — already tied to the Phase 7 `medici_favor` bill discount), St Pol/Ribérac interests (hostile, seat Bruges, baseline 30 — the vendetta's commercial face), and a generic Genoese consortium (neutral, seat Genoa, baseline 50). `sim/houses.ts`'s `driftHouseRelations` mean-reverts each house's `GameState.houseRelations[houseId]` (0-100, mirrors Conscience) toward its baseline every week, the same shape exchange rates and scarcity already drift — except St Pol's own baseline drops once `stpol_first_blood` is set (Chapter 1 vendetta content), so relation keeps drifting, just toward a worse floor. `applyHouseTradeFootprint` gives each house a small weekly trade presence at its own home city (a 2-unit nudge via the existing `adjustScarcity` — the same function the player's own buy/sell already use), the "reduced fidelity" trade §10 asks for, not a second parallel player.
2. **Agents** (design doc §6, previously unbuilt) — `Agent { name, placement, placedWeek }` on `GameState.agents`, placed via a new `PLACE_AGENT` action at an escalating cost (`25f × (agents held + 1)`, the same shape as courier investment's own cost curve). A city agent (`sim/houses.ts`'s `cityIsShielded`) shields that city's weekly report from ever being planted; a house agent (`agentsInHouse`) has a 15%/week chance of surfacing that house's one insider secret via the existing Phase 7 Secret system (`addSecret`), capped at one per house. "Plant false news" from the player's own side is not wired — rival houses have no news feed of their own to deceive at this fidelity, so there's nothing yet for that verb to act on; recorded as a scope note in the design doc rather than inventing a rival intelligence-feed system this phase doesn't need.
3. **Counter-intelligence** (design doc §6) — `corruptNews` in `sim/houses.ts`: every week, for every city not shielded by a player agent, each hostile house (St Pol, in Chapter 1) independently rolls a 12% chance to silently replace that city's freshly generated report with distorted prices (0.5×-1.8× the true figure) before it ever reaches `pendingNews`. Bruges is never targeted, matching its existing always-live treatment. The player has no way to tell a planted report from a true one — that's the point, and why it's verified with a scripted driver forcing `Math.random` rather than by eye (see Verification).
4. **`EventEffects.rep`** (`houseId -> relation delta`, design doc §8's own example JSON) — wired into `resolveEvent`, deferred at Phase 6 and Phase 7 for lack of a house to hold a reputation with. No Chapter 1 event uses it (Chapter 1's 32 events shipped in Phase 7, before this field existed); available to Phase 9+ chapter authors without another engine change.
5. **New UI** — `HousesPanel` (no dedicated screen for this in design doc §11, the same situation Phase 7's `SecretsPanel` was in): the three houses with disposition, seat, and live relation; a combined dropdown to place an agent inside a house or shielding a city, with the escalating cost shown on the button; and a list of currently-placed agents. Wired into `App`'s sidebar below `SecretsPanel`.

All four scope judgement calls above (three houses not the fuller roster; reduced-fidelity trade footprint instead of a second player; "plant false news" left unwired for the player; `rep` wired but unused by existing content) are recorded as implementation notes in `banco-di-niccolo-design.md` (Sections 6 and 10).

**Verification.** A scripted driver (`sim/houses.ts` functions called directly, plus a full `processAction` loop) with `Math.random` forced to fixed values confirmed: `corruptNews` distorts an unshielded non-home-city report when every roll hits and leaves it untouched when every roll misses; it never touches the home-city (Bruges) report even when every roll hits; a city agent shields that city from corruption even when every roll hits; `cityIsShielded` reports correctly for shielded and unshielded cities. A separate 150-week `ADVANCE_WEEK` loop (resolving the week-0 event first, then placing an agent inside St Pol's house) confirmed the agent eventually surfaces St Pol's insider secret, every house's relation stays within [0,100] throughout, and — reaching `chapter1_complete` at week 86, matching Phase 7's own verified playthrough length — St Pol's relation had fallen to ~15, well below its 30 baseline, confirming the `stpol_first_blood` baseline penalty fired correctly during that playthrough. Live in the browser: placed a named agent shielding London's reports for 25f (cash 40f → 15f), confirmed it appears under "Shielding cities," and confirmed the next agent's cost correctly escalated to 50f. `tsc --noEmit` and `vite build` both clean.

No branch network, no sub-brokered condotta, no Chapter 2 content or an actual chapter transition (Phase 9+). Those remain later phases. **Do not start Phase 9 — stop here per instructions.**

---

## Phase 9 — Done

Section 12's own Phase 9 line is "one chapter content pack per phase, adding systems as chapters demand them" — no explicit "Playable:" sentence, the same pattern Phases 5/6/8 used. The deliverable, scoped to that line and to §9's own Chapter 2 synopsis: **Chapter 1 flows straight into a real, playable Chapter 2 on the same campaign clock — a new Florentine agency at Trebizond, a rival AI house that can cost the player cargo, a mid-campaign roster join, and an extraction race against a deadline that scores what survives — ending in `chapter2_complete`, a real terminal state the way `chapter1_complete` used to be.** Verified via a scripted driver (the same isolation-testing approach Phase 7/8 used for anything impractical to click through turn-by-turn) plus live-browser checks, including against the one real pre-Phase-9 campaign already on disk. See "Verification" below for both.

1. **Chapter 2 unlocks Chapter 1's exit rather than ending the campaign there.** `chapter1_complete` was a hard stop in `processAction` (mirroring `insolvent`) because Phase 7 had nothing to put past it. Phase 9 lifts that freeze and moves it to a new `chapter2_complete` instead; Chapter 2's own opening event (`ev_c2_001`) triggers directly off the `chapter1_complete` flag already being set, so a campaign that finishes Chapter 1 rolls straight into Chapter 2 on its next `ADVANCE_WEEK` with no separate "start next chapter" action. `GameScreen.tsx`'s old full-screen Chapter 1 ending screen was replaced with a Chapter 2 ending in the same spot; `useGameLocal`'s and the cloud API's `victory` status derivation both moved from `chapter1_complete` to `chapter2_complete` accordingly.
2. **22 new events** (`packages/niccolo/src/content/events/chapter2.json`, `ev_c2_001` through `ev_c2_022`) — inside §9's own "25 to 40" range's lower end, deliberately lighter than Chapter 1's 32 since this chapter is one location and one arc rather than Chapter 1's sprawl (recorded as an implementation note in the design doc, Section 9). Covers every named beat: the Trebizond agency assignment, Diniz's mid-campaign join, the Doria rivalry, arrival and the silk market, Emperor David's court (an audience, a request for favour, the Venetian bailo's warning), two secrets (an Ottoman alum ledger, Doria's silent backers), word of the Sultan's muster, the city preparing for what's coming, and the extraction chain itself (objective opens, success, failure, and a matching epilogue either way) into a single finale that sets `chapter2_complete`.
3. **This is the first phase that is actually a new chapter**, so it's the first to ship its own `chapter2.json` per content type (cities, routes, characters, houses, events) rather than growing the existing `chapter1.json` files in place the way Phase 7 grew Naples/cannon into Chapter 1's own files and Phase 8 grew the houses file. `sim/content.ts` concatenates both chapters' arrays into the single `CITIES`/`ROUTES`/`CHARACTERS`/`EVENTS`/`HOUSES` exports every other module already reads from — no consumer needed to know two chapters exist. This establishes the one-file-per-chapter convention for Phase 10 onward.
4. **Trebizond** — a 14th city, one good (silk, cheaper there than anywhere else on the map — the arbitrage the chapter's trade beat is built on), reached by one new sea route from Venice (6 weeks, seasonal — the Black Sea passage). Its own currency, `asper` (`CurrencyId` gained a sixth member), needed a real backward-compatibility fix, not just a new value: an *already-saved* Chapter 1 campaign's `exchangeRates` predates `asper` entirely, and `LedgerPanel`'s currency list reads every `CURRENCIES` entry unconditionally — caught live against the one real pre-Phase-9 campaign on disk (`exchangeRates['asper'].toFixed(2)` throwing on load, not just in the scripted driver, which seeds fresh state and wouldn't have surfaced it). Fixed with a new `withAllCurrencies` helper (`sim/currency.ts`) called from both `useGameLocal`'s and `useGameCloud`'s load paths, backfilling any missing currency at its peg value; `driftExchangeRates` got the same missing-key fallback so a currency absent from a save's `exchangeRates` self-heals into a normal drifting rate on the very next `ADVANCE_WEEK` instead of drifting `undefined` into `NaN` forever.
5. **Pagano Doria** ships as a fourth `House` (hostile, seat Trebizond), reusing Phase 8's "reduced fidelity" model rather than a second cargo hold and ledger. One new capability: `resolveHouseSabotage` (`sim/houses.ts`) — any hostile house with a player vessel carrying cargo docked at its own home city has a 15%/week chance of costing that vessel part of one cargo good. Generalised over "any hostile house," not special-cased to Doria by name (St Pol's own home city, Bruges, already qualifies), so a future chapter's hostile house inherits sabotage for free. A `doria_sabotage_occurred` flag set the week it fires lets content react to it (`ev_c2_017`, "Spoiled goods") without the engine needing to know which house did it.
6. **Diniz joins mid-campaign** — the first real use of the generic mid-campaign join Phase 5's own roster notes flagged as deferred (`banco-di-niccolo-design.md` Section 7). A new `EventEffects.joinCharacter` activates a `status: 'pending'` character (added to the `Character.status` union) already present in save data, or — for a campaign whose save predates that character existing in content at all — adds them fresh from content, so the same event effect works whether the campaign started before or after this deploy. A matching `EventEffects.characterDeparts` scripts Diniz's loss on the extraction's failure branch: a genuine scripted departure, distinct from Phase 5's generic loyalty-zero departure, and a safe no-op if the named character isn't currently active.
7. **The extraction deadline is calendar-relative, not the historical 15 August 1461.** The campaign clock runs continuously across chapters with no per-chapter reset — Phase 7's own verified playthrough already reached `chapter1_complete` at week 86, in-game November 1461, after the real fall of Trebizond would already have happened historically. Pinning the failure trigger to the actual date would already be in the past the moment Chapter 2 unlocks for any normally-paced campaign. The deadline (`dateAfter: "1463-01-01"`) is chosen to sit safely after typical Chapter 1 completion instead — a deliberate accuracy-for-playability trade recorded here and in the design doc rather than left as a silent deviation from this section's own "historical schedule" line.
8. **No new UI screens.** `MapView`, `HousesPanel`, `HouseholdPanel`, `DispatchesPanel`, and `LedgerPanel` all already read their lists generically from content (`CITIES`, `HOUSES`, etc.), so Trebizond, Doria, and Diniz all render correctly through every existing panel with zero component changes beyond widening `MapView`'s SVG `viewBox` (660→780) so Trebizond's marker and label fit on screen.

Also required, not originally itemised: the `withAllCurrencies` backward-compatibility fix (point 4) — found only by testing against a real pre-existing save rather than exclusively fresh scripted state, worth calling out as a reason to keep doing that live-browser check against real saves, not just new ones, whenever a save's *nested* data (not just its top-level shape) gains a new key.

**Verification.** A scripted driver exercised both extraction branches directly against `processAction`/`resolveEvent`, seeding a freshly-created campaign past `chapter1_complete` (the same isolation-testing precedent Phase 7/8 used for anything impractical to click through by hand): the success path — reach Trebizond, buy silk, sail it and Diniz back to Venice ahead of the deadline — reached `chapter2_complete` with `trebizond_extraction_success` set, `trebizond_fallen` unset, and Diniz still active; the failure path — extraction objective opened, clock forced past `1463-01-01` with nothing ever moved to safety — reached `chapter2_complete` with `trebizond_fallen` set and Diniz departed via `characterDeparts`; a simulated legacy save (Diniz filtered out of `characters` entirely before seeding `chapter1_complete`) confirmed `joinCharacter`'s content-fallback path still adds him fresh and active; a direct call confirmed `characterDeparts` no-ops against a character who isn't active; `resolveHouseSabotage` was exercised with `Math.random` forced to fixed values, confirming it fires only for a vessel docked at a hostile house's own home city (Trebizond or Bruges, not London) and reduces cargo only when it does; `driftExchangeRates` was confirmed to backfill a currency missing from a hand-built legacy rates object instead of producing `NaN`. `processAction`'s freeze was confirmed to trigger on `chapter2_complete`, not `chapter1_complete`. Live in the browser: the one real pre-Phase-9 campaign on disk loaded cleanly post-deploy (this is what caught the missing-currency Ledger crash described in point 4, before it could reach production), with Trebizond, Doria, and the `asper` exchange rate all rendering correctly across the map, Dispatches, Household, Houses, and Ledger panels; a fresh campaign's week-0 Chapter 1 event fired and resolved normally; test campaigns created during this check were deleted afterward, leaving only the one real campaign that already existed. `tsc --noEmit` (both the client package and the `api/` package) and `vite build` all clean.

No Chapter 3 content, no production-asset systems (Cyprus sugar estates are Chapter 3's own ask per §12), no branch network. Those remain Phase 10+. **Do not start Phase 10 — stop here per instructions.**

---

## Phase 11 — Done

Phases 0-10 shipped Chapters 1-3 complete (see the Session log's 2026-07-22 Phase 10 entry). Section 12's own phase list implies Phase 11 continues "one chapter content pack per phase" into Chapter 4 (Scales of Gold). Section 14 ("SCOPE CONTROL") directly contradicts that: *"Chapters 1 to 3 are the game. Build those to full quality before any Chapter 4+ content... Do not scaffold all 8 chapters thinly."* This conflict was flagged to the owner before any Phase 11 work started; confirmed 2026-07-22 that Phase 11 honours Section 14 literally — a quality pass on the shipped Chapters 1-3, not Chapter 4 content. Deliverable: **insure a cargo run at Bruges, Venice, or Genoa, watch it survive or get hit by a storm at sea, and see the payout land** — plus two standing regressions closed that every phase since Phase 7/10 had noted and deferred as out of scope.

1. **Cargo insurance and voyage risk** (design doc §4: *"Insurance is purchasable in Bruges/Venice/Genoa at a premium that reflects the insurer's information, not the player's... Cargo in transit is capital at risk"* — named in the Trade System since the design doc's first draft, deferred by Phase 2/3's own implementation notes to "Phase 4," and never actually built by any phase since). New `sim/insurance.ts`: `quoteInsurance` prices a policy from the route's own risk (sea costs more than land, seasonal routes cost more still) plus a surcharge for how stale — or altogether absent — the underwriting city's own knowledge of the destination is, reusing the existing news/latency system's notion of a report's age rather than inventing a second information model just for this. `resolveVoyageRisk` rolls a weekly chance of a storm/piracy loss for any vessel under way with cargo — at most one vessel hit per week, the same one-event-per-week shape `resolveHouseSabotage` already uses — costing part of one cargo good; an active policy on the hit vessel pays out cash scaled to the fraction of coverage actually lost, an uninsured vessel just loses the goods. `DISPATCH_VESSEL` gained an optional `insure` flag: checking it while docked at one of the three underwriting cities quotes and charges the premium at the moment of departure, recording a policy that lapses unused on arrival (`clearArrivedInsurance`) or is consumed by a loss. New `GameState.insurance`/`lastVoyageEvent` fields; extended `useGameLocal.ts`'s save-guard for both. UI folded into the existing dispatch flow in `GameScreen.tsx` (a checkbox plus a persistent "storm struck..." report line) rather than a new Venture Planner screen — see the scope note below.
2. **Map SVG unbounded-height bug** (noted since Phase 7, still present through Phase 10): the root cause was `GameScreen.tsx`'s outer container using `minHeight: '100vh'` rather than a hard `height: '100vh'` — a soft floor doesn't cap flex children, so the sidebar's own stacked-panel content height stretched the whole page (and the map's `height: 100%` svg along with it) taller than the viewport instead of scrolling internally the way `SIDEBAR`'s existing `overflowY: auto` already intended. Fixed by bounding the outer container to a real `height` (plus `overflow: hidden`), which is what lets that pre-existing `overflowY: auto` actually take effect. All sixteen cities (including Naples, Trebizond, Famagusta, and Kouklia near the bottom of the map) now render within the viewport with no page scroll required.
3. **`api/tsconfig.json` JSX-flag noise** (noted since Phase 10): `tsc --noEmit -p api/tsconfig.json` was type-checking every UI component under a blanket `../src/**/*` include, none of which the API build actually uses (only `api/game/index.ts`'s single import of `../../src/sim/state`) or needs JSX support for. Narrowed `include` to `../src/sim/**/*` and `../src/content/**/*` — the only paths any api route actually imports — rather than adding a `jsx` compiler option the API build has no reason to need.

Also required, not originally itemised: a defensive `state.insurance ?? []` fallback in `sim/actions.ts` for a cloud save from before this phase — only `useGameLocal.ts`'s save-guard rejects a too-old shape; `useGameCloud.ts` has never had an equivalent guard for any phase's new top-level `GameState` field, a pre-existing gap this phase didn't attempt to close more broadly.

**Verification.** A scripted driver (`processAction`/`quoteInsurance`/`resolveVoyageRisk` called directly, the same isolation-testing precedent every prior phase used) confirmed: a sea+seasonal route (Bruges-Venice) quotes a higher rate than a land route (Bruges-Ghent) at week 0, when neither destination has a report yet; insurance can only be bought at Bruges/Venice/Genoa, rejected elsewhere; dispatching with `insure: true` charges exactly the quoted premium and records a policy; with `Math.random` forced to fixed values, a storm hit reduces cargo and pays out only when a policy exists, no loss fires when the risk roll misses, and a policy lapses on arrival whether used or not. Live in the browser: bought wool at Bruges, checked "Insure this cargo," sent the ship to Venice, watched cash drop by exactly the quoted 2f premium and the sidebar report "Insured for 21f this voyage"; advancing weeks through two scripted events (the dyeworks apprentice's refusal chain paying out on schedule), a storm struck the ship in week 5, losing its one unit of wool, and the sidebar correctly reported "Insurance paid 21f," confirmed against the cash ledger. Confirmed the map fix visually: a fresh campaign's full 16-city map, including the southern/eastern cities added in Phases 7, 9, and 10, renders within the viewport with the sidebar scrolling internally instead of stretching the page. Test campaigns created during this check were deleted afterward (all local saves — no cloud/production data touched). `tsc --noEmit` (client package) and `vite build` both clean; `tsc --noEmit -p api/tsconfig.json` is now clean (previously noisy — not a regression this phase introduced, see point 3).

No Chapter 4 content (Venice debt pressure, Lisbon, Madeira, the Gambia voyage, the expedition/disease-clock system) — Section 14 holds that back until Chapters 1-3 are judged at full quality, a judgement the owner makes, not Claude Code. No Venture Planner UI screen (design doc §11 screen 6) — insurance was folded into the existing dispatch flow instead, the same consolidation precedent Phase 4 used when it folded "Counting House" into the Ledger screen rather than building a second, mostly-redundant one. **Do not start Chapter 4 content without being asked — Section 14 governs here, not Section 12's phase-number sequence.**

---

## Phase 12 — Done

The design doc's own Chapter 1 line has always said the player should "start as apprentice with 0 capital and 3 friendships," but every phase through 11 actually began with a ship, a courier, and 40 florins already in hand — a simplification Phase 1 made so there was something to sail from turn one. The owner asked for that gap to be closed properly: a real prologue chapter — **Chapter 0, "Claes begins"** — played before Chapter 1, where Claes starts with nothing and the game's systems reveal themselves one at a time as he earns them, handing off into the exact Chapter 1 start Phases 1-11 already built. Alongside it, the interactive guided tour (built in the cross-cutting work below Phase 11) needed to grow from a map/trade walkthrough into one covering Household, Dispatches, and the Ledger too. Planned in full (including two explicit confirmations from the owner: a true zero-capital start rather than the existing resources with panels hidden, and a "Skip the prologue" option defaulting new campaigns into Chapter 0 rather than making it mandatory) before any code was written.

1. **A true zero-capital start, with minimal new engine machinery.** The key finding that shaped this phase: most systems don't need new gating at all. `MarketPanel` and insurance already only render for a vessel with cargo capacity — the starting courier (capacity 0) keeps them dormant for free. `resolveVoyageRisk`, `resolveMaturingObligations`, and condotta/house drift all safely no-op with no cargo, no obligations, and no condotta. The Household roster and Dispatches/news system were already vessel-independent, which is exactly the hook for "3 friendships": `content/characters/chapter1.json` now seeds only Marian, Julius, and Godscalc `active`; Tobie, Gregorio, and Astorre start `pending`, joining narratively through Chapter 0's own events via the *existing* `joinCharacter` effect (built for Diniz's Ch2 join). The only genuinely new gates: `components/LedgerPanel.tsx` and `components/HousesPanel.tsx` both gained a `flags` prop and an early `if (!flags.chapter0_complete) return null`, mirroring `EstatePanel.tsx`'s own existing pattern exactly; and `sim/actions.ts`'s `advanceWeek` suspends `resolveWeeklyUpkeep` (wages, loyalty drift) until `chapter0_complete` — an apprentice doesn't owe the household's wages, that's Marian's problem until he's formally made her factor.
2. **A new `EventEffects.grantVessel`** (`sim/types.ts`/`sim/events.ts`) hands the player a new vessel mid-campaign — a no-op if a vessel with that id already exists, the same idempotency `joinCharacter` already has. Chapter 0's finale uses it to grant `ship_1` (matching the exact id `GameScreen.tsx`/`GuidedTour.tsx` already hardcode elsewhere).
3. **`sim/state.ts`'s `createInitialState`** gained a `{ skipPrologue?: boolean }` options parameter. Default: only the courier, 0f, characters exactly as content authors them (3 active/3 pending), flags empty. `skipPrologue: true`: today's exact pre-Chapter-0 behaviour — both vessels, 40f, and `flags: { chapter0_complete: true }` set explicitly (not left absent, since the new trigger condition needs it truthy) — plus every *Chapter 1 roster* character forced active. Deliberately scoped to just the roster's own three pending ids, not "every pending character," after a scripted-driver check caught it wrongly force-activating Diniz (Chapter 2's own mid-campaign joiner) too on the first pass.
4. **New `content/events/chapter0.json`** (8 events, `ev_c0_001`-`ev_c0_008`, `"chapter": 0`): the dyeworks introduction; a courier errand to Ghent that genuinely teaches the map/dispatch mechanic (the round trip costs real weeks, not a scripted skip); a dye-job commission mirroring `ev_c1_002`'s own cash-vs-conscience shape; Gregorio's and Tobie's narrative joins; a stale-information beat setting up Dispatches' own lesson; and a finale ("Marian's decision") that grants the ship, 40f, joins Astorre, and sets `chapter0_complete` — the entire hand-off mechanism. `content/events/chapter1.json`'s own opening event (`ev_c1_001`) gained one added trigger condition, `"flag": "chapter0_complete"`, alongside its existing date/location checks — Chapter 1 simply waits for Chapter 0 to finish, regardless of calendar date, and a skip-prologue campaign satisfies that flag immediately, firing at week 0 exactly as it always has.
5. **Lobby and API plumbing.** `pages/Lobby.tsx` gained a "Skip the prologue" checkbox; `createGame`'s signature (`useGameLocal.ts`, `useGameCloud.ts`, `useGameHybrid.ts`, and `api/game/index.ts`'s POST body) threads `skipPrologue` through to `createInitialState`. No save-shape guard changes needed — nothing new was added to `GameState`'s top-level shape.
6. **The guided tour grew from 8 to 11 steps** and was re-gated from "week 0, ship at Bruges, undispatched" to `flags.chapter0_complete === true` — Chapter 0 now owns the player's literal first moves, so the tour's job shifted to the systems that come after: a new "Assign an officer" step (Julius, since he's active on every path), "Invest in a courier line," and "Write a bill of exchange," with a revised outro mentioning insurance/Houses/Secrets narratively rather than forcing a third full round-trip. The static `TutorialOverlay`'s auto-show gained the same `chapter0_complete` gate — its "you hold 40 florins, a ship..." opening line isn't true during the prologue itself.

**Two real bugs found and fixed during verification, not anticipated in planning:**
- The guided tour's own resume logic (`firstIncompleteStep`) reset all the way back to the welcome card whenever the tour was interrupted mid-flow by an unrelated scripted event (`GameScreen.tsx` unmounts `GuidedTour` while any event is pending) — because the manual intro/outro steps always report `isComplete() => false`, a naive "skip while complete" scan could never get past step one on a *re*-mount. Fixed by checking whether *any* real (non-manual) step has already completed before deciding to show the intro at all; also had to make two individual step conditions (`Look at Antwerp`, `Confirm the voyage`) persistently true against a vessel's `location`, not just its transient `destination`, since arrival naturally clears `destination` back to null.
- The original demo city chosen for the "invest in a courier line" step, Ghent, sits at Bruges' 1-week base-latency floor — already the fastest any city can be, so its own Invest control is permanently stuck on "Fastest" and can never actually demonstrate the mechanic. Re-checked every neighbour's base latency and switched to London (base 2 weeks, same 15f entry cost).

**Verification.** A scripted driver (`processAction` called directly against `sim/state.ts`/`sim/events.ts`, the same isolation-testing precedent every prior phase used) confirmed: a default campaign starts with only the courier, 0f, and exactly Marian/Julius/Godscalc active (Diniz correctly left untouched, still pending); a `skipPrologue: true` campaign matches today's exact prior behaviour, including `ev_c1_001` firing immediately; walking the full 8-event chain (dispatching the courier to Ghent and back for real) correctly grants `ship_1` and 40f at the finale, activates all 6 roster characters, and fires `ev_c1_001` on the very next `ADVANCE_WEEK` after `chapter0_complete` — not the same tick, matching the existing chained-event precedent. Live in the browser: played a fresh campaign through the entire Chapter 0 chain end to end (courier round trip, the dye-job choice, both narrative joins visible in Household as they happened, Ledger/Houses confirmed absent throughout), watched the finale hand off a ship at 51f (11f earned during the prologue + the 40f stake) straight into `ev_c1_001` and then two more already-due Chapter 1 events in sequence, confirming the continuous campaign clock handles a prologue running ahead of Chapter 1's own dates correctly. Ran the expanded 11-step guided tour twice — once uninterrupted, once deliberately interrupted by a real Chapter 1 event mid-flow to confirm the resume fix — and both times it correctly auto-skipped every already-completed step and landed exactly where real progress left off, including through the new Household/Dispatches/Ledger steps. `tsc --noEmit` (client and `api/tsconfig.json`) and `vite build` all clean throughout. Test campaigns and localStorage state created during this check were cleared afterward.

No changes to Chapters 1-3's own content or systems beyond the single added trigger flag on `ev_c1_001` — this phase is additive, sitting entirely before the existing campaign start. **Chapter 4+ content remains paused per Section 14 — this was a Chapters-1-3-quality/onboarding phase, not a step toward it.**

---

## Phase 12 follow-up #4 — bigger, pannable/zoomable map; medieval castle city icons; sidebar moved left

Owner report right after follow-up #3 shipped: the map itself was "too cluttered and can't see overlap of icons," plus a request for a larger map area, real pan/zoom navigation, larger city icons, a medieval visual style for them, and — mid-implementation — moving the sidebar to the left ("that's more natural"). Given the previous session's visual swing needed a course-correction after implementation, the city icon design was mocked up and approved *before* any code was written this time: https://claude.ai/code/artifact/737981ee-d174-4955-a6a4-2fa4e8270191 (a turreted-castle glyph, option A of three). Pan/zoom mechanism (drag to pan, scroll wheel to zoom toward the cursor) was also confirmed up front via `AskUserQuestion`.

1. **Layout** (`pages/GameScreen.tsx`): sidebar moved to the left of the map (JSX order swapped, `borderLeft`→`borderRight`), narrowed 320px→280px, map pane padding reduced, `MAP_PANE` gained `position: 'relative'` so the new "Reset view" control can anchor to its corner. `<MapView>` gained `key={state.id}`, so pan/zoom state resets cleanly on a new campaign (a remount) without any extra logic inside the component.
2. **Medieval city icons** (`components/MapView.tsx`): the plain city `<circle>` is now a crenellated-castle SVG path — a single-tower version for inland cities (half-width 8, half-height 10), a wider 3-tower version with a small pennant for ports (half-width 12, half-height 14) — same reachable/normal gold-vs-parchment fill and fog-opacity logic as before, no new colors. City labels moved out to `x+14` to clear the wider icon, with two one-city exceptions found by direct visual inspection after the bigger icons shipped: **Ghent** (boxed in by Bruges/Antwerp/Calais on multiple sides) and **Kouklia** (whose label ran straight into neighbouring Famagusta — at ~41 units apart, the tightest city pair in the entire game, tighter than the Bruges cluster) both flip their label to the left (`x-14`, right-aligned) instead. The vessel dock-slot fan-out radius (`DOCK_RADIUS`, from follow-up #3) was recalibrated for the bigger icons using `Math.hypot(halfWidth, halfHeight) + 9` — the diagonal ±60°/±120° slots are the binding case, not straight up, since a castle's crenellations poke out near its top corners.
3. **Pan and zoom** (`components/MapView.tsx`): the outer `<svg viewBox="0 0 780 560">` stays fixed — all existing click hit-testing, label offsets, and vessel-render math keep working unchanged — while everything in-world (sea, landmasses, routes, cities, vessels) now renders inside one inner `<g transform="translate(panX,panY) scale(zoom)">`. The two border-frame `<rect>`s stay *outside* that group (rendered after it) so the pane's edge frame doesn't pan away or have its stroke width scale with zoom. Drag-to-pan uses `window`-level `mousemove`/`mouseup` listeners (not SVG-scoped — a fast drag can exit the map pane into the sidebar, and an SVG-scoped `mouseup` there would never fire, leaving the drag stuck) gated on an `isDragging` state, with drag-start values kept in a `ref` to avoid stale-closure bugs. Scroll-to-zoom is a real native `wheel` listener attached via `useEffect` + a ref (`{ passive: false }`) — **not** JSX `onWheel`, since React registers `wheel` as passive by default and `preventDefault()` inside a synthetic handler is silently ignored, so the page would scroll anyway. Screen-to-viewBox coordinate conversion accounts for the SVG's own letterboxing (`preserveAspectRatio: xMidYMid meet` centers the content within `MAP_PANE` rather than stretching it whenever the pane's aspect ratio doesn't match 780:560) via a shared `getContentScale`/`clientToViewBox` helper, used by both the drag delta and the zoom-toward-cursor math. Zoom is clamped to `[0.7, 2.5]` (lower than an initially-considered 4x cap, since stroke widths and font sizes scale with zoom too and the bigger icons didn't need more headroom). A small "Reset view" button (absolutely positioned, bottom-right of the pane) resets to the exact default `(panX:0, panY:0, zoom:1)`.

**Verification.** `tsc --noEmit` (client + api) and `vite build` clean after each part. Live in the browser: confirmed the enlarged pane and left-sidebar layout; confirmed castle icons render distinctly (port vs. inland) with correct fill/fog; confirmed the 2-vessel dock fan-out still clears the bigger icons at Bruges with no overlap; confirmed the Famagusta/Kouklia pair (deliberately checked as the game's tightest) no longer collides after the label flip; confirmed real drag-to-pan (inspected the resulting `transform` directly — correct direction and magnitude); confirmed scroll-to-zoom keeps the exact content point under the cursor fixed through a zoom step (verified numerically — Bruges stayed at the same screen position to a fraction of a pixel across a zoom from 1.0→1.21); confirmed city click hit-testing is still exactly correct at a non-default pan/zoom (dispatched a real click at the computed screen position for Antwerp mid-zoom, got the correct preview panel and a working confirm-dispatch button, and completed the dispatch); confirmed "Reset view" returns to the exact default transform; confirmed every `GuidedTour`-dependent id (`vessel-button-*`, `household-assign-*`, `dispatches-invest-*`, `city-node-*`, `advance-week-button`) is still present and correctly counted. No console errors.

---

## Phase 12 follow-up #3 — legible vessel markers on the map; combined Fleet & Household list

Continuing the "who is where" complaint from follow-up #2: two visual concepts were prototyped and shown before writing any code, since a redesign is expensive to redo if the direction is wrong. A cold/minimal high-contrast concept was rejected ("prefer the older style which feels more like the books... also all feels very compounded and confused"); a second concept — the exact existing warm palette/serif/landmass map kept as-is, vessels getting a small glyph + name label instead of a plain dot, the sidebar's colored-pill idea dropped for one plain text list — was approved, including the follow-up call that name labels should only show on selection/hover (to avoid crowding at the Bruges hub) rather than always-on.

Investigation found the real gaps: vessels already rendered on the map (`MapView.tsx`) as plain colored circles, but a docked vessel's marker was drawn at the *exact same coordinates* as its own city's dot (`vesselPoint()` returned `{x: at.x, y: at.y}` when `!v.destination`) — invisible when more than one vessel shared a city, with no name and no shape distinction beyond color. Every campaign that finishes Chapter 0 permanently ends up with three vessels (`ship_1`, `courier_1`, `handcart_1` — the handcart is never removed) and `courier_1`/`handcart_1` are both `kind: 'courier'`, i.e. visually identical — this is the *normal* end state, not a rare edge case. Separately, officer location was only visible by scrolling into `HouseholdPanel.tsx`'s per-character dropdowns, disconnected from the vessel list entirely.

1. **`components/MapView.tsx`** — docked vessels now fan out into a small ring of slots around their city instead of sitting on top of it: radius `(c.port ? 7 : 5.5) + 9`px, three fixed angle slots (-120°/-90°/-60°, a 60°-wide arc centered straight up, clear of the city's own label and every route line near the crowded Bruges/Ghent cluster) assigned deterministically by sorting same-city vessels by id — at most 3 vessels ever exist in this game, so no "N here" badge is needed. Each vessel now renders as a distinguishing glyph (ship: small pennant triangle; courier: a dot, plus a chevron under way rotated via `Math.atan2` toward its destination) instead of a plain circle. A name label renders only when a vessel is selected or hovered (new `hoveredVesselId` state, `onMouseEnter`/`onMouseLeave`) — both the deliberate fix for label crowding and the only way to tell `courier_1` and `handcart_1` apart, since their glyphs are otherwise identical. Everything else in this file (landmasses, compass rose, rhumb lines, routes, city circles/labels, border) is untouched.
2. **Real bug caught during verification, not anticipated in planning:** the selected vessel's always-on name label (rendered after, i.e. on top of, the cities) is wide enough to visually cover a nearby city's own dot — reproduced concretely: with the ship selected and docked at Bruges, its "The Charetty ship" label rendered directly over Antwerp's marker (only ~54 units away), silently swallowing clicks meant for Antwerp with no error and no visual sign why. Fixed with `pointerEvents="none"` on the label `<text>` only (not the glyph itself, which still needs to be hoverable) — clicks now pass through the label to whatever's underneath.
3. **`sim/characters.ts`** gained an exported `assignmentSummary(character, vessels)`, moved out of a local, unexported function in `HouseholdPanel.tsx` (alongside the file's existing `activeCharacters`, `tradeBonus`, etc. — the established home for this kind of derived domain text). Fixed a real bug while moving it: the `idle` branch returned the hardcoded literal `'Idle, at Bruges'` instead of deriving from the character's own `location` field (documented in `types.ts` as authoritative) — harmless only because no character's `location` has ever changed in this codebase, but wrong in principle and would have propagated into the new list below had it not been fixed.
4. **`pages/GameScreen.tsx`** — the old plain "Vessels" list is now "Fleet & Household": the same clickable vessel buttons (same ids, same onClick — `GuidedTour.tsx` depends on `vessel-button-{id}` and was unaffected), with any active officer whose assignment is `aboard` that vessel nested directly beneath it, and every other active officer listed below with their own plain assignment line (`Negotiating at Ghent`, `Idle, at Bruges`, etc., via the shared `assignmentSummary`). `HouseholdPanel.tsx` itself is unchanged in structure — its assignment-editing dropdowns stay exactly where they are; the new list is a read-only glance, not a replacement for actually reassigning an officer.

**Verification.** `tsc --noEmit` (client + api) and `vite build` clean after each part. Live in the browser: confirmed 1- and 2-vessel dock slot geometry via exact DOM transform inspection (ship at -60°, courier at -120°, matching the design precisely); confirmed hovering a non-selected vessel shows its label (via a dispatched `mouseover` event — the Browser pane's synthetic `hover` action doesn't reliably trigger React's hit-testing in this harness, a known quirk, not a product bug); confirmed clicking Bruges' own city dot still works with vessels fanned out around it; caught and fixed the label/city-overlap bug above via a concrete repro (dispatch to Antwerp silently failing) before and after the fix; confirmed chevron rotation math exactly via `atan2` (150.945° computed both by the browser and by hand for the Bruges→Ghent bearing); ran the Guided Tour to confirm `vessel-button-{id}` targeting and spotlighting still work correctly against the restructured sidebar; confirmed `household-assign-{id}` (6) and `dispatches-invest-{cityId}` (13) counts intact.

---

## Phase 12 follow-up #2 — dispatch-back bug, silent sabotage loss

Owner report right after the previous follow-up shipped: "cant move dispatch rider back from ghent in week 3," then "cant move handcart back from ghent either" — general complaint that gameplay was confusing, couldn't tell who/what was where, actions weren't clear.

1. **Real bug, not confusion: `CityPreviewPanel`'s dispatch button used the wrong "am I here" check.** `GameScreen.tsx` passed `isLive={dockedCityIds.has(previewCity.id)}` — true whenever *any* vessel was docked at the previewed city, not specifically the *selected* one. Once the handcart (or any second vessel) sat at Bruges, previewing Bruges to send a *different*, elsewhere vessel home showed "You are here — prices are true" and silently hid the entire reachable/confirm-dispatch section, because that section was gated on `!isLive` rather than on whether the selected vessel itself was already there. Reproduced exactly: dispatch rider sent to Ghent, handcart left docked at Bruges — previewing Bruges from the (Ghent-docked) dispatch rider showed no dispatch button at all, matching the report precisely. Fixed in `CityPreviewPanel.tsx` by computing a `selectedVesselHere` check from the selected vessel's own `location`/`destination` and gating the dispatch section on that instead of the global `isLive` flag — `isLive` still drives the price-display branch (any vessel docked there does mean you know the city's true current prices), but no longer decides whether dispatch controls appear for a vessel that isn't the one sitting there.
2. **A connected, real clarity gap: hostile-house sabotage had zero UI feedback.** While investigating, cargo on the handcart (sitting idle, docked at Bruges) dropped from 3 to 2 units with no visible cause. Root cause: `resolveHouseSabotage` (Phase 8, "Doria's sabotage," generalized to any hostile house at its own home city) — St Pol's own home city is Bruges, so a docked cargo vessel at the player's *own home port* can be sabotaged, same as any other hostile house's home city. This is working as designed, not a new bug, but unlike storm/piracy loss (`lastVoyageEvent`, reported in the sidebar) sabotage produced no player-visible message at all — cargo just silently shrank. Added `SabotageLossEvent`/`GameState.lastSabotageEvent` (optional field, so it never invalidates an in-progress save the way a required field would) and a matching sidebar line ("Week N: {house} got to {vessel}'s cargo at {city} — lost X {good}."), mirroring the existing storm-loss report exactly.

**Verification.** Reproduced the exact reported scenario live in the browser (dispatch rider to Ghent, handcart left at Bruges, confirmed the dispatch button was missing before the fix and present after) and confirmed the return dispatch actually completes. A scripted driver forced a sabotage event (mocked `Math.random`) and confirmed the event object's house name, city, vessel, and quantity are all correct and the cargo is actually reduced by the reported amount. `tsc --noEmit` (client + api) and `vite build` clean.

Not yet addressed: the owner's broader "can't clearly see who/what is where" complaint may need more than these two fixes — e.g. vessel markers directly on the map, or a clearer character-location summary in Household. Flagged as a candidate follow-up, not undertaken here without the owner's steer on direction.

---

## Phase 12 follow-up — Chapter 0 given a real trade loop; top bar shows what's held

Owner feedback on the just-shipped Phase 12: "need to make it clearer what you own and hold... in the top bar. Plus the walk through doesn't explain what to do in chapter 0 and there isn't anything to do as no money or chance of exchanging goods. gameplay doesn't make much sense." Both points were real gaps, not misunderstandings — with 0f and a capacity-0 courier, Chapter 0 genuinely had no way to buy or sell anything, and neither the static tutorial nor the guided tour said anything at all during the prologue (both were gated behind `chapter0_complete`).

1. **A real trade loop for Chapter 0.** New `EventEffects.grantCargo` (`sim/types.ts`/`sim/events.ts`, mirrors `grantVessel`'s shape) hands a vessel starter goods with no purchase behind them — the mechanism for giving Claes something to sell before he has any florins of his own to buy with. Rewrote `content/events/chapter0.json`'s opening beats: `ev_c0_001` now also grants a small permanent vessel, **Claes's handcart** (`handcart_1`, capacity 3, land-only) — Julius lending him a cart "if you're carrying messages, you can carry a sack or two alongside them"; `ev_c0_002` loads it with 3 units of madder via `grantCargo` for the Ghent errand. The handcart has a real cargo hold, so `MarketPanel` (already purely capacity-gated) opens for it like any vessel — the player genuinely buys and sells through the same UI the full game uses, just at a small scale. The handcart is never removed; it persists as a minor permanent extra vessel after the finale, alongside the ship.
2. **Two real bugs this exposed, not anticipated in planning**, both in `sim/events.ts`'s `checkTriggers`: (1) only `ev_c1_001` was gated behind `chapter0_complete` — every later Chapter 1 event (`ev_c1_002` onward) triggers on bare `dateAfter`+`location` with no flag at all, authored before Chapter 0 existed. Chapter 0 can now genuinely take several real calendar weeks (two Ghent round trips), so the campaign clock could cross into Chapter 1's own dates while the prologue was still running, misfiring its content mid-Chapter-0 — caught by a scripted driver that advanced enough real weeks to expose it. (2) Symmetrically, Chapter 0's own `ev_c0_001` (bare `location: "bruges"`, no flag) fired even for a **skip-prologue** campaign, showing "Claes at the dyeworks" alongside Chapter 1's real opener. Fixed both with one general rule in `checkTriggers` rather than patching individual trigger objects: Chapter 0 events only fire while `!chapter0_complete`, Chapter 1+ events only fire once it's true — closes the whole class for any future chapter content too, not just today's events.
3. **Household's wage warning was flatly wrong during Chapter 0.** `HouseholdPanel.tsx` always showed "Wages due next week... cannot pay — loyalty will fall" regardless of whether upkeep actually applied — misleading, since `advanceWeek` already suspends wages until `chapter0_complete`. Added a `wagesSuspended` prop (`GameScreen.tsx` passes `!state.flags.chapter0_complete`); shows "Wages: not yet owed — no one draws a salary until you're formally made factor" instead.
4. **Chapter 0 gets its own short "How to play."** `TutorialOverlay.tsx` gained a second, 4-step `variant="chapter0"` step set (distinct `localStorage` seen-flag from the main tutorial) describing the actual starting position — no capital, the handcart, watching for event cards — instead of the main tutorial's "you hold 40 florins, a ship..." framing, which isn't true yet. `GameScreen.tsx`'s auto-show effect now fires once for the Chapter 0 variant immediately (rather than never appearing pre-completion), then once more for the main variant after the finale; the manual "How to play" button picks whichever variant matches the campaign's current state.
5. **Header now shows holdings, not just cash.** `GameScreen.tsx`'s top bar previously read `{cash}f · {date} · conscience {N}` — cargo was only visible by opening each vessel individually in the sidebar. Added a `hold: ...` segment aggregating cargo across every vessel combined (e.g. `51f · hold: 3 Madder · Week of 21 March 1460 · conscience 100`, or `hold: nothing` when empty) — what's owned is visible at a glance regardless of which vessel is selected.

**Verification.** A scripted driver walked the real trade loop end to end: handcart granted at `ev_c0_001`, loaded with madder at `ev_c0_002`, dispatched to Ghent, madder sold via the actual `SELL_GOOD` action (cash 0→54f), then the rest of the chain through to the finale (ship granted, handcart still present at capacity 3, all three joins confirmed). A second driver confirmed skip-prologue campaigns are unaffected (40f, both vessels, no handcart, `ev_c1_001` fires alone, no `ev_c0_001`). Live in the browser: fresh campaign showed the Chapter 0 tutorial automatically ("You are Claes," 4 steps), household showed the corrected wage message, `ev_c0_001`/`ev_c0_002` fired with the new text and granted the handcart with cargo, the header tracked `0f → hold: 3 Madder` and then `51f → hold: nothing` across the buy/sell round trip at Ghent exactly as scripted. `tsc --noEmit` (client + api) and `vite build` clean throughout. Test campaign and localStorage state cleared afterward.

---

## Cross-cutting: onboarding tutorial, guided walkthrough, and map presentation

Requested directly by the owner immediately after Phase 11, not itself a Section 12 phase — same Chapters-1-3-quality-pass spirit Section 14 asks for, so no scope check was needed this time. Three pieces:

1. **Tutorial** — new `components/TutorialOverlay.tsx`: an 8-step modal (the same full-screen-card visual language `EventOverlay` already uses) covering the house's starting position, the map/dispatch mechanic, buying and selling, stale information vs. the live truth, credit and the maturity ladder, household assignments, Phase 11's cargo insurance/voyage risk, and scripted events. Dismissal ("Skip" or finishing on "Begin") is a local browser preference (`localStorage`, via the component's own exported `hasSeenTutorial()`), not `GameState` — the tutorial has no campaign-specific content, so it doesn't belong in a save. Auto-shown once per browser the first time `GameScreen` is reached with no scripted event already blocking the screen (checked against `pendingEvents.length` so it never stacks visually with `EventOverlay`); reachable afterward from a permanent "How to play" button on both `Lobby.tsx` and `GameScreen.tsx`'s header.
2. **Map presentation** — `components/MapView.tsx` previously rendered city dots and route lines on a flat black rectangle; it now draws six hand-fitted landmass shapes (Britain; the Low Countries; Burgundy/Savoy/France; Italy; the Pontic coast near Trebizond; Cyprus) in a parchment-brown tone over a dark sea wash, positioned directly against the existing city `x`/`y` coordinates from all three chapters' content files rather than a real coastline trace — the design doc's own aesthetic direction is "manuscript and counting-house, ink on paper," not cartographic accuracy. Added a jagged ridge line for the Alps between the Burgundy and Italy landmasses (the same divide the Geneva-Milan route's `seasonal` flag already treats as a real crossing), a compass rose and faint rhumb lines in the one empty corner (portolan-chart flavour, decorative only), and a double-line border frame. Route lines and city dots/fog-of-war logic are unchanged — only the backdrop underneath them changed.
3. **Guided, hands-on walkthrough** — new `components/GuidedTour.tsx`, requested as a follow-up ("a function for a play through tutorial") once the static tutorial shipped. A coach-mark tour that spotlights real UI elements (a gold-bordered box computed from each target's actual `getBoundingClientRect()`, dimming everything else) and auto-advances only once the player performs the real action a step asks for, rather than a click-through slideshow. Scoped tightly to a fresh, undispatched campaign at week 0 (`canGuidedTour` in `GameScreen.tsx`): the scripted steps are only true at the exact start, so the entry points (a header button, and a second button on the static tutorial's last step, "Walk me through the first move") are hidden once the ship actually departs. On mount it auto-skips any step already satisfied (e.g. the ship is pre-selected by default), so replaying it mid-setup doesn't force redundant clicks. Target elements gained plain `id` attributes for this to hook into: `vessel-button-{id}`, `market-buy-{goodId}`, `market-sell-{goodId}`, `city-node-{cityId}`, `advance-week-button`, `confirm-dispatch-button`, and the sidebar itself (`id="game-sidebar"`).
4. **Map clicks preview instead of instantly dispatching, with real price visibility** — the owner reported the guided tour's own scripted trade "didn't hold together" (see the bug below) and separately asked for price visibility per city with the ability to "click between" them. Confirmed with the owner: clicking a city (reachable or not, `MapView.tsx`'s `onClick` no longer gated on reachability) now shows a new `components/CityPreviewPanel.tsx` in the sidebar — live prices if the vessel is docked there, the last-known report and its age if not, or "No report yet" — with dispatch demoted to an explicit "Send {vessel} here" confirmation inside that panel, shown only when the previewed city is actually reachable. The previewed city gets a second highlight ring on the map (`previewedCityId` prop) distinct from the existing reachable/unreachable fill, so a click's target is visually obvious. Selecting a vessel now defaults the preview to wherever that vessel currently is, so the panel never opens empty.

**Bug found via user report, fixed same session:** the guided tour's first scripted trade (buy wool at Bruges, sail to Ghent) could not be completed as written — Ghent's market (`content/cities/chapter1.json`) trades only cloth and madder, never wool, so there was no Sell button for it at all once the ship arrived. Worse, even a sellable pairing between those two specific cities would have been a guaranteed loss: wool is produced cheaply at London (10f) and dearest at Bruges (20f, an import price), so buying it at Bruges to carry anywhere was backwards from the very "buy low, sell high" lesson the tour exists to teach. Re-checked every one-hop neighbour of Bruges against the actual market data and replaced the whole scripted trade with a real, verified-profitable one: buy cloth at Bruges (24f), sail to Antwerp (30f) — confirmed against `chapter1.json` directly, not assumed. The tour now also carries the loop through to an actual sale (a new "Sell it at a profit" step) instead of stopping at "advance the clock," so it demonstrates the complete mechanic rather than leaving the player to discover the (previously broken) second half themselves.

Also required, not originally itemised: splitting the old single "dispatch" step into two — "Look at Antwerp" (click the map marker to preview it) and "Confirm the voyage" (click the new confirm button) — to match point 4's preview-then-confirm flow; `TourStep.isComplete`/`targetId` and `GuidedTour`'s props gained a `previewCityId` parameter for this. Also: the sidebar scrolls internally (Phase 11's own height fix), so a step's target can be off-screen below the fold — discovered when the "advance the clock" step's spotlight highlighted nothing visible. `Element.scrollIntoView()` proved unreliable here (observed moving the sidebar's `scrollTop` a few pixels instead of the ~1000px actually needed, for reasons not fully pinned down); replaced with a direct computation against `#game-sidebar`'s own `scrollTop` using the target's and sidebar's `getBoundingClientRect()`, which reliably brings the target on screen. A related bug the same fix surfaced: the callout card's fallback position (when clamped to stay on-screen) could land directly on top of its own spotlighted target; fixed by flipping the card above the target when there isn't room below, instead of clamping into an overlap.

**Verification.** `tsc --noEmit` and `vite build` both clean throughout. Live in the browser: the static tutorial's first step appeared automatically after the week-0 scripted event resolved on a fresh campaign, Next/Back/dot-indicator navigation worked through all 8 steps, and both "Skip" and finishing persisted the seen-flag. Confirmed the map: all sixteen cities across Chapters 1-3 sit visibly within a landmass or on a recognizable coastline, the compass rose and border render without overlapping any city marker. Confirmed click-to-preview: clicking Ghent (undocked, no report yet) showed "No report yet for this city" plus a reachable "Send The Charetty ship here" button without moving the ship; confirming it did dispatch correctly, and previewing it again afterward correctly read "Not reachable directly from the ship's current position." Confirmed the corrected guided tour end-to-end: bought cloth at Bruges (cash 40f→16f), previewed and confirmed Antwerp as the destination across the new split steps, advanced the clock (ship arrived, week 1), sold the cloth at Antwerp for 29f (cash 16f→45f, a real 5f profit), and finished cleanly on the closing step. Test campaigns and localStorage state created during this check were cleared afterward.

---

## Cross-cutting: cloud persistence (start/save/load campaigns via Supabase)

Not a Section 12 phase — a cross-cutting infrastructure request (start new games, save games, and persist them to the same Supabase database as Thrash Margin, inside the same Vercel deployment) applied after Phase 8. Reuses Thrash Margin's existing infrastructure rather than standing up a second one, per the request: the same `users`/`games` tables, the same JWT auth (`_lib/auth.ts`), the same Postgres pool factory (`_lib/db.ts`), and the same CORS handler (`_lib/cors.ts`) — all imported directly by Niccolo's own API routes rather than duplicated.

1. **Schema** — `packages/thrash-margin/server/src/db/schema.sql` gained a `game VARCHAR(16) NOT NULL DEFAULT 'thrash_margin'` column on `games` (both in the `CREATE TABLE` for fresh installs and as an idempotent `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` for the already-deployed Supabase instance), plus an `(owner_id, game)` index. This discriminates which app owns a row so both games' saves can share one table without either one's list/lookup queries seeing the other's rows. **Run against the live production database** (via the Supabase SQL editor) and confirmed present (`information_schema.columns` shows `game character varying DEFAULT 'thrash_margin'::character varying`).
2. **API routes** — `packages/niccolo/api/game/index.ts` (POST create, GET list) and `packages/niccolo/api/game/[id].ts` (GET/PUT/DELETE), all filtering/inserting with `game = 'niccolo'`. Root re-export shims at `api/niccolo/game/index.ts` and `api/niccolo/game/[id].ts` match the existing convention exactly (`export { default } from '../../packages/thrash-margin/api/game/index'`-style). `POST` calls Niccolo's own `createInitialState(id, name)`; `PUT` overwrites the full state blob (the client stays authoritative, same trust model `useGameLocal` already had) and derives `status` from `state.insolvent`/`state.flags.chapter1_complete`.
3. **`GameState.name`** — a new optional field, set by `createInitialState`'s second parameter, so a campaign's chosen name travels with its save instead of being bolted on with an `as unknown as Record<string, unknown>` cast (the pattern Thrash Margin's own API uses).
4. **Client hooks** — `useGameLocal.ts` rewritten from a single `niccolo_save` slot into a multi-campaign index (`niccolo_saves` + `niccolo_save_<id>`, mirroring Thrash Margin's own local-save index exactly), with a one-time migration from the old single-slot format. `useGameCloud.ts` is the cloud equivalent (create/load/dispatch-with-sync/delete/list against the new API routes, syncing full state after every dispatched action — not just at a turn boundary, since Niccolo has no separate submit-at-end-of-turn moment the way Thrash Margin's `END_TURN` does). `useGameHybrid.ts` switches between them based on the shared `tm_token` session Thrash Margin's login already writes — Niccolo still has no accounts of its own, exactly as `lib/portalAuth.ts` already documented.
5. **Lobby and routing** — `pages/Lobby.tsx` (new): list active/finished campaigns with name, week, status, and Continue/Delete; a "Begin a new campaign" name field and button. `App.tsx`'s game-rendering body moved to `pages/GameScreen.tsx`, now reading `id` from the route (`useParams`) and calling `loadGame(id)` on mount; the insolvency and chapter-complete end screens' buttons now return to the lobby instead of resetting in place, and the sidebar gained "← Back to campaigns" and "Abandon this campaign" (delete + return) in place of the old single-slot "Reset campaign." `main.tsx` now wires `BrowserRouter`/`Routes` (`/` → `Lobby`, `/game/:id` → `GameScreen`) — Niccolo had `react-router-dom` as a declared but previously-unused dependency. No separate Login page: Niccolo continues to rely entirely on Thrash Margin's existing `/thrash-margin/login`, reached via the shared `PortalNav`.
6. **Dropped the cross-package `_lib` import and the `@repo/engine` dependency** — the first deploy attempt revealed both were unreliable in Vercel's actual serverless function build (see Verification below for how this was found). `packages/niccolo/api/_lib/{db,auth,cors}.ts` are now local duplicates of Thrash Margin's versions (same env vars, same Postgres instance, same JWT secret — genuinely the same infrastructure, just not a shared source file), and `packages/niccolo/src/sim/clock.ts` duplicates the three tiny pure functions (`dateForWeek`, `formatWeekDate`, `advanceWeek`) Niccolo used from `@repo/engine`. A dedicated `packages/niccolo/api/tsconfig.json` (mirroring Thrash Margin's own working api tsconfig) covers `api/**/*` and `../src/**/*` as one project.

**Verification.** Local esbuild bundling and a mocked-request smoke test both passed before the first deploy, but production still returned `FUNCTION_INVOCATION_FAILED` on every request to `/api/niccolo/game` — Vercel's actual build/runtime behaves differently from a local bundler simulation in ways a local test can't catch. Root-caused with a series of temporary diagnostic endpoints (each isolating one import), redeployed between each: confirmed the cross-package `_lib` import and, separately, the `@repo/engine` workspace package (whose `package.json` points `main` at raw `.ts` source with no compiled output) were each independently unresolvable in the deployed function's filesystem at runtime, even though both bundled and ran fine locally. Fixed by duplicating both (point 6 above) rather than depending on that resolution behaviour. Confirmed fixed with a full production round trip: registered a real test account, created a campaign via `POST /api/niccolo/game`, confirmed it via `GET` (list and single), saved progress via `PUT` and confirmed the change persisted, then deleted it via `DELETE` and confirmed the list emptied again — all against the live Supabase database, no mocks. Test user and any leftover rows were cleaned up afterward. Live in the browser: a fresh Lobby correctly showed no campaigns, "Begin →" created "Campaign #1" and navigated to it, the week-0 event fired and resolved normally, "← Back to campaigns" returned to the Lobby showing the save with its correct week and status, and a full page reload preserved it (this part was local; the production round trip above used the API directly). `tsc --noEmit` and `vite build` both clean throughout.

---

## Decisions needed from you

None outstanding. Phase 11's scope conflict (Section 12's phase sequence implying Chapter 4 content vs. Section 14's "Chapters 1 to 3 are the game" scope control) was flagged and confirmed on 2026-07-22: Phase 11 honours Section 14, delivering a Chapters 1-3 quality pass (cargo insurance/voyage risk, the map height regression, the api tsconfig noise) rather than Chapter 4 content. Recorded in Section 4 and Section 14 of `banco-di-niccolo-design.md`. Chapter 4+ work remains paused until the owner explicitly asks for it.

Phase 9's judgement calls (22 events rather than the fuller "25 to 40" range; a calendar-relative extraction deadline rather than the historically exact 15 August 1461; Trebizond/routes/characters/houses shipped as new `chapter2.json` files rather than grown into the existing `chapter1.json` files) follow the same scope-discipline precedent as every prior phase and are recorded as an implementation note in `banco-di-niccolo-design.md` (Section 9).

The `game` column migration has been run against the live Supabase database and the full cloud save/load round trip is verified in production (see "Cross-cutting: cloud persistence" above). `@repo/engine` is no longer used by Niccolo anywhere (client or API) — its three functions were duplicated into `packages/niccolo/src/sim/clock.ts` after the workspace package proved unreliable in Vercel's serverless function build; the original package is untouched in case anything else still wants it.

Phase 8's scope judgement calls (three houses rather than the fuller §10 roster; reduced-fidelity trade footprint instead of a second simulated player; "plant false news" left unwired for the player's own agents; `rep` wired but unused by existing Chapter 1 content) follow the same scope-discipline precedent as every prior phase and are recorded as implementation notes in `banco-di-niccolo-design.md` (Sections 6 and 10).

Phase 7's scope judgement calls (2 secrets rather than 3; condotta and secrets built as minimal real systems rather than one-off event flavour; Naples added with no market and a placeholder currency; three additive trigger-field extensions; Simon/Jordan/Felix as narrative-only, not roster characters; the chapter exit as a flag rather than an actual Chapter 2 transition) follow the same scope-discipline precedent as every prior phase and are recorded as implementation notes in `banco-di-niccolo-design.md` (Sections 5, 6, 7, 8, and 9).

Phase 6's two scope judgement calls (effects limited to flag/cash/conscience; weekly-tick trigger evaluation) follow the same scope-discipline precedent as every prior phase and are recorded as an implementation note in `banco-di-niccolo-design.md` (Section 8).

Phase 5's three scope judgement calls (roster seeding, assignment-to-system mapping, Conscience trigger) were made under the same scope-discipline precedent as Phases 3/4 — no invented stand-ins for systems that don't exist yet — and are recorded as implementation notes in `banco-di-niccolo-design.md` (Section 7).

Three Phase 4 scope questions were flagged for your input and confirmed on 2026-07-19 — all are now recorded as implementation notes directly in `banco-di-niccolo-design.md` (Section 5) rather than left as open questions:

1. Multi-currency uses 5 currencies grouped by `City.power` (florin/groot/pound/écu/ducat), not the design doc's full historical set — several of which (Ottoman/Mamluk coin) have no city in the Chapter-1 map yet.
2. Bills of exchange have no branch gating in Phase 4 — any city can be used — since the branch network is Phase 5+ content gated behind a manager character.
3. Insolvency is binary (forced haircut sale, then game over) rather than adding a stand-in rescue lever, since the design doc's actual rescue options (Medici support, marriage capital, branch sale) all depend on Phase 5/8 systems.

Two Phase 3 judgement calls were confirmed on 2026-07-19 and are recorded in Sections 4 and 6 of the design doc:

1. News latency is shortest-path over any route (land or sea), not just the land-only courier network Phase 1's dispatch-rider uses.
2. Background price drift (±0.08/week, independent of the player's trades) is confirmed as the intended reading of the design doc's "scarcity drifts with simulated supply flows between nodes" — the magnitude stays a placeholder pending a feel-check once Chapter 1's content pack (Phase 7) lands.

The audit verdict (sibling app, no shared engine) is recorded in `AUDIT.md` for reference.

---

## Session log

### 2026-07-19 — Phase 0
- Pulled latest TM commits from origin/main (was 2 commits behind).
- Completed all 5 Phase 0 tasks.
- No Phase 1 work started.

### 2026-07-19 — Phase 1
- Added `@repo/engine` as a declared dependency of `packages/niccolo` and ran root `npm install` to create the workspace symlink (Phase 0 had left it undeclared).
- Added clock utilities to `packages/engine`.
- Authored the 12-city Chapter 1 map and its 13 routes as JSON content files.
- Built the sim core (types, initial state, pure reducer), the local-storage save hook, the SVG map view, and the App shell (clock display, vessel list, dispatch-by-clicking-a-lit-city, advance-week button, reset button).
- Verified end-to-end in the browser: dispatched the ship Bruges → Venice, advanced 8 weeks, confirmed arrival and date math, confirmed save persists across reload, confirmed the courier is blocked from sea routes.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 2
- Authored 8 goods and extended all 12 cities with per-good base market prices as JSON content.
- Added `BUY_GOOD`/`SELL_GOOD` actions, cargo + capacity on `Vessel`, and a scarcity-based price/drift model (`sim/market.ts`) to the sim core.
- Built `MarketPanel` and wired cash/cargo display into the App shell.
- Added a defensive check to discard pre-Phase-2 saves missing the new fields instead of crashing on them.
- Verified end-to-end in the browser: bought wool cheap in London, sold it dear in Bruges for a 40f profit, bought cloth with the proceeds, confirmed prices move with trades and confirmed the save (including cargo and cash) survives a page reload.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 3
- Added `NewsItem`, `pendingNews`, `knownPrices`, `courierInvestment` to the sim types and state.
- Built `sim/news.ts`: Dijkstra-based latency-from-Bruges per city (over all routes, not just land), weekly news generation, arrival resolution, and courier-investment cost/floor logic.
- Added `applyBackgroundFlows()` to `sim/market.ts` — small weekly random drift independent of the player's own trades, so stale reports can actually be wrong by the time they arrive.
- Added the `INVEST_COURIER` action and wired news generation/arrival into `ADVANCE_WEEK`.
- Built `DispatchesPanel` (per-city report age, next-arrival week, latency, invest button) and wired it into the App shell; added information-age fog to `MapView`.
- Extended the save-guard to discard pre-Phase-3 saves missing the new fields.
- Verified end-to-end in the browser: confirmed the week-0 fog-of-war baseline (only Bruges live), watched near-city reports arrive on schedule, invested in London's courier line and watched its latency and button state update, sailed to Dijon and confirmed the true dockside price had drifted from base under background flows the player couldn't see coming, and confirmed the save (news queue, courier investment) survives a page reload. Reset the campaign to a clean state afterward.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 4
- Flagged 3 scope questions before starting (currency set, bill gating, insolvency rescue) — all confirmed with the recommended option.
- Added `CurrencyId`/`Currency`/`ExchangeRates`/`Obligation` types; gave every Chapter-1 city a `currency` field grouped by `power`.
- Built `sim/currency.ts` (5 pegged currencies, weekly mean-reverting drift) and `sim/credit.ts` (bills, deposits, loans, discounting, maturity resolution, forced-liquidation insolvency logic).
- Wired `WRITE_BILL`/`TAKE_DEPOSIT`/`WRITE_LOAN`/`DISCOUNT_OBLIGATION` actions; extended `advanceWeek` to drift exchange rates and resolve maturing obligations before price drift each week; gated `processAction` to a no-op once `insolvent`.
- Extended the save-guard for the new fields; rounded cash displays for fractional-florin interest math.
- Built `LedgerPanel` (balance sheet, maturity ladder, currency rates, all three instrument forms) and wired it into `App.tsx`, including an insolvency game-over screen.
- Verified end-to-end in the browser: borrowed against a bill, leveraged the proceeds into a profitable wool run, watched the bill auto-settle from cash at maturity with FX drift affecting the exact amount owed, confirmed loan-out and early discounting, then deliberately triggered insolvency (over-leveraged with no liquid cargo) to confirm the game-over screen and reset flow, and reset to a clean campaign afterward.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 5
- Authored the six-officer Chapter 1 roster (Marian, Julius, Tobie, Godscalc, Gregorio, Astorre) as JSON content, each with skills, loyalty, salary, and a starting `idle` assignment.
- Added `Character`/`CharacterAssignment` types, `GameState.characters`/`conscience`, and the `ASSIGN_CHARACTER` action.
- Built `sim/characters.ts`: `assignCharacter`, `negotiateDiscount` (law skill → credit-rate discount), `investigateLatencyBonus` (intrigue skill → news-latency cut), `tradeBonus` (trade skill → buy/sell price edge), `resolveWeeklyUpkeep` (salary payment, loyalty drift, loyalty-zero departure), and `applyConscienceCost` (Conscience spend + Godscalc/Tobie loyalty penalty).
- Wired all four into the existing systems rather than inventing new ones: `news.ts`'s latency functions and `credit.ts`'s bill/deposit/loan writers now take `characters` into account; `actions.ts`'s `buyGood`/`sellGood` apply the trade bonus and `advanceWeek` runs weekly upkeep; `WRITE_LOAN` with `kind: 'prince'` now costs Conscience.
- Built `HouseholdPanel` (roster, skills, loyalty, salary, a combined assignment dropdown per officer, Conscience/wages summary) and wired it into `App.tsx`; added a Conscience readout to the header; taught `DispatchesPanel` to name an investigating officer inline with a city's latency.
- Extended the save-guard for `characters`/`conscience`.
- Verified end-to-end in the browser: negotiated a bill's rate down via Julius at Bruges, cut London's report latency in half by assigning Tobie to investigate there (confirmed against the courier-investment button switching to "Fastest"), watched a wool purchase get cheaper with Marian aboard the ship, wrote a prince loan and watched Conscience and Godscalc/Tobie's loyalty fall while the rest of the roster was untouched, then starved the payroll on purpose and watched every active officer's loyalty drop uniformly on the next `ADVANCE_WEEK`. Reset the campaign to a clean state afterward.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 6
- Added `ScriptedEvent`/`EventTrigger`/`EventChoice`/`EventEffects` types and `GameState.flags`/`firedEvents`/`pendingEvents`; added the `RESOLVE_EVENT` action.
- Built `sim/events.ts`: `triggerMatches` (date/location/flag, AND-combined) and `checkTriggers` (queues newly-satisfied events, never refires a fired one) and `resolveEvent` (applies the front-of-queue choice's effects, marks it fired).
- Wired `checkTriggers` into `createInitialState` and the end of `advanceWeek`; `processAction` now blocks every action except `RESOLVE_EVENT` while an event is pending, mirroring the existing `insolvent` short-circuit.
- Authored 10 test events as JSON (`content/events/chapter1.json`), including two flag-gated chains, to exercise every trigger and effect field without writing any Chapter 1 narrative content.
- Extended the save-guard for the new fields; built `EventOverlay` (title, body, one button per choice, full-screen modal) and wired it into `App.tsx`.
- Created `.claude/launch.json` (it didn't exist yet) so the niccolo dev server could be previewed for verification.
- Verified end-to-end in the browser: a week-0 date+location event fired and blocked the UI immediately on a fresh campaign; a second date-triggered event fired on schedule three weeks later; refusing its shortcut choice set a flag whose chained follow-up event fired only on the *next* week's tick (not instantly), paying out cash on resolution; confirmed normal play was inert while an event card was open and resumed immediately after resolving it. Reset the campaign afterward.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-19 — Phase 7
- Added three additive `EventTrigger` fields (`flags`, `flagAbsent`, `cargoAtLeast`) and two additive `EventEffects` fields (`secret`, `condotta`) to `sim/types.ts`; wired all five into `sim/events.ts`'s `triggerMatches`/`resolveEvent`.
- Built `sim/secrets.ts` (`addSecret`, `useSecret`, `resolveSecretExpiry`) and `sim/condotta.ts` (`startCondotta`, `resolveWeeklyCondotta`); added `GameState.secrets`/`condotta` and the `USE_SECRET` action; wired weekly condotta resolution and secret expiry into `advanceWeek`.
- Added a `medici_favor` discount hook to `writeBill` in `sim/credit.ts` (Florence only); added the `chapter1_complete` freeze to `processAction`, mirroring `insolvent`.
- Added Naples (13th city, no market) and a Genoa–Naples sea route to the Chapter 1 map content; added a `cannon` good sellable only at Milan.
- Authored 22 new scripted events (`ev_c1_011` through `ev_c1_032`) covering every named Chapter 1 beat: courier-round arrivals, pawnbroking, the Medici relationship, the ostrich errand, two alum-adjacent secrets, the St Pol/Ribérac vendetta escalating to first blood (with a bridging event added so the previously dead-end `simon_ignored` branch also reaches the vendetta), Astorre's Naples condotta, the cannon shipment logistics set piece, marriage to Marian, Felix's death, and a five-thread finale that sets `chapter1_complete`.
- Built `SecretsPanel` and a condotta status line in `HouseholdPanel`; wired both into `App.tsx` along with a chapter-completion ending screen (mirroring the existing insolvency screen) and the `USE_SECRET` dispatch.
- Extended the save-guard in `useGameLocal.ts` for `secrets`/`condotta`.
- Verified with a scripted driver calling `processAction` directly (a ~90-week, 32-event campaign isn't practical to click through by hand for verification): two full playthroughs — one delivering the cannon shipment successfully via Milan→Genoa→Naples, one declining it and taking the `simon_ignored` vendetta branch — both reached `chapter1_complete: true` cleanly with all five finale flags set correctly and no stuck events. Isolated checks confirmed `cargoAtLeast` fires the shipment's success event only at 6+ cannon (not at 3), its `flagAbsent` guard prevents the success/failure events from double-firing, `USE_SECRET` pays out once and rejects reuse or use-after-expiry, and the Medici discount applies at Florence only.
- Verified live in the browser: clean load with no console errors, the week-0 event resolves normally, Naples renders correctly in the map, both assignment dropdowns, and the bill-city selector, and the new Household condotta line displays. Noted (but did not fix, as out of this phase's scope) a pre-existing CSS issue where the map SVG's height isn't bounded on tall viewports, requiring a page scroll to reach cities near the bottom of the map.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-20 — Phase 8, plus cloud persistence
- Added `House`/`Agent`/`AgentPlacement` types, `GameState.houseRelations`/`agents`, and the `PLACE_AGENT` action; authored `content/houses/chapter1.json` (Medici, St Pol/Ribérac, Genoese).
- Built `sim/houses.ts`: `driftHouseRelations` (mean-reversion toward baseline, souring further once `stpol_first_blood` is set), `applyHouseTradeFootprint` (each house nudges its home city's scarcity weekly via the existing `adjustScarcity`), `placeAgent`/`cityIsShielded`/`agentsInHouse` (escalating-cost agent placement), `corruptNews` (a hostile house's chance to silently plant a false report in an unshielded, non-home city), and `resolveWeeklyAgentIntelligence` (a house agent's chance to surface that house's insider secret via the existing Phase 7 Secret system).
- Wired all of the above into `sim/actions.ts`'s `advanceWeek` and added the `rep` effect (`houseId -> relation delta`) to `sim/events.ts`'s `resolveEvent`, giving design doc §8's own example JSON a real home at last.
- Built `HousesPanel` (houses with disposition/relation, placed agents, and a place-agent control) and wired it into the sidebar; extended the save-guard for `houseRelations`/`agents`.
- Verified Phase 8 with a scripted driver forcing `Math.random`: `corruptNews` distorts unshielded reports and never touches Bruges or a shielded city; a 150-week `ADVANCE_WEEK` loop confirmed an agent inside St Pol's house eventually surfaces its secret, all relations stayed within [0,100], and St Pol's relation had fallen well below baseline by the `chapter1_complete` playthrough's end (week 86), confirming the vendetta penalty fired. Live in the browser: placed a named agent shielding London (cash 40f→15f), confirmed it displays, confirmed the next agent's cost escalated to 50f. `tsc --noEmit` and `vite build` both clean.
- **Cross-cutting persistence** (not a Section 12 phase — a follow-on infrastructure request): added a `game` discriminator column to Thrash Margin's existing `games` table (schema + idempotent migration; not yet run against the live Supabase instance — no credentials available this session, see "Decisions needed from you"); built `packages/niccolo/api/game/{index,[id]}.ts` reusing Thrash Margin's `_lib` (db/auth/cors) directly, plus root re-export shims matching the existing convention; gave `GameState` a proper `name` field; rewrote `useGameLocal` from a single save slot into a multi-campaign index (mirroring Thrash Margin's own local-save pattern) and added `useGameCloud`/`useGameHybrid`; built a new `Lobby` page and moved the game UI to `GameScreen` (route-driven via `useParams`), wiring `react-router-dom` into `main.tsx` for the first time (it was a declared but unused dependency).
- Verified the API import chain (cross-package `_lib` + Niccolo's sim engine + `@repo/engine` + JSON content) bundles and executes cleanly via esbuild plus a smoke test against a mocked request and an intentionally-unreachable database. Verified the full local flow live in the browser: Lobby → begin campaign → play → back to Lobby → reload, all correct.
- `tsc --noEmit` and `vite build` both clean.

### 2026-07-21 — Cloud persistence: migration run, production deploy debugged and fixed
- Ran the `game` column migration directly against the live Supabase database via the SQL editor; confirmed the column exists with the correct type and default.
- Production still returned `FUNCTION_INVOCATION_FAILED` on every `/api/niccolo/game` request despite the prior session's clean local smoke test — Vercel's real build/runtime doesn't behave identically to a local esbuild simulation. Root-caused with a series of temporary, single-purpose diagnostic endpoints (each redeployed and curled in turn, then removed): isolated the failure first to the cross-package import of Thrash Margin's `_lib` files, then — after fixing that by duplicating them locally — to the `@repo/engine` workspace package specifically (its `package.json` points `main` at raw `.ts` source with no compiled output, which Vite handles fine but Vercel's function builder can't resolve at runtime).
- Fixed both: duplicated `_lib/{db,auth,cors}.ts` into `packages/niccolo/api/_lib/` (same env vars, same DB, same JWT secret — genuinely the same infrastructure, not a shared source file), duplicated the three functions Niccolo used from `@repo/engine` into a new `packages/niccolo/src/sim/clock.ts` and repointed all three call sites, and added a dedicated `packages/niccolo/api/tsconfig.json` mirroring Thrash Margin's own working api tsconfig.
- Along the way, discovered and worked around a Vercel serverless-function-count limit on the current plan (adding several diagnostic endpoints at once pushed past it, surfacing as silent 404s rather than deploy errors) by keeping the function count low and iterating on one diagnostic file in place instead of adding new ones.
- Confirmed the fix with a full production round trip against the live API and database: registered a real test account, created a campaign, listed it, fetched it, saved a change via `PUT` and confirmed it persisted, deleted it via `DELETE` and confirmed the list emptied. Cleaned up the test user and any rows afterward.
- Removed all temporary diagnostic endpoints once the real fix was confirmed.

### 2026-07-22 — Phase 9, Chapter 2 content pack (The Spring of the Ram)
- Lifted `chapter1_complete`'s hard freeze in `sim/actions.ts`'s `processAction`, moved the freeze to a new `chapter2_complete`, and pointed Chapter 2's opening event at the `chapter1_complete` flag directly, so a completed Chapter 1 campaign now flows straight into Chapter 2 on its own clock instead of ending there. Replaced `GameScreen.tsx`'s Chapter 1 ending screen with a Chapter 2 one in the same spot; moved `useGameLocal`'s and the cloud API's (`packages/niccolo/api/game/[id].ts`) `victory` status derivation from `chapter1_complete` to `chapter2_complete`.
- Shipped Chapter 2 as new `chapter2.json` files per content type (`cities`, `routes`, `characters`, `houses`, `events`) rather than growing the existing `chapter1.json` files in place, establishing the one-file-per-chapter convention for future phases; updated `sim/content.ts` to concatenate both chapters' arrays into the single exports every other module reads.
- Added Trebizond (14th city, one good — silk, cheapest on the map — reached by a new 6-week seasonal Venice–Trebizond sea route) and its own currency `asper` (`CurrencyId` gained a sixth member; `CURRENCIES`/`PEG` in `sim/currency.ts` extended).
- Added Pagano Doria as a fourth `House` (hostile, seat Trebizond); built `resolveHouseSabotage` in `sim/houses.ts` (any hostile house with player cargo docked at its own home city has a weekly chance of costing part of one cargo good — generalised over any hostile house, not special-cased to Doria) and wired it into `advanceWeek`, including a `doria_sabotage_occurred` flag content can react to.
- Added `Character.status: 'pending'` and a new `EventEffects.joinCharacter` (activates a pending character already in save data, or adds one fresh from content for a save that predates them) and `EventEffects.characterDeparts` (a scripted departure distinct from Phase 5's loyalty-zero one); wired both into `sim/events.ts`'s `resolveEvent`. Authored Diniz as Chapter 2's mid-campaign joiner.
- Authored 22 new events (`ev_c2_001`–`ev_c2_022`): the Trebizond agency assignment, Diniz's join, the Doria rivalry, arrival and the silk market, Emperor David's court, two secrets, word of the Sultan's muster, the city preparing, and an extraction chain (objective/success/failure/epilogue) into a finale setting `chapter2_complete`. Used a calendar-relative deadline (`dateAfter: "1463-01-01"`) rather than the historical 15 August 1461 fall of Trebizond, since the continuous campaign clock would already be past any fixed historical date by the time Chapter 2 unlocks for a normally-paced campaign (Phase 7's own verified playthrough reached `chapter1_complete` at week 86, in-game November 1461).
- Widened `MapView`'s SVG `viewBox` (660→780) so Trebizond's marker and label fit; no other UI component needed changes since all of them already read their lists generically from content.
- Verified with a scripted driver against `processAction`/`resolveEvent` directly: success path (reach Trebizond, buy silk, sail it and Diniz back to Venice ahead of the deadline) reached `chapter2_complete` with `trebizond_extraction_success` set and Diniz active; failure path (deadline forced past without ever moving anything to safety) reached `chapter2_complete` with `trebizond_fallen` set and Diniz departed; a simulated legacy save (Diniz absent from `characters`) confirmed `joinCharacter`'s content-fallback path; `characterDeparts` confirmed to no-op on an already-inactive character; `resolveHouseSabotage` confirmed with forced `Math.random` values to fire only for a vessel docked at a hostile house's own home city.
- Live in the browser, loading the one real pre-Phase-9 campaign already on disk, hit a genuine regression the scripted driver couldn't catch (it only seeds fresh state): `LedgerPanel` crashed reading `exchangeRates['asper']` on a save from before that currency existed. Fixed with a new `withAllCurrencies` helper (`sim/currency.ts`) called from both `useGameLocal`'s and `useGameCloud`'s load paths, backfilling any missing currency at its peg value, plus the same missing-key fallback added to `driftExchangeRates`. Re-verified live: the same campaign now loads cleanly with Trebizond, Doria, and the asper rate all correct across every panel; a fresh campaign's week-0 event still fires and resolves normally. Test campaigns created during this check were deleted afterward.
- `tsc --noEmit` (client package and the `api/` package) and `vite build` both clean.

### 2026-07-22 — Phase 10, Chapter 3 content pack (Race of Scorpions) + production assets

- Added `CurrencyId`'s seventh member, `bezant` (Lusignan Cyprus, pegged low), to `sim/types.ts`/`sim/currency.ts` — `driftExchangeRates`/`withAllCurrencies` picked it up for free since both already iterate the `CURRENCIES` array rather than naming currencies individually.
- Built `sim/estates.ts`, the production-asset system design doc §12 scopes to this phase: `Estate { cityId, goodId, stage, weeksInStage, stockpile }` on `GameState.estate`, singular and Kouklia-only on this first outing (the same reduced-fidelity discipline Phase 7's condotta and Phase 8's houses used for theirs). `plant` folds into the new `ESTABLISH_ESTATE` action (25f, gated on a `kouklia_estate_available` flag); `growing` (8 weeks) and `refining` (3 weeks → a 15-unit batch) advance automatically every `ADVANCE_WEEK` via `resolveWeeklyEstate`; `ready` is the one stage that waits on the player's own `HARVEST_ESTATE` action rather than ticking through silently; `ship` is the new `SHIP_ESTATE_GOODS` action, loading the stockpile onto a docked vessel exactly like any cargo bought at a market, so refined sugar sells through the existing trade system rather than a second parallel one. Wired all three actions into `sim/actions.ts` and `estate: null` into `sim/state.ts`'s initial state.
- Authored Chapter 3 as its own content pack per Phase 9's one-file-per-chapter convention: `content/cities/chapter3.json` (Famagusta, the port and siege backdrop, with a `sugar` market; Kouklia, the estate, with no market of its own — Naples's and Trebizond's own precedent for a city that isn't a trading post), `content/routes/chapter3.json` (Venice–Famagusta sea, 5 weeks; Famagusta–Kouklia land, 2 weeks), `content/goods/chapter3.json` (`sugar`, anticipated but never shipped since design doc §4's original goods list), and `content/events/chapter3.json` (25 events, `ev_c3_001`–`ev_c3_025`) — at the low end of §9's "25 to 40" range, matching Chapter 2's own precedent for a one-location, one-arc chapter. No new House this phase: Cyprus's antagonist is a historical siege, not a rival trading company, so unlike Trebizond's Doria this chapter adds no house content at all.
- The political beat (design doc's "choose or be forced between Zacco and Carlotta") is `ev_c3_004` (back Zacco, back Carlotta, or name neither) feeding `ev_c3_005` (forced allegiance to Zacco, the historical victor, once neutrality's floor date passes) — naming a side explicitly is never overridden, but `ev_c3_008` still charges the cost of having backed Carlotta once her cause fails on the historical schedule regardless of what the player believed when they chose her, echoing Chapter 2's own "history is the deadline" lesson. Katelina's scripted end (`ev_c3_018`) follows Felix's Chapter 1 precedent exactly — narrative-only, not a `Character` record, so no new engine hook was needed. The Famagusta siege race (`ev_c3_019`–`ev_c3_021`) mirrors Trebizond's extraction pattern (`location` + `cargoAtLeast` success trigger, `dateAfter` + `flagAbsent` failure fallback), with the failure deadline set to `1466-06-01` — well past any plausible Chapter 3 start date, the same generous-buffer reasoning Phase 9 used for Trebizond's own deadline, given the campaign clock runs continuously with no per-chapter reset.
- `famagusta_siege_resolved` + `katelina_lost` + `cyprus_branch_opened` gate `chapter3_complete`, which lifts and replaces `chapter2_complete`'s hard freeze in `sim/actions.ts`'s `processAction` the same way Phase 9 moved the freeze off `chapter1_complete` — updated `useGameLocal.ts`'s `statusOf`/`isCurrentShape`, `packages/niccolo/api/game/[id].ts`'s status derivation, and `GameScreen.tsx`'s ending screen (now Chapter 3's) to match. Built `components/EstatePanel.tsx` (offer/establish, stage + stockpile display, harvest button, load-onto-docked-vessel control) and wired it into `GameScreen.tsx`'s sidebar.
- Verified with a scripted driver (seeding a fresh campaign past `chapter2_complete` directly, the same isolation-testing precedent every prior phase used, since playing two full prior chapters live isn't practical) exercising three branches against `processAction`/`resolveEvent`: the success path (back Zacco, establish/grow/harvest/refine the estate, ship 10 sugar to Venice ahead of the siege) reached `chapter3_complete` with `famagusta_siege_survived` set and `famagusta_fallen` absent; the failure path (back Carlotta, run the clock past both the 1464 political deadline and the 1466 siege deadline with nothing ever shipped) reached `chapter3_complete` with `carlotta_cause_failed`, `famagusta_fallen`, and `katelina_lost` all set; the neutral path confirmed `chose_neutral_cyprus` holds off any allegiance until the forced-hand deadline, then flips to `sided_zacco`/`zacco_forced`. Confirmed the estate's own mechanics directly: growing reaches `ready` at exactly 8 weeks, harvest moves it to `refining`, refining yields exactly a 15-unit stockpile after 3 weeks and loops back to `growing`, and `SHIP_ESTATE_GOODS` correctly moves cargo between the estate's stockpile and a docked vessel's hold.
- Live in the browser: found and deleted one stale pre-Phase-10 local save missing the `estate` field, confirming `isCurrentShape`'s save-guard correctly rejects it rather than crashing (the same "discard rather than throw" precedent every prior phase's guard used) rather than a live regression to chase. A fresh campaign's week-0 event still resolves normally with `EstatePanel` correctly invisible until the estate is offered; the map (already widened to 780×560 in Phase 9) renders all sixteen cities including Famagusta and Kouklia with no further `viewBox` change needed. Injected `kouklia_estate_available` into a live save to exercise `EstatePanel` end-to-end in the browser (offer → establish click → growing display), confirming no console errors. Noted, but did not touch (pre-existing and out of this phase's scope, confirmed via `git stash` that it predates every change here): the map SVG still renders at an unbounded height on tall viewports (Phase 7's own noted issue), and `packages/niccolo/api/tsconfig.json`'s broad `../src/**/*` include reports JSX-flag noise across every existing panel component, not just this phase's new one — the actual build (`vite build`) is unaffected and clean.
- `tsc --noEmit` (client package) and `vite build` both clean. `tsc --noEmit -p api/tsconfig.json` reports pre-existing, unrelated JSX-flag noise (see above) affecting every panel component, not a regression from this phase's changes.

### 2026-07-22 — Phase 11, trade risk & insurance (Section 14 quality pass, not Chapter 4)

- Flagged a scope conflict before starting: Section 12's phase sequence implies Phase 11 continues into Chapter 4, but Section 14 ("SCOPE CONTROL") says Chapters 1-3 should reach full quality first and Chapter 4+ content should wait. Confirmed with the owner: Phase 11 follows Section 14, delivering a Chapters 1-3 quality pass instead.
- Built `sim/insurance.ts`: `quoteInsurance` (route risk + seasonal surcharge + a destination-info-staleness surcharge reusing the existing news/latency system), `resolveVoyageRisk` (weekly storm/piracy chance for any vessel under way with cargo, one vessel hit per week at most, mirroring `resolveHouseSabotage`'s shape; insured vessels get a cash payout scaled to the fraction of coverage lost, uninsured vessels just lose the goods), `clearArrivedInsurance` (a policy lapses unused once its vessel arrives).
- Added `Insurance`/`VoyageLossEvent` types and `GameState.insurance`/`lastVoyageEvent`; extended `DISPATCH_VESSEL` with an optional `insure` flag, wired through `dispatchVessel` (quotes and charges the premium, records a policy) and `advanceWeek` (resolves voyage risk, applies payout/loss, records the last event) in `sim/actions.ts`. Extended `useGameLocal.ts`'s save-guard for both new fields; added a defensive `state.insurance ?? []` fallback for cloud saves predating this phase.
- Built the insurance UI directly into the existing dispatch flow in `GameScreen.tsx` rather than a new Venture Planner screen: a checkbox ("Insure this cargo before it departs") shown when the selected vessel is docked at Bruges/Venice/Genoa with cargo aboard, an active-policy summary line while under way, and a persistent "storm struck..." report line showing the most recent voyage-loss event and whether insurance paid out.
- Fixed the map SVG unbounded-height bug (noted since Phase 7): `GameScreen.tsx`'s outer container used `minHeight: '100vh'` instead of a hard `height: '100vh'`, so the sidebar's stacked-panel content stretched the whole page — and the map's `height: 100%` svg with it — instead of scrolling internally the way `SIDEBAR`'s existing `overflowY: auto` intended. Bounded the container to a real `height` plus `overflow: hidden`.
- Fixed `api/tsconfig.json`'s JSX-flag noise (noted since Phase 10): narrowed `include` from a blanket `../src/**/*` to `../src/sim/**/*` and `../src/content/**/*` — the only paths any api route actually imports (verified via grep) — instead of adding a `jsx` compiler option the API build never needs.
- Verified with a scripted driver (`processAction`/`quoteInsurance`/`resolveVoyageRisk` called directly): sea+seasonal routes quote higher than land routes; insurance is rejected outside Bruges/Venice/Genoa; dispatching with `insure: true` charges exactly the quoted premium; with `Math.random` forced, a storm reduces cargo and pays out only when insured, no loss fires on a missed roll, and a policy lapses on arrival. Live in the browser: bought wool at Bruges, insured a voyage to Venice for a 2f premium (sidebar confirmed "Insured for 21f this voyage"), advanced through two scripted events, and watched a storm hit in week 5 — cargo lost, sidebar correctly reported "Insurance paid 21f." Confirmed the map fix visually (all 16 cities fit within the viewport, sidebar scrolls internally). Test campaigns (local saves only) deleted afterward.
- `tsc --noEmit` (client package) and `vite build` both clean; `tsc --noEmit -p api/tsconfig.json` now clean (previously noisy).

### 2026-07-22 — Onboarding tutorial and map presentation (requested directly, not a phase)

- Built `components/TutorialOverlay.tsx`: an 8-step modal covering the starting position, map/dispatch, trade, stale information, credit/the maturity ladder, household assignments, cargo insurance/voyage risk, and scripted events. Dismissal persists via the component's own `hasSeenTutorial()`/`localStorage` check, not `GameState` — it's a browser preference, not campaign data.
- Wired an auto-show effect into `GameScreen.tsx` (once per browser, only once no scripted event is already blocking the screen) and a permanent "How to play" button into both `GameScreen.tsx` and `Lobby.tsx`.
- Redesigned `components/MapView.tsx`: added six hand-fitted parchment-toned landmass shapes (Britain, Low Countries, Burgundy/Savoy/France, Italy, the Pontic coast near Trebizond, Cyprus) over a dark sea wash, positioned against the existing city coordinates across all three chapters; added an Alps ridge line, a compass rose, faint rhumb lines, and a border frame. Route/city/vessel rendering logic unchanged.
- Verified live in the browser: tutorial auto-shows after the week-0 event resolves, step navigation and dot indicator work, dismissing from either the Lobby or the game screen persists across both; the redesigned map shows recognizable, distinct landmasses and sea for all sixteen cities with no overlap on the compass rose or border. `tsc --noEmit` and `vite build` both clean.

### 2026-07-22 — Guided, hands-on tutorial walkthrough (follow-up request)

- Built `components/GuidedTour.tsx`: a 6-step spotlight coach-mark tour (select ship → buy wool → dispatch to Ghent → advance the clock → done) that auto-advances only when the player performs the real action, not on a click-through timer. Added plain `id` attributes to the real target elements (`vessel-button-{id}` in `GameScreen.tsx`, `market-buy-{goodId}` in `MarketPanel.tsx`, `city-node-{cityId}` in `MapView.tsx`, `advance-week-button` and `game-sidebar` in `GameScreen.tsx`) for the tour to spotlight via `getBoundingClientRect()`.
- Gated behind `canGuidedTour` in `GameScreen.tsx` (week 0, ship docked at Bruges, undispatched) since the scripted steps only make sense at the exact campaign start; wired two entry points — a header button, and a new second button on the static `TutorialOverlay`'s last step ("Walk me through the first move").
- Debugging note worth keeping: `Element.scrollIntoView()` did not reliably bring an off-screen sidebar target into view (observed moving `scrollTop` a few px instead of the ~1000px needed, and a `requestAnimationFrame`-deferred retry never even ran — rAF callbacks appear not to fire in this project's headless browser-testing tool, though real foreground tabs shouldn't have that problem). Replaced with a direct `scrollTop` computation against `#game-sidebar` using both elements' `getBoundingClientRect()`, which worked reliably. Fixed a related overlap this surfaced: the callout card's on-screen clamping could place it directly over its own spotlighted target; now flips above the target when there isn't room below instead.
- Verified live in the browser: launched from both entry points, confirmed the already-satisfied "select ship" step auto-skips, watched each spotlight track its real target (buy-wool button, Ghent's map marker, the now-correctly-scrolled-into-view Advance-week button), confirmed auto-advance fires only after the real action (cash deducted, vessel dispatched, week incremented) and not before, and confirmed "Finish" and "Skip tour" both exit cleanly without corrupting campaign state. `tsc --noEmit` and `vite build` both clean.

### 2026-07-23 — Fixed the guided tour's broken trade loop; map clicks now preview before dispatching

- User report: following the guided tour, bought wool at Bruges, sailed to Ghent, and couldn't sell it there. Root cause confirmed against `content/cities/chapter1.json`: Ghent's market never trades wool at all (only cloth and madder), and separately, wool is cheapest at London (10f) and dearest at Bruges (20f) — so the script had the player buying high with nowhere to sell, the opposite of the "buy low, sell high" lesson it exists to teach.
- Fixed `components/GuidedTour.tsx`'s scripted trade to a real, verified-profitable one-hop loop: buy cloth at Bruges (24f), sail to Antwerp, sell there (30f base). Extended the tour with an actual "Sell it at a profit" step so it now demonstrates the complete loop instead of stopping at "advance the clock."
- Separately implemented the owner's request for price visibility: `MapView.tsx`'s city click is no longer gated on reachability and no longer dispatches by itself — it previews. New `components/CityPreviewPanel.tsx` (rendered in `GameScreen.tsx`'s sidebar) shows the clicked city's live prices (if the vessel is docked there), last-known report and age (if not), or "No report yet," plus — only when that vessel could actually reach it — an explicit "Send {vessel} here" button and the insurance checkbox (moved here from its old always-a-vessel-away spot). The previewed city gets a second gold ring on the map so the click target is unambiguous. Selecting a vessel now defaults the preview to its current location.
- Updated `GuidedTour.tsx` to match the new two-click dispatch flow: split the old single "send the ship" step into "Look at Antwerp" (preview) and "Confirm the voyage" (the new confirm button), threading a `previewCityId` prop through the tour's step-completion checks.
- Verified live in the browser: clicking Ghent (never visited) showed "No report yet for this city" and a working "Send The Charetty ship here" button without moving the ship; confirming actually dispatched it, and re-previewing Ghent afterward correctly read "Not reachable" while under way. Ran the corrected guided tour end-to-end — cloth bought at Bruges (40f→16f), Antwerp previewed then confirmed as destination, clock advanced, cloth sold at Antwerp for 29f (16f→45f, a genuine profit) — finishing cleanly. `tsc --noEmit` (client and api) and `vite build` all clean.

### 2026-07-23 — Phase 12, Chapter 0 prologue ("Claes begins") + guided tour expansion

- Planned in full before writing code: the design doc's own Chapter 1 line ("start as apprentice with 0 capital and 3 friendships") had never actually been built — every prior phase started with a ship, courier, and 40f already in hand. Confirmed three decisions with the owner up front: a true zero-capital start (not the existing resources with panels hidden), a "Skip the prologue" option defaulting new campaigns into Chapter 0 rather than making it mandatory, and re-gating the guided tour to an "advanced systems" tour that runs after Chapter 0 completes.
- Key finding that shrank the implementation: most systems need no new gating at all, since `MarketPanel`, insurance, voyage risk, and obligation resolution are all already dormant with no cargo vessel/cash/obligations. Only `LedgerPanel.tsx`/`HousesPanel.tsx` (new `flags` prop, `EstatePanel.tsx`'s own early-return pattern) and weekly wage upkeep (suspended in `sim/actions.ts` until `chapter0_complete`) needed real new gates.
- Added `EventEffects.grantVessel` (`sim/types.ts`/`sim/events.ts`, mirrors `joinCharacter`'s idempotency) so Chapter 0's finale can hand over the cargo ship mid-campaign. Gave `createInitialState` (`sim/state.ts`) a `{ skipPrologue }` option: default seeds only the courier, 0f, and characters exactly as content authors them (3 active/3 pending, "3 friendships"); `skipPrologue: true` reproduces today's exact prior behaviour (both vessels, 40f, `flags.chapter0_complete` set explicitly, only the Chapter 1 roster's 3 pending ids forced active — a scripted-driver check caught an early version of this wrongly also force-activating Diniz, Chapter 2's own joiner).
- Authored `content/events/chapter0.json` (8 events): a dyeworks introduction, a real courier errand to Ghent (teaches map/dispatch through actual travel time, not a scripted skip), a dye-job commission mirroring `ev_c1_002`'s cash-vs-conscience shape, Gregorio's and Tobie's narrative joins, a stale-information beat, and a finale granting the ship + 40f + Astorre + `chapter0_complete`. Added one trigger condition to Chapter 1's own opening event (`ev_c1_001`): `"flag": "chapter0_complete"` — the entire hand-off mechanism, and fully backward compatible for skip-prologue campaigns.
- Wired "Skip the prologue" end to end: `Lobby.tsx` checkbox → `createGame`'s new `skipPrologue` parameter (`useGameLocal.ts`/`useGameCloud.ts`/`useGameHybrid.ts`) → `api/game/index.ts`'s POST body → `createInitialState`.
- Grew `GuidedTour.tsx` from 8 to 11 steps (Household assignment, Dispatches courier investment, Ledger bill-writing) and re-gated it (and the static `TutorialOverlay`'s auto-show) from "week 0" to `flags.chapter0_complete`.
- Found and fixed two real bugs during verification: (1) the tour's `firstIncompleteStep` resume logic reset to the welcome card on any interruption-triggered remount, because the manual intro step's `isComplete()` always returns false — fixed by only showing the intro when *no* real step has completed yet, and by making two step conditions check persistent `location` instead of transient `destination`/`previewCityId`. (2) The original demo investment city, Ghent, sits at Bruges' 1-week latency floor and can never actually be invested in — switched to London.
- Verified with a scripted driver (default vs. skip-prologue starting state, the full 8-event chain including a real courier dispatch, correct week-after-next-tick handoff to `ev_c1_001`) and live in the browser (full Chapter 0 playthrough with Household growing as characters joined, Ledger/Houses confirmed hidden throughout, the finale handing off into three already-due Chapter 1 events in sequence; the expanded guided tour run twice, once deliberately interrupted mid-flow to confirm the resume fix). `tsc --noEmit` (client + api) and `vite build` all clean.

### 2026-07-24 — Phase 12 follow-up: Chapter 0 gets a real trade loop, top bar shows holdings

- User feedback on the just-shipped Chapter 0: no way to tell what's held from the top bar, and no actual gameplay during the prologue — 0f and a capacity-0 courier meant no trade was possible at all, and neither tutorial said anything until after Chapter 0 ended.
- New `EventEffects.grantCargo` (mirrors `grantVessel`) hands starter goods with no purchase behind them. `ev_c0_001` now also grants a small permanent vessel, Claes's handcart (capacity 3, land-only); `ev_c0_002` loads it with 3 madder for the Ghent errand. Since `MarketPanel` is purely capacity-gated, this makes buying/selling genuinely playable during the prologue, through the same UI as the full game.
- Two real bugs caught by a scripted driver, both fixed with one general rule in `checkTriggers` (`sim/events.ts`): Chapter 1+ events (only `ev_c1_001` was explicitly gated; every later one triggers on bare date+location) could misfire mid-Chapter-0 once the calendar crossed their dates during a now-longer prologue; symmetrically, Chapter 0's own `ev_c0_001` (bare `location: bruges`) fired even for skip-prologue campaigns. Fixed: Chapter 0 events require `!chapter0_complete`, Chapter 1+ events require `chapter0_complete` — one rule, not per-event patches.
- Fixed `HouseholdPanel.tsx`'s wage warning, which always showed "cannot pay — loyalty will fall" even though `advanceWeek` already suspends upkeep pre-`chapter0_complete`; added a `wagesSuspended` prop with a correct message instead.
- `TutorialOverlay.tsx` gained a second 4-step `chapter0` variant (own seen-flag) describing the true starting position, auto-shown immediately on a fresh campaign rather than never appearing until after the prologue.
- Header (`GameScreen.tsx`) now shows a `hold: ...` segment aggregating cargo across every vessel, not just cash/date/conscience.
- Verified with two scripted drivers (full trade loop incl. a real `SELL_GOOD` sale at Ghent; skip-prologue path unaffected) and live in the browser (Chapter 0 tutorial auto-shows correctly, handcart granted and loaded, header tracked `0f → hold: 3 Madder → 51f → hold: nothing` across the actual buy/sell round trip). `tsc --noEmit` and `vite build` clean.

### 2026-07-24 — Phase 12 follow-up #2: fixed dispatch-back bug, added sabotage feedback

- User report: couldn't move the dispatch rider back from Ghent, then couldn't move the handcart back from Ghent either — general "gameplay is confusing, can't see who/what is where."
- Reproduced exactly: sent the dispatch rider to Ghent, left the handcart docked at Bruges; previewing Bruges (to send the dispatch rider home) showed no dispatch button at all. Root cause: `CityPreviewPanel`'s `isLive` prop (`GameScreen.tsx`) was computed from whether *any* vessel was docked at the previewed city, not the *selected* one — a second vessel sitting at the destination hid the dispatch section for a completely different, elsewhere vessel. Fixed by computing a `selectedVesselHere` check from the selected vessel's own location and gating the dispatch/reachable section on that instead of the global `isLive` flag (which still correctly drives the "prices are true" price-display branch).
- While investigating, found a connected clarity gap: cargo silently dropped from a docked, idle vessel with zero explanation. Cause: `resolveHouseSabotage` (Phase 8) — St Pol's home city is Bruges, so a hostile-house sabotage roll can hit cargo sitting at the player's own home port, same as any hostile house's home city — working as designed, but with no UI feedback at all, unlike storm/piracy loss. Added `SabotageLossEvent`/`GameState.lastSabotageEvent` (optional field, doesn't invalidate old saves) and a matching sidebar report line.
- Verified live (reproduced the exact bug, confirmed the fix, confirmed the return dispatch completes) and with a scripted driver (forced sabotage via a mocked `Math.random`, confirmed the event's house/city/vessel/quantity are correct and cargo is actually reduced by the reported amount). `tsc --noEmit` and `vite build` clean.
- Not yet addressed: the broader "can't see who/what is where" complaint may want more (vessel markers on the map, a clearer household-location summary) — flagged, not undertaken without the owner's steer on direction.

### 2026-07-24 — Phase 12 follow-up #3: legible vessel markers on the map; combined Fleet & Household list

- Prototyped two visual directions before writing code (a cold/minimal one, rejected: "prefer the older style which feels more like the books... compounded and confused"; a second keeping the exact existing warm palette/map with vessel glyphs+labels and a plain combined list, approved).
- `MapView.tsx`: docked vessels now fan out around their city (radius `(port?7:5.5)+9`, slots at -120°/-90°/-60°, assigned deterministically by vessel id) instead of sitting exactly on the city's own dot — fixes both invisible overlap when multiple vessels share a city and (incidentally) a latent city-click-swallowing risk. Vessels get a real glyph (ship: pennant; courier: dot + a chevron rotated toward its destination while under way) and a name label shown only when selected or hovered — the deliberate answer to label crowding at the Bruges hub, and the only way to tell `courier_1`/`handcart_1` apart since their glyphs are identical.
- Caught a real bug during verification: the selected vessel's label, rendered on top of the map, was wide enough to cover a nearby city and silently swallow its clicks (reproduced: dispatching to Antwerp did nothing while the ship's Bruges label overlapped it). Fixed with `pointerEvents="none"` on the label text only.
- Moved `assignmentSummary` out of `HouseholdPanel.tsx` into `sim/characters.ts` as a shared, exported helper, fixing a hardcoded `'Idle, at Bruges'` literal to properly derive from the character's own `location` field in the process.
- `GameScreen.tsx`'s old "Vessels" list is now "Fleet & Household": same vessel buttons (same ids, `GuidedTour` unaffected), with aboard-officers nested under their vessel and everyone else listed below with a plain assignment line. `HouseholdPanel.tsx`'s own editing dropdowns are unchanged — this is an additional glance, not a replacement.
- Verified live: exact DOM transform inspection of the 1- and 2-vessel dock geometry, hover-label behavior, the city-click regression before/after the fix, exact chevron rotation math, and a Guided Tour run confirming all touched/adjacent target ids still resolve. `tsc --noEmit` (client + api) and `vite build` clean throughout.

### 2026-07-24 — Phase 12 follow-up #4: bigger pannable/zoomable map, medieval castle icons, sidebar moved left

- User feedback on the just-shipped map fixes: still "too cluttered and can't see overlap of icons" — asked for a larger map area, real pan/zoom navigation, larger city icons, a medieval visual style for them, and (mid-implementation) the sidebar moved to the left.
- Mocked up 3 candidate castle-icon designs and confirmed the winner (turreted castle) plus the pan/zoom mechanism (drag to pan, scroll to zoom toward cursor) via `AskUserQuestion` before writing any real code — learned that lesson the hard way in the previous follow-up.
- `GameScreen.tsx`: sidebar swapped to the left (280px, was 320px), map pane enlarged and padding trimmed, `MapView` gained `key={state.id}` so pan/zoom resets cleanly on a new campaign via remount.
- `MapView.tsx`: city markers are now a crenellated-castle path (single tower inland, 3-tower+pennant for ports) instead of a plain circle, same fill/fog logic, no new colors. Found and fixed two label-crowding cases by direct inspection: Ghent (boxed in by three neighbours) and Kouklia (whose label ran into Famagusta — the tightest city pair in the whole game, ~41 units apart) both flip their label to the other side. Recalibrated the vessel dock-slot fan-out radius for the bigger icons (`Math.hypot(halfWidth, halfHeight) + 9`, diagonal slots are the binding case).
- Added real pan/zoom: an inner `<g transform="translate() scale()">` wraps all in-world content (border-frame rects stay outside it deliberately, so they don't pan away or scale their stroke); drag-to-pan via `window`-level mouse listeners (SVG-scoped would get stuck if the drag exits into the sidebar); scroll-to-zoom via a real native (non-passive) `wheel` listener, since React's synthetic `onWheel` is passive by default and can't `preventDefault()`; screen-to-viewBox conversion accounts for the SVG's own letterboxing. Zoom clamped to `[0.7, 2.5]`. A "Reset view" button restores the exact default.
- Verified live: real drag (inspected the resulting transform), scroll-to-zoom keeping the exact cursor-point fixed (checked numerically to a fraction of a pixel), city click hit-testing still exactly correct mid-zoom (dispatched a real click, completed a real dispatch to Antwerp under a non-default transform), Reset view restoring the exact default, and every `GuidedTour`-dependent id still present and correctly counted. `tsc --noEmit` and `vite build` clean, no console errors.

### 2026-07-24 — Phase 12 follow-up #5: real Ptolemaic-projection map backdrop, cities spaced by actual geography

- User handed over a separate, already-built map project (`map/niccolo-map/`) — a data-driven Donis-trapezoidal ("Ptolemaic") world map (Nicolaus Germanus, c.1460) with real coastlines, rivers, mountains, and a 30-place gazetteer — asking for it as the map's backdrop, with cities placed on it "properly," since the hand-drawn landmasses (previous follow-up's own note: "no new colors") still crowded every city too close together.
- Confirmed three decisions via `AskUserQuestion` before writing code: keep the game's existing dark palette (recolor the source's native light-paper geometry, not the reverse, and don't restyle anything else), keep full pen-and-ink detail (hachured land, doubled "ghost" sketch strokes, rivers, mountains, lat/long graticule — none of it simplified away), and use the full Ptolemaic domain (lon 0–95, lat 4–66) rather than a tighter crop. The third mattered more than expected: Chapter 4 (Lisbon, Madeira, the Gambia, Timbuktu) turned out to already be live in `content/cities/chapter4.json`, wired into `CITIES` with placeholder coordinates that only make sense in the old hand-drawn 780×560 space — the full domain is what lets those already-shipped cities plot correctly too, not just future ones.
- Ran a background investigation (three parallel deep-dives into the source project's own code/data) before implementing, and resolved one real conflict it surfaced directly rather than guessing: the rendering side had tuned the palette using "uniform" margins while the coordinate side had computed final city positions under the source's own asymmetric default margins. Verified with a standalone script against the source's real `projection.js` that margin choice only changes total canvas height (a legend band this game doesn't draw), never any city's projected (x, y) — so the two could be combined safely with zero recomputation.
- New `content/geography.json` (a straight copy of the source's landmass/seas/islands/rivers/mountains/label data — deliberately one flat file, not chaptered like `cities/chapterN.json`, since the physical world doesn't grow per chapter), new `sim/projection.ts` (a typed port of the source's `createProjector`/`ringToPath`/`smoothPath`/relief-hump math, geometry only, no color or DOM), and new `sim/geography.ts` (calls `createProjector` once at module load — ES modules are singletons — and exports a `BACKDROP` constant of pre-built path strings for every layer, at canvas width 10000 with uniform margins, `convergence: 0.55`, `latStretch: 1.15`; resulting canvas 10000×7226.4).
- Migrated all 20 cities' `x`/`y` across `content/cities/chapter1-4.json` to their real projected positions at those exact settings — nothing else about any city changed, and `routes/*.json` distances/seasonality were explicitly left alone as gameplay-balance numbers independent of real geography. Every closest-pair distance either improved substantially (Florence–Naples: 29.7px → 590px, since the old hand-drawn map had crushed all of Italy into a blob) or, for the one pair that got tighter (Ghent–Antwerp, genuinely close together in reality), still cleared the vessel dock-slot overlap floor with 1.76× to spare.
- Rewrote `MapView.tsx`: removed the hand-drawn `LANDMASSES`/`ALPS_RIDGE`/`RhumbLines`, replaced with the real backdrop's JSX (hachure-pattern land, doubled ghost coastline strokes, seas, islands, rivers, lakes, mountains, graticule, region/sea name labels, all recolored into five new muted-gold/slate palette constants derived from the existing GOLD/INK/SEA_COLOR ramp — no new hues introduced), clipped to the domain's true trapezoidal neatline. Repositioned the compass rose and border-frame chrome via a new `CHROME` scale factor instead of literals. Replaced the hardcoded `{panX:0, panY:0, zoom:1}` default view with one computed from a live bounding box over `CITIES` (correctly covering all 20, including the already-live Chapter 4 ones) — comes out to zoom ≈1.53, comfortably inside the existing `[0.7, 2.5]` clamp with real headroom to spare. Cleared `FLIPPED_LABEL_CITY_IDS` (previously `{ghent, kouklia}`): both flips were solving crowding the new real spacing already fixes, confirmed by direct label-bbox math and then live inspection at every cluster (Flanders, Milan/Genoa/Florence/Venice, Cyprus) — no new flip needed either. `CASTLE_HALF_SIZE`/`DOCK_RADIUS`/icon pixel sizes deliberately left unscaled — icon size is an independent design choice, and the real spacing already solves the crowding complaint on its own.
- Verified live in the browser: recolored backdrop renders fully in the dark palette at both default and max zoom with no light-theme flash, hachure/rivers/mountains/graticule all clearly subordinate to castle icons; default view frames the real 20-city cluster (not the whole Thule-to-Timbuktu domain) and "Reset view" returns to that exact computed transform; drag-to-pan and scroll-to-zoom-toward-cursor both still work correctly at the much larger canvas; city click, preview, and dispatch still work at a non-default pan/zoom (dispatched the ship to Antwerp and back to Bruges, both completed correctly); region/sea labels ("Germania," "Italia," "Hispania") read as unobtrusive atmosphere rather than clutter; multi-vessel dock-slot fan-out re-confirmed at Bruges (ship + dispatch rider both docked, both clearly clear of the castle icon and each other) — the dock-slot math was untouched by this migration and needed no changes. Zero console errors throughout. Test campaign deleted afterward.
- `tsc --noEmit` (client + api) and `vite build` clean throughout Parts A/B/C.
- **This verification pass had a real gap, corrected the same day (see the next entry): every check above was performed either at the exact computed `DEFAULT_VIEW` transform without independently checking the actual on-screen pixel size of anything, or at a manually-crafted close-up `viewBox` override — never at a normal, moderate zoom level a real player would actually use. That gap is exactly why the "icon pixel sizes deliberately left unscaled" claim above went unchallenged despite being wrong.**

### 2026-07-25 — Phase 12 follow-up #6: fixed invisible map icons and a too-narrow zoom range

- User report right after the previous entry shipped: "map doesnt work, doesnt zoom in close enough to actually see the cities... at least another 5x," plus "looks a fair way off the plan" more generally.
- Root cause of "doesn't work": the previous entry's claim that `CASTLE_HALF_SIZE`/`DOCK_RADIUS`/city labels/vessel glyphs/route strokes could stay "unscaled" was wrong. They're all authored in absolute units against the *old* 780px-wide canvas; the backdrop's own geometry scales correctly because `sim/geography.ts` computes it via the projector's own `P.scale()`, but nothing carried that scaling to this file's hand-authored UI elements. On the new 10000-unit canvas that's a ~12.8x mismatch — every icon, label, and vessel marker rendered at roughly 1/12.8th its intended size, i.e. sub-pixel at any zoom up to the old `ZOOM_MAX` of 2.5. The previous entry's own live checks never caught this because they either read the exact `DEFAULT_VIEW` transform without measuring actual rendered pixel size, or used a manual `viewBox` override zoomed in far enough (a 260-unit crop rendered into ~800px) that the same absolute-unit icons looked enormous instead of invisible — the same underlying bug, symmetric failure mode, missed both times because pixel size was never actually checked.
- Introduced two separate scale factors instead of one blanket fix, after discovering a second, opposite problem the first attempt (scaling everything by the full ~12.8x canvas-growth factor) caused: `UI_SCALE` (`VB_WIDTH/780`, ~12.8x) for the compass rose and border frame only — true canvas chrome that lives in the open margins and never competes for space with a city. `ICON_SCALE = 1.5` for everything that must coexist inside a real, sometimes-tight city cluster — castle icons, `DOCK_RADIUS`, city labels, vessel glyphs/labels, route lines, region/sea labels. `1.5` isn't a guess: measured the real closest city pair in the game directly from the migrated coordinates (Bruges–Ghent, 73.1 world units — closer than Milan–Genoa at 84.5 or Famagusta–Kouklia at 145.5), then picked the largest `ICON_SCALE` whose combined port+inland icon radius still leaves comfortable clearance against that gap (at 1.5, combined radius is 46.9 units, 36% of the gap left clear). A full ~12.8x scale-up (matching `UI_SCALE`) would have made a single icon's own footprint *bigger than the entire gap between Bruges and Ghent* — since icon footprint and inter-city distance are both world-unit quantities that scale together under zoom, this overlap is zoom-invariant: no amount of zooming in fixes an icon that's fundamentally too big for its neighbourhood, only a smaller `ICON_SCALE` does.
- Raised `ZOOM_MAX` from 2.5 to 12 (`ZOOM_MIN` unchanged at 0.7) — computed, not guessed: at `ICON_SCALE` 1.5 a port icon's half-diagonal is ~27.7 world units, and reaching a legible ~25 screen px for it requires zooming to roughly 25 / (27.7 × 0.0834 px-per-unit-at-zoom-1) ≈ 10.8; 12 gives a bit of headroom beyond that. Coincidentally (or not) almost exactly matches the user's own "at least another 5x" ask (2.5 → 12 is 4.8x). `DEFAULT_VIEW`'s own zoom-fit computation was untouched and still comes out to ~1.53, comfortably inside the new range.
- Verified rigorously, having been burned by screenshot-based verification missing this exact bug twice already: wrote a direct DOM bounding-box collision check (`getBoundingClientRect()` on every city's icon `<path>` and label `<text>`, checked pairwise for real intersection) rather than trusting a screenshot — confirmed zero icon-icon overlaps across all 20 cities at the live-computed `DEFAULT_VIEW` transform (previously, before this fix, checking the same way found Bruges/Ghent/Antwerp/Calais/London *and* Milan/Genoa/Florence/Venice *and* Famagusta/Kouklia all directly overlapping at default zoom). Found one minor real case the same way — Ghent's label corner grazes Antwerp's icon by ~4×10px at max zoom — and confirmed *not* worth a `FLIPPED_LABEL_CITY_IDS` entry: computed that flipping it the other way would actually be worse (Bruges, at 68 units, is closer to Ghent than Antwerp is at 83.8), matching this file's own established "only flip if it actually reads as crowded on screen" standard. Re-verified the vessel dock-slot fan-out at Bruges (ship + dispatch rider) still clears the recalibrated icon/dock geometry with zero overlap. Screenshots at a genuinely zoomed-in level (not a manual `viewBox` hack) now show crisp, legible, well-separated castle icons and city names for the first time since the real-geography migration shipped.
- **Deliberately not changed, flagged for the owner instead of guessed at:** `DEFAULT_VIEW` still frames the full 20-city span (Madeira/Gambia/Timbuktu in the southwest to Trebizond in the east) by design, per the original migration's own reasoning. Because avoiding icon overlap caps `ICON_SCALE` at ~1.5, and fitting the *entire* real-world span by definition means a small zoom, city icons at the literal default view are still only ~4-5 screen px — technically non-overlapping and fully clickable (the sidebar preview panel shows full info for any city regardless of its on-screen pixel size), but not something you'd visually pick out at a glance without zooming in first. Tried framing just the Chapter 1-3 "core" 16 cities instead (excludes the geographically remote Chapter 4 outliers) and it only buys zoom 2.34 instead of 1.53 — icons at ~7px, not a meaningfully different experience. Concluded this is an inherent trade-off of showing real, continent-spanning Ptolemaic geography rather than a compressed schematic, not a bug fixable by further constant-tweaking, and left `DEFAULT_VIEW`'s bounding-box logic untouched rather than silently redesigning it a third time this feature — surfaced explicitly instead.
- `tsc --noEmit` (client + api) and `vite build` clean.

### 2026-07-24 — Phase 13, Chapter 4 (Scales of Gold) + Ledger/Counting House split + header reorg

- Section 14 ("SCOPE CONTROL") had held Chapter 4+ content since Phase 11's own note; the owner explicitly lifted that hold this session and asked for Chapter 4 by name, alongside two direct UI requests (the core-loop buttons moved to the header; the banking function split back into its own screen) — all three landed together, one phase, one entry, the same "Phase N + follow-up" shape several earlier sessions used.
- **New map content** (`content/{cities,routes,goods,characters}/chapter4.json`): Lisbon and Madeira (port, Atlantic-Portuguese sphere), Gambia (port, sells ivory/malaguetta — both named in the design doc's own §4 goods list since Phase 1 but never implemented until now), Timbuktu (inland, power `songhai` — Sunni Ali's historical 1468 capture of the city is the backdrop for the Songhai-threat beat, not a scripted date check). New currency `cruzado` (`CurrencyId`'s eighth member, `sim/currency.ts`), pegged at 1.10 — deliberately comparable to `ducat`, not low like `asper`/`bezant`, a rising Atlantic trade coin rather than a declining kingdom's. `driftExchangeRates`'s `?? par` fallback and `withAllCurrencies`'s spread-merge absorb it for any pre-existing save with zero extra guard code, the same mechanism Chapters 2/3 already relied on for `asper`/`bezant` — verified directly this phase (see Verification below), not just asserted by precedent.
- **A new character, Gelis van Borselen** (`content/characters/chapter4.json`, `status: "pending"`), joins mid-arc via the same `EventEffects.joinCharacter` Diniz used in Chapter 2. No new House: Madeira's "St Pol collision" reuses the existing hostile `stpol` house (see below) rather than inventing a rival company the way Trebizond's Doria was — nothing in this chapter's own scope calls for a second AI company.
- **`Route.type` gains `'river'`** (`sim/types.ts`) for the Gambia→Timbuktu leg (`gambia-timbuktu`, `content/routes/chapter4.json`, `seasonal: true` — the real Gambia's navigability genuinely is tidal). Checked all three courier-restriction call sites before touching any of them: `actions.ts`'s `dispatchVessel` and `content.ts`'s `reachableFrom` both already exclude "not land" generically (`route.type !== 'land'`), so couriers are barred from the river with zero code changes at either site — confirmed by a scripted assertion, not just read-through. Only `MapView.tsx`'s dash-style branch needed a real third case (`'2 2'`, distinct from sea's `'5 4'`); `insurance.ts`'s own sea/land premium and risk pairs deliberately fall through to the land rate for a river route, left that way on purpose since `sim/expedition.ts`'s disease clock (below) is the dedicated new risk model for this leg — stacking storm/piracy risk on top would double-jeopardy the same voyage.
- **The disease clock** (new `sim/expedition.ts`), mirroring `insurance.ts`/`resolveHouseSabotage`'s self-contained, hardcoded-constant, one-dramatic-event-per-week shape: tracks at most one vessel at a time (`GameState.expedition?: ExpeditionState | null`) while it's docked at Gambia/Timbuktu or under way on the river route specifically (arriving at and idling in either city already counts — the clock isn't gated to the river crossing alone), and clears itself the week the tracked vessel leaves that zone. Weekly chance starts at 12%, climbs 1.5%/week upriver, caps at 45%; halved if Tobie (the household's own physician, `content/characters/chapter1.json`) is aboard the tracked vessel — a thematic reuse of an existing character rather than a new stat. A hit escalates `healthy → ailing → stricken` (never regresses, a repeat hit at `stricken` just reapplies that tier's cost) and costs cash/conscience (15f/2 conscience ailing, 35f/5 conscience stricken); the first transition into `stricken` sets `expedition_crisis`, which `content/events/chapter4.json`'s `ev_c4_031` reacts to narratively. Both `expedition` and `lastExpeditionEvent` are optional/nullable fields (not required-but-nullable the way `Estate` is) specifically so `useGameLocal.ts`'s `isCurrentShape` needs zero changes — a save from before this field existed just has no expedition running, not a shape mismatch. Wired into `actions.ts`'s `advanceWeek` right after sabotage/estate resolve; this is the first time `advanceWeek` itself ever touches `conscience` (previously only event choices and `applyConscienceCost` did), worth flagging as a real precedent shift even though the effect is small.
- **St Pol's Madeira collision** (`ev_c4_009`) reuses the existing hostile house rather than adding a second one: a new flag `stpol_madeira_collision` adds a *second*, independently-stacking penalty tier in `sim/houses.ts`'s `baselineFor`, alongside the existing `stpol_first_blood` tier from Chapter 1 — two separate grudges the house has given St Pol, not one bigger version of the first. Verified the stacking arithmetic directly: converged relation sits at ~30 with neither flag, ~10 with only first blood, ~20 with only the Madeira collision, and ~0 (clamped) with both.
- **Event pack** (`content/events/chapter4.json`, 31 events — 30 main-line plus `ev_c4_031`, the optional crisis reaction) across 5 sub-arcs, each ending in its own thread flag: Venice debt pressure (2 events, `venice_debt_resolved`) → Lisbon (3, `lisbon_agency_opened`, plus secret #1: Portuguese Guinea-financing intelligence) → Madeira/St Pol (4, `madeira_collision_resolved`) → Gambia/Timbuktu — the chapter's logistics/extraction set piece, including Umar's choice (narrative-only per the design doc's own §4 instruction that the slave trade is referenced, never a market mechanic or a new roster character — the same treatment Felix's and Katelina's scripted deaths got) and secret #2 (Timbuktu's "the learning," a scholar's geographic manuscript) — funnels both its success and failure branches through a shared `gambia_epilogue` flag, mirroring Chapter 2/3's own extraction-funnel pattern exactly, including the double-flag gate (`..._resolved` set by both branches, `..._success`/`..._failed` distinguishing them) → Gelis (4, `married_gelis`, a real cash-stakes wager rather than an invented RNG bet — Marian's own Chapter 1 marriage arc set the "two flavor choices converge on one flag" precedent this follows). Finale (`ev_c4_030`) is a 5-flag AND-gate mirroring Chapter 1's own 5-thread finale, deliberately flags-only with no `dateAfter` floor (Chapter 3's cleaner precedent) since every thread is already sequence-gated upstream. The Gambia failure deadline is `1471-01-01`, not the chapter's nominal 1468 — the same accuracy-for-playability trade Phase 9/10 made explicit for Trebizond/Famagusta, since the continuous clock means Chapter 3 alone can already run past 1466.
- **Chapter-complete freeze migrates to `chapter4_complete`** across the same 4 files every prior chapter moved it through: `sim/actions.ts`'s `processAction`, `hooks/useGameLocal.ts`'s `statusOf`, `api/game/[id].ts`'s inline status derivation, and `pages/GameScreen.tsx`'s full-screen ending block (new Chapter-4 epilogue text/stats — Gambia success/failure, Umar's choice, secrets, departures — replacing the `chapter3_complete` block the same way each prior chapter's ending replaced its predecessor's).
- **Header reorg**: all three of Advance one week, ← Back to campaigns, and Abandon this campaign moved from the bottom of the sidebar into the header's button cluster (`GameScreen.tsx`), alongside the existing Guided tour/How to play pair — the sidebar was about to get longer (the new expedition status line, below) and these are the game's most-reached-for controls, now available without scrolling. The cluster wraps onto a second line at this viewport width rather than overflowing or overlapping; `GuidedTour.tsx`'s spotlighting needed no change for the move itself since its scroll-into-view logic only engages for targets outside the viewport, which a header element never is.
- **Ledger split into Ledger + Counting House** (design doc §11, screens 2 and 3 — Phase 4 had folded them together for expedience): `LedgerPanel.tsx` trimmed to balance sheet/exchange rates/maturity ladder (keeping "Discount now" inline, on purpose — it acts on a specific visible ladder row, and moving it to the other tab would force switching mid-action to re-find the same row, the same consolidation-for-usability call Phase 4/11 each made in their own day, just one level more granular here); new `CountingHousePanel.tsx` takes the bill/deposit/loan-writing forms. A lightweight 2-way tab (`GameScreen.tsx`'s own `ledgerTab` state, not new app-wide navigation) switches between them, both panels kept mounted (toggled via `display`) so in-progress form input survives a tab switch. Real dependency found and fixed in the same change: `GuidedTour.tsx` had a step targeting `id="ledger-borrow-button"` with body copy about "the Ledger" — renamed to `counting-house-borrow-button` (button + tour's `targetId` + body text, together), and the tour now force-pins `ledgerTab` to `'countingHouse'` for its own duration (`useEffect` on `showGuidedTour`) so that step's target is never hidden behind the other tab. No "protest" split out — grepped the whole sim, nothing implements protesting a bill yet, so there was nothing to move.
- **Real bug found during this phase's own verification, unrelated to any of the above**: toggling a button between the plain `BUTTON` style and the `BUTTON_ACTIVE`/`PRIMARY_BUTTON` variant (vessel selection, the new Ledger/Counting-House tabs, `CityPreviewPanel`'s dispatch-confirm button, `GuidedTour`'s and `TutorialOverlay`'s own primary buttons) triggered a real React warning — mixing the `border` shorthand (on the base style) with a `borderColor` longhand override (on the active variant) across re-renders of the same element ("Removing borderColor border"). Not new to this phase (the vessel-selector pair already had it; the new tab buttons just added a second instance), but caught live while verifying the tab split, so fixed at the root in all four places it existed (`GameScreen.tsx`, `GuidedTour.tsx`, `TutorialOverlay.tsx`, `CityPreviewPanel.tsx`): every "active/primary" variant now overrides the full `border` shorthand instead of just `borderColor`. Confirmed fixed in a genuinely fresh browser tab (the warning persisted in an already-open tab across the fix — stale Fast-Refresh state, not a real regression — and cleared entirely in a new one).
- **Expedition status** in the sidebar: no new panel (the disease clock has no player action), just a third status line beside the existing `lastVoyageEvent`/`lastSabotageEvent` reports, plus a live "N weeks upriver, crew health: X" line while `state.expedition` is active.
- **Not this phase's work, landed alongside it**: a separately-developed real-geography map migration (see the preceding entry, "Phase 12 follow-up #5") replaced `MapView.tsx`'s hand-drawn landmasses with a projected Ptolemaic backdrop and re-plotted every city's `x`/`y`, including this phase's own four new ones (picked up correctly once they existed in `content/cities/chapter4.json` — verified they render at sane, non-overlapping on-screen positions post-migration). This phase's own river-route dash style (`'2 2'`) survived that rewrite untouched and was reconfirmed working against the merged file, not reapplied.
- **Verification.** Scripted driver (`processAction`/`resolveEvent`/`checkTriggers` called directly, seeded past `chapter3_complete` with every Chapter 0-3 event pre-marked fired — the same isolation precedent every prior phase used): full success path through all 5 threads including a real river round trip (Bruges→Lisbon→Madeira→Gambia→Timbuktu and back, the disease clock ticking organically the whole way, gold actually bought and carried home) reaching `chapter4_complete` with `gambia_expedition_success` set; full failure path (deadline forced past `1471-01-01` with no gold delivered) confirming the funnel still reaches `gambia_epilogue` and `chapter4_complete` via `gambia_expedition_failed` instead; `resolveWeeklyExpedition` exercised directly with `Math.random` forced (escalation curve, Tobie's halving, single-fire `crisisReached`, clearing on leaving the zone); St Pol's stacked-penalty convergence math; a courier confirmed rejected on the river route. Live in the browser: fresh campaign through the header/tab/map changes with zero console errors; `cruzado` confirmed rendering correctly in the Ledger; and — in place of the one pre-existing real campaign from earlier sessions, which this session's browser context no longer had access to — directly stripped `cruzado` from a real save's `exchangeRates` in `localStorage` to reconstruct the exact pre-Chapter-4 shape, reloaded, and confirmed the Ledger renders the backfilled peg value with no crash: the same failure mode the real Phase 9 `asper` bug hit, deliberately reproduced and confirmed fixed rather than assumed. `tsc --noEmit` (client + api) and `vite build` clean throughout.
- **No Chapter 5 content.** Section 14's discipline continues past Chapter 4 the same way it governed Chapter 3→4: reaching Chapter 4 is not standing authorization for Chapter 5. **Do not start Chapter 5 without being asked.**

### 2026-07-25 — Phase 12 follow-up #7: zoomed-out map no longer shows void beyond its own edges

- User report right after the previous entry's zoom-range fix shipped: zooming all the way out exposed bare black/void corners beyond the map's own drawn edges, not a real part of the map.
- Root cause: the outer `<svg viewBox>` stays a fixed size (by design, so pan/zoom math never has to change), and the in-world content is scaled/panned inside it via one inner `<g transform="translate() scale()">`. `ZOOM_MIN` was 0.7 — any zoom below 1 shrinks that content smaller than the viewBox itself, so no amount of panning can make it cover the whole visible area; the gap shows the `<svg>`'s own `VOID_COLOR` background. Separately, even at zoom ≥ 1, panning itself was never clamped, so dragging far enough in any direction could push the content's edge into view with the same result.
- Fixed both halves: raised `ZOOM_MIN` from 0.7 to exactly 1 (the smallest zoom at which the content can possibly cover the fixed viewBox), and added a `clampPan(panX, panY, zoom)` helper — `panX` restricted to `[VB_WIDTH*(1-zoom), 0]`, `panY` to `[VB_HEIGHT*(1-zoom), 0]` — applied everywhere `view` is set: `DEFAULT_VIEW`'s own computed pan, the drag handler, and the scroll-to-zoom handler. At zoom 1 this pins pan to exactly `(0,0)` (no slack — nowhere to pan since content exactly fills the viewBox); at higher zoom it allows exactly enough pan to explore the zoomed-in area without ever exposing an edge.
- **Real bug caught during this fix's own verification, unrelated to the fix itself**: firing synthetic wheel/zoom events at the map while it briefly had zero layout size (observed in practice right as a scripted event modal opens over the map on a fresh campaign) made `getContentScale`'s scale compute as 0, and every dependent calculation divided by it, producing `NaN` that then permanently corrupted `view` — `Math.max`/`Math.min` never recover from a `NaN` input once it's in state, so the map stayed frozen at `translate(NaN,NaN)` until a reload. Added a guard in both the wheel and drag handlers: skip the event entirely if the SVG's current content scale isn't a positive finite number, rather than letting a momentarily-invisible element corrupt persistent state.
- Verified live: zoomed all the way out (confirmed `scale(1)`) and dragged hard in every direction — map fills the pane edge to edge throughout, no void ever visible; repeated at a mid-zoom level (~2.6x) and confirmed pan clamps to the exact computed valid range rather than drifting past it.
- `tsc --noEmit` reports pre-existing, unrelated errors in `sim/objectives.ts` (a separate, still-in-progress feature, not part of this fix — confirmed via `git status` that file is untracked and outside this change's scope). `vite build` (which only strips types, not full-checks them) is clean, matching every prior verification this session.
### 2026-07-25 — Phase 12 follow-up #8: "Engraver's Chart" map treatment — tried, reverted same day

- Owner feedback on the map's overall look, not just bugs: "i dont love the map... looks childish." Generated three real-geometry mockups as a throwaway Artifact (established practice for this exact call) and owner picked "C — Engraver's Chart": near-black sea, fine engraving instead of crosshatch, bone-pale ink coastline, no doubled "ghost" stroke — plus two technical asks, improve how the map is drawn and fix its edges.
- Implemented in full: recolored `SEA_COLOR`/`GEO_COAST`/`GEO_HATCH`/`GEO_RELIEF`/`GEO_WATER` to the near-black/bone-pale palette, removed the ghost double-stroke, added `smoothRingPath` (cyclic Catmull-Rom for closed coastline rings, vs. the existing open-path `smoothPath` used for rivers) so coastlines curve naturally instead of reading as straight-line low-poly outlines, widened/thinned the hachure for a sparser engraved-contour feel, and replaced the border frame's two mismatched axis-aligned `<rect>`s with two trapezoid `<path>`s (`insetQuad`/`quadToPath`) that actually follow the sheet's real trapezoidal shape (Donis-projection meridians converge toward the pole).
- **Shipped, then reverted the same day**: seeing the real, full implementation live — not the small isolated mockup crop — the owner's reaction was "it looks much worse now." On direct re-inspection this was the right call: the near-black/high-contrast direction reads as cold and clinical, and clashes with the rest of the game's warm gold/parchment UI chrome (sidebar text, city labels, route lines) rather than sitting inside it. **This is exactly the trap an earlier session's own memory had already flagged** — "this project's visual identity is the warm ink/gold/parchment palette... do not propose a colder/more minimal direction without being asked" — offering it as one of three mockup options, and having it picked, wasn't the same as it actually being asked for once seen at full scale. Reverted via `git revert` back to the previous ("Illuminated Vellum") palette/coastline/frame code intact and unchanged.
- **Lesson for any future map style pass**: a small cropped terrain-only mockup can look fine in isolation but still clash once it's the full map sitting inside this game's actual warm-toned chrome — future style mockups for this project should render inside a rough approximation of the real sidebar/header context, not just an isolated terrain swatch, before committing to a direction.

### 2026-07-25 — Phase 14: chapter objectives, organizational turn phases, single-house hotseat experiment

- Owner asked for three things at once: win/advance conditions per chapter ("a certain number of money made in a period"), scoping the other Houses for a multiplayer version where friends occupy rival Houses, and multi-step turns that require completing a number of activities per week ("like deploy the army"). A follow-up message reframed the first ask before any code was written: objectives should tie into the *story*, not just be a cash metric, and must be toggleable off per campaign at creation — so a richer, narrative-flag-driven objectives layer, not a pure `cash >= N` gate.
- Ran a background design workflow (three parallel design+critique fronts covering all three asks) before writing anything, since each one had a real fork the owner's own wording didn't resolve on its own — surfaced all four via `AskUserQuestion` rather than guessing, and the owner picked the recommended option on every one: **turn structure is organizational only** (tabs the player can freely switch, never a hard gate — a real "must deploy the army to advance" requirement doesn't fit a game with no army-deployment mechanic to begin with); **"deploy the army" was just an illustrative example**, not a request for a real tactical/military system — none was built; **multiplayer scoped down to a single small hotseat experiment** (one house, same device, no networking, never touching the real story chapters) rather than a networked multi-house game; and **objectives get authored retroactively for Chapters 1-4 now**, not just Chapter 5 onward, so the feature reads as complete rather than starting mid-story.
- **Objectives system** (`sim/types.ts`'s `Objective`/`ObjectiveKind`, new `sim/objectives.ts`, new `content/objectives/chapter{1,2,3,4}.json` — 16 objectives total: 5/2/3/6 per chapter). An `Objective` is content describing an *existing* thread's gating flag, not a new mechanic — `objectivesForChapter(state, chapterNumber)` is a pure, read-only projection over flags the chapter's own event content already sets (`kind: {type:'flag'}`/`{type:'flagAbsent'}`/`{type:'cashThreshold', amount, byWeek}`), nothing in `sim/events.ts`'s trigger/effect machinery changed. `currentChapterNumber(state)` derives "which chapter's list is live" from the same `chapterN_complete` flag chain the finale events themselves already use, so Chapter 0 (no objectives content, a single bare finale flag with no thread to describe) naturally returns an empty list. Three objectives are marked `inevitable` (scripted beats that will occur regardless of player choice, e.g. Felix's fate, First blood with St Pol) so the panel reads "will occur" rather than implying they're avoidable; one (Umar's choice, Chapter 4) is marked `optional` and excluded from the "N of M resolved" denominator since the design doc treats it as narrative-only, not a thread the player is expected to close.
- New `components/ObjectivesPanel.tsx`: read-only, self-hides with an empty list (Chapter 0, or the campaign's own "Hide chapter objectives" toggle — `GameScreen.tsx` simply doesn't render it at all in that case rather than passing an empty array), status-colored rows (open/will-occur/resolved/missed/costly), footer "N of M threads resolved" count. New `GameState.objectivesHidden?: boolean`, set at creation via a new Lobby.tsx checkbox ("Hide chapter objectives — track your own progress without a checklist naming which story threads still need resolving") threaded through `createGame`'s full chain (`useGameLocal.ts`/`useGameCloud.ts`/`api/game/index.ts`/`sim/state.ts`'s `CreateInitialStateOptions`) the same way `skipPrologue` already was.
- **Multi-step turn phases** (new `TurnPhase` type, new `components/PhaseStepper.tsx`, `GameScreen.tsx`): three organizational tabs — Trade & Dispatch (`EstatePanel`), Household & Intelligence (`DispatchesPanel`+`HouseholdPanel`+`SecretsPanel`+`HousesPanel`), Counting House (the existing Ledger/Counting-House split) — toggled via `display: activePhase === X ? 'block' : 'none'` so every panel stays mounted and no in-progress form input is lost switching tabs. `MarketPanel` (buy/sell) deliberately sits outside all three groups and stays always visible, since seeing prices is needed to decide *any* trade regardless of which tab is open. A `PhaseStepper` shows a "✓" per tab once visited that week, purely cosmetic — no tab is ever required to advance, and "Advance one week" works identically regardless of which tab is active or visited, per the owner's own confirmed choice. The whole feature is opt-in and off by default: a `phaseTabsEnabled` boolean is a local UI preference persisted in `localStorage` (not campaign state, mirrors `hasSeenTutorial()`'s own precedent), toggled via a new header button ("Multi-step turns: on/off"); when off, every phase group's `display` falls back to always-`block`, i.e. the old single-column layout, unchanged. `visitedPhases` resets to just `{trade}` at the start of each new week, and the Counting House group shows an explicit placeholder ("Not available yet — credit isn't Claes's to extend until he's formally made the house's factor") during Chapter 0 rather than a silent empty tab — a critique-flagged gap fixed before it shipped.
- **`GuidedTour.tsx`'s `requiresPhase` extension**: the tour's existing "Assign an officer"/"Invest in a courier line" steps now carry `requiresPhase: 'household'`, and "Write a bill of exchange" carries `requiresPhase: 'finance'`. A new `onStepChange` callback fires on every step change (including on mount, so resuming a tour directly onto a phase-gated step still works) and `GameScreen.tsx`'s handler force-selects the right tab — and, for the finance step specifically, also force-pins the Ledger/Counting-House sub-tab to `'countingHouse'`, the same forcing the Chapter 4 phase already did for that sub-tab alone. Confirmed live this session, not just read through: stepping through the full 11-step tour end-to-end, the Household tab auto-opened exactly when the officer-assignment step became current (with the Trade panel correctly hidden underneath it), and the Finance tab plus its Counting-House sub-tab both auto-opened together exactly when the bill-writing step became current.
- **Single-house hotseat experiment** (`sim/types.ts`'s `HotseatDecision`, `sim/houses.ts`'s `ManualTradeChoice`/`ManualPlantChoice`/`ManualSabotageChoice`, new `components/HotseatDecisionModal.tsx`): a Lobby-time `<select>` ("Let a friend run a rival house this campaign") names one House (Medici/St Pol/Genoese/Doria) whose weekly dice-driven behavior — market nudge direction, false-news planting, sabotage attempts — a second player at the same device decides instead of `Math.random()`, every other House untouched. `GameState.hotseatHouseId?: string | null` set at creation through the same options chain as `objectivesHidden`. When set, clicking "Advance one week" opens `HotseatDecisionModal` instead of dispatching immediately: trade-good/direction selects (scoped to the house's own home-city market), plant-news controls (hostile houses only, target city picker), and a sabotage checkbox correctly disabled with "no eligible cargo" when no docked player vessel qualifies that week — confirmed via the same eligibility check `resolveHouseSabotage` itself uses, not a separate guess. Confirming the modal calls `advanceWeek(state, hotseatDecision)`, which derives `manualTrade`/`manualPlant`/`manualSabotage` from `state.hotseatHouseId` and passes them into `houses.ts`'s three functions — each accepts an optional `manual` param that overrides the dice roll only when `manual.houseId` matches the house actually eligible that week, falling back to the normal random behavior otherwise (verified this fallback directly: a hotseat house not eligible for sabotage that week still resolves via dice as if no hotseat were set). No new House record, no networking, no AI beyond what already existed — exactly the "Option B" scope the owner confirmed, not a networked multi-house game.
- **Verification.** Scripted driver (`processAction`/`objectivesForChapter`/`advanceWeek` called directly): 26/26 assertions covering `currentChapterNumber` at every flag-chain boundary, objective status transitions (pending → complete/missed) for all three `ObjectiveKind` variants, `inevitable`/`optional` display-only flags not affecting the resolved-count math, phase-tab display logic, and the hotseat manual-override fallback (both the "matches eligible house" and "house not eligible, dice still rolls" branches). Live in the browser this session: fresh hotseat campaign (St Pol) confirmed the modal gates "Advance one week," the manual trade nudge moved Flemish Cloth's price in the chosen direction, and the sabotage checkbox correctly read "no eligible cargo" and stayed disabled; toggled Multi-step turns on mid-campaign and confirmed all three phase tabs switch cleanly with the right panels showing/hiding (Trade panel's vessel buttons and Household panel's Invest/assignment controls each confirmed absent when the other tab was active, not just present when their own tab was); ran the full guided tour end-to-end on a fresh skip-prologue campaign and confirmed both `requiresPhase` steps auto-opened their tabs, including the nested Ledger/Counting-House sub-tab forcing. One unrelated hiccup during this pass: a concurrent session's live edits to `geography.ts`/`MapView.tsx` briefly served a broken Vite HMR update (`ringToPath is not defined`) that crashed the tab; a plain reload recovered cleanly and the map (from that other session's own in-flight "Engraver's Chart" work, see the preceding entries) rendered correctly afterward — not a regression from this phase's own changes. Test campaigns deleted afterward. `tsc --noEmit` (client + api) and `vite build` clean.
- **Scope boundaries, confirmed with the owner and worth restating explicitly**: no real tactical/military system exists or was implied by "deploy the army" — that phrase was illustrative only. The hotseat experiment is intentionally minimal (one house, one device, no lobby/networking/turn-passing infrastructure) and does not touch or gate any real story chapter. Objectives are a read-only display layer over existing flags, not a new win/loss mechanic — reaching a chapter's own finale flag was always how the game "won" that chapter; this just makes the threads visible. **Do not start Chapter 5, build networked multiplayer, or add a real military/deployment system without being asked.**

### 2026-07-25 — Map follow-up: one small, scoped change — more detailed Britain coastline

- Owner steer after the reverted map-style attempt: the real complaint was that the map isn't detailed enough (not the color palette), and the unrequested trapezoid border ("view finder") should never have been added unprompted. Explicit instruction going forward: **small, considered changes, one at a time**, starting with England.
- Checked the source `map/niccolo-map/data/geography.json` before touching anything: Britain's coastline ring has always been a 10-point polygon — the coarsest-but-one shape in the whole gazetteer (only Hibernia/Thule/Scandia/Sicilia/etc. at 4 points are cruder), not something lost in copying. Since it's the one island an actual playable city (London) sits on, it's worth more detail than purely decorative islands.
- Hand-authored a 21-point replacement ring for `content/geography.json`'s `"britain"` entry only — nothing else touched, no code changes, no palette/frame/smoothing changes reintroduced from the reverted attempt. Verified with a plain point-in-polygon check before rendering anything: London (lon 20, lat 54) lands inside the new shape, Calais (22.3, 53.15, correctly on the separate mainland ring) does not.
- Verified live: London now sits next to a real coastal indentation instead of a flat edge; reran the same direct-DOM icon-overlap check as every prior map change (zero collisions across all 20 cities, confirming this purely-data change didn't disturb anything else). `tsc --noEmit` and `vite build` clean.

### 2026-07-25 — Map follow-up: real coastline data for England & Wales (owner wanted "300 points, not 30")

- Owner reaction to the 21-point version: "thats better, but can we add even more vector points... more like 300 than 30." Hand-authoring hundreds more points freehand isn't credible — real coastlines are genuinely fractal, and guessed points would just be more elaborate guessing. Used an actual public-domain coastline dataset (Natural Earth's `ne_50m_admin_0_countries`, fetched directly, United Kingdom feature) instead.
- **The hard part was the coordinate system, not the data**: this map's `"lon"/"lat"` fields are Ptolemaic-style degrees (measured from the Fortunate Isles, historically distorted), not real WGS84 — a real coastline's raw lon/lat can't be pasted in directly. The gazetteer already carries both systems for its cities (`lon`/`lat` plus `modernLon`/`modernLat`), so derived a local linear transform per axis from the England/Flanders cluster (London, Calais, Bruges, Ghent, Antwerp) and anchored it exactly on London — the one hard constraint, since it's the one real game city on this landmass.
- **Deliberately scoped to England + Wales, not all of Great Britain**: tested extrapolating the same transform up to Scotland's latitude and it overshot past the map's own domain boundary (`latMax: 66`) — confirmed this isn't a bug in the transform but a real, deliberate feature of this dataset: Edinburgh's own gazetteer entry (though not a real game city) already encodes Ptolemy's well-documented historical error of drawing Scotland bent northeastward, which a single linear formula can't reproduce from just two calibration points. Rather than force it, kept the previous hand-placed Scotland points (Firth of Forth through Argyll, unchanged) and spliced in real coastline detail only for the England/Wales arc, which is both the actual ask and where the interesting bay/estuary detail lives anyway.
- Extracted Great Britain's outline from the source polygon (508 points; Northern Ireland and other UK-administered islands correctly excluded — Ireland already has its own separate "hibernia" landmass in this map), identified the England/Wales arc by nearest-point match to the real Berwick and Solway Firth border coordinates, trimmed it to the latitude range the transform is actually valid for (dropped the two ends nearest the border, where the linear extrapolation was already visibly overshooting), reversed it to match the ring's existing winding direction, and spliced it between the retained Scotland points — 233 total points, up from 21.
- Verified with a plain point-in-polygon check before rendering anything (same discipline as the prior entry): London still lands correctly inside, Calais still correctly outside. Verified live: London now sits beside real, recognizable coastal detail (an actual Thames-estuary-like indentation, real bays and headlands along the south coast and Wales) instead of a straight-line approximation; the whole island silhouette reads as genuinely Britain-shaped, not a rough polygon. Reran the same direct-DOM icon-overlap check as every prior map change (zero collisions, unchanged). `tsc --noEmit` and `vite build` clean. `content/geography.json` grew from ~4KB to 28KB — noted, not a concern at this size.
- **If Scotland ever gets the same real-data treatment**: it needs its own local transform calibrated near Scotland's own latitude (Edinburgh, if a reliable modern/Ptolemaic pair can be sourced for it or a similar-latitude reference), not an extrapolation of the England-fitted one — flagging this rather than guessing it now, matching the owner's own "small, considered, one at a time" instruction.

### 2026-07-25 — Map follow-up: real coastline data for France's Atlantic & Channel coast

- Owner: "Now we need to do the same for France." Same method as Britain — real public-domain coastline data, transformed via a locally-calibrated linear fit, verified before and after — but France is structurally different: it isn't a separate island ring, it's a stretch embedded inside the single continent-spanning `landmass` ring (Eurafrasia, per that ring's own file comment), alongside land-border segments (Pyrenees, Alps, Rhine) that don't correspond to any coastline at all.
- **Used a physical coastline dataset, not a political boundary one**: the France *country* polygon includes its land borders with Spain/Italy/Switzerland/Germany, which would have spliced in artificial straight-line "coastline" wherever France borders another country inland. Pulled the actual world coastline layer instead (`ne_50m_coastline`) and located France's stretch (187 points, Belgium border to the Spanish Atlantic border) within its giant single Eurasia-Africa trace (10,297 points) by nearest-match to the real border coordinates — the same technique used to isolate England/Wales's arc from Great Britain's ring.
- **Reused the England-calibrated transform** (fit from London/Calais/Bruges/Ghent/Antwerp, anchored on London) rather than deriving a new one — checked it stays within the domain across France's whole latitude range (43.3–53°) before committing to it, unlike Scotland, which failed exactly this check. A wider transform fit across the whole French cluster (adding Dijon/Lyon/Geneva) was tried first and rejected: averaging two structurally different local distortions fit *both* worse (Channel cities off by up to 3.3° when Dijon anchors it) than just using the tighter Channel-only fit and accepting it as the one calibrated transform for this whole coast.
- **Real bug caught before it shipped**: the initial extraction bounding box was generous enough to catch a few points of the Belgian coast (Ostend/Zeebrugge) before the actual France border, which the transform rendered as a spurious loop-and-return near Dunkirk — found by checking Calais's own position against the spliced polygon and seeing it fail a simple point-in-polygon test, traced to the loop, fixed by trimming the arc to start at the real France/Belgium border coordinate (nearest-point match, same technique as the Berwick/Solway cuts) rather than the bounding box's own edge.
- Spliced the 187-point real arc into the existing `landmass` ring in place of its old 3-point approximation of the entire French coast (indices 7–9, replaced; everything else — Iberia, the Mediterranean, the rest of the continent — untouched). `landmass` grew from 62 to 246 points.
- Verified: Calais (a real game port city, right at the new coastline) still reads correctly on sight — sitting on the coast, not adrift in open sea — even though it lands a few tenths of a degree outside a strict point-in-polygon test; expected and correct for a port city whose hand-authored position was never going to land exactly on an independently-sourced coastline, not a defect. Dijon/Lyon/Geneva (inland) all confirmed still inside the landmass. Reran the same direct-DOM icon-overlap check as every prior map change (zero collisions across all 20 cities). `tsc --noEmit` and `vite build` clean. Test campaign deleted afterward.
- **Deliberately not done, flagged rather than folded in silently**: France's Mediterranean coast (Spanish border near Perpignan to the Italian border near Menton — a separate, clean 59-point run of the same coastline dataset, already located and ready to use) needs its own transform validity check before splicing, the same discipline Scotland and this Atlantic arc both needed — not assumed to just work by extending this one further. A natural next small step if wanted.

### 2026-07-25 — Map follow-up: real coastline data for France's Mediterranean coast

- Owner: "yes please, high quality" — completing the France pass, following exactly the discipline flagged as needed in the previous entry rather than assuming the Atlantic transform would extend cleanly.
- **Checked first, as promised**: applied the England/Atlantic-calibrated transform to Lyon/Geneva/Genoa/Florence/Milan (all real gazetteer cities near the Mediterranean coast) and found it was off by 1.7–3.8° for every one of them — confirmed this transform genuinely doesn't reach this region, the same kind of breakdown Scotland hit, not a guess.
- **Fit a proper local transform instead**, calibrated on Lyon/Geneva/Genoa/Florence (all close to the target coastline in both longitude and latitude) and anchored on Genoa specifically, since it sits right where the French coast ends and Italy's own (already-existing, untouched) representation begins. Errors dropped to near-zero for Geneva/Milan and under 1.6° for Lyon/Florence — a properly local fit, not an extrapolation.
- Isolated the Perpignan-to-Menton stretch (48 points) from the same physical-coastline dataset used for the Atlantic pass, by nearest-point match to the real French/Spanish and French/Italian Mediterranean border coordinates (same technique as every border cut so far this feature). Found the existing `landmass` ring represented this entire stretch of coast with a single point — spliced the 48-point real arc in to replace it, everything else (Iberia's own coast, Italy, the rest of the Mediterranean) left untouched. `landmass` grew from 246 to 293 points.
- Verified: Dijon/Lyon/Geneva (inland) and Genoa/Milan/Florence (the nearby Italian coast, unaffected by this change) all confirmed still correctly positioned relative to the new coastline. Reran the same direct-DOM icon-overlap check as every prior map change (zero collisions across all 20 cities). `tsc --noEmit` and `vite build` clean. Test campaign deleted afterward.
- **France's coastline is now real data end to end**: Belgian border, around the Atlantic, down to Spain, across the Mediterranean to Italy — the whole thing, not just the parts nearest existing game cities.

### 2026-07-25 — Map bug fix: self-intersecting coastline near Iberia ("Spain looks ruined")

- Owner report while looking at the live map: "spain looks ruined." Zoomed the browser directly onto Lisbon (dispatching real `WheelEvent`s at its actual DOM position, not eyeballing a static screenshot) and found a genuine tangle — jagged, crossing lines right where Iberia's coastline meets the newly-real French coast — not a rendering/screenshot artifact.
- **Root cause, found with a proper geometric check, not a guess**: wrote a segment-intersection scan over the entire 293-point `landmass` ring (checked every non-adjacent edge pair for a real crossing) and got two confirmed self-intersections, both right at the seam between the France Atlantic splice and Iberia's own untouched crude hexagon.
- Traced it to the France Atlantic commit (`0d12f68`): its real-coastline extraction correctly found the France/Belgium border but overshot the France/Spain border by 22 points, continuing down what turned out to be real coastline for **Spain's own Cantabrian coast** (confirmed by inverting the transform on the tail points — they land almost exactly on Spain's real north coast, e.g. `[12.9, 39.55]` → modern ≈ `(-6.24°, 42.17°)`, the Galicia/Asturias coast) before the splice cut off. Immediately after that mislabeled tail, the ring continues into Iberia's own old crude hexagon (which independently starts at the same real-world area, Galicia) — two different, non-agreeing representations of the same stretch of coast, crossing each other.
- **Fix, minimal and targeted**: identified the one point in the over-extended tail that actually matches the real France/Spain Atlantic border (Hendaye, `[18.47, 41.38]` ptolemaic — within ~0.4° of the border's transformed position) and truncated the arc there, deleting exactly the 22 erroneous points after it (`landmass` 293 → 271 points). Left Iberia's own crude hexagon untouched — redoing Iberia properly with real data is a separate future pass, not folded into this fix.
- Verified: reran the segment-intersection scan — zero self-intersections anywhere in the ring (was 2). `tsc --noEmit` and `vite build` clean. Live in the browser: zoomed back onto Lisbon and confirmed the tangle is gone — a clean (if still crude, since Iberia itself hasn't been redone) hexagon with a single honest seam to the real French coastline, not a chaotic scribble.
- **Note for whoever eventually redoes Iberia's own coastline**: the deleted 22-point tail was genuine, accurate Natural Earth data for Spain's Cantabrian coast under the England-calibrated transform — worth checking whether that same transform still validates near Iberia's own latitude before reusing it, rather than re-extracting from scratch.

### 2026-07-26 — Map follow-up: real coastline data for Italy's mainland coasts

- Owner: "do Italy next." Italy is structurally different from every region done so far — a long peninsula with two coasts on opposite sides (Tyrrhenian/west and Adriatic/east) facing different seas, not one contiguous stretch — so this ran as two independent research passes (one per coast) rather than one linear extraction, each deriving and validating its own local transform before touching anything.
- **Tyrrhenian (west) coast, Menton (France border) to just past Salerno**: fit and leave-one-out validated on genoa/florence/naples (anchored on Genoa), errors 0.03–1.5° across all four northern/central Italian gazetteer cities including a held-out Rome and an untouched Milan — comfortably under the danger threshold. Extracted 67 points from the same `ne_50m_coastline` dataset by nearest-match to the real Menton border coordinate, deliberately stopping just past the Sorrento Peninsula/Amalfi coast (a genuinely detailed, worthwhile stretch) rather than pushing on into Calabria/the toe/Sicily.
- **Adriatic (east) coast, Italy/Slovenia border (near Trieste) to Ancona**: a separate local transform, fit and leave-one-out validated on venice/genoa/milan/florence (anchored on Venice, the one real game city on this coast — errors 0.08–0.27°); confirmed Rome/Naples diverge badly (2.98–4.84°) under this same transform, i.e. correctly scoped to the northern Adriatic only, not extended south. 36 points extracted the same way, by nearest-match to the real Trieste-area border coordinate.
- **A genuine self-intersection surfaced during splicing, caught before shipping, not after**: connecting the new Tyrrhenian arc directly to the existing French Mediterranean arc's own endpoint crossed one of the new arc's own early points — two independently-fit local transforms don't perfectly agree at a shared border (expected, same as every prior seam), but this particular mismatch pinched into a real crossing rather than just an untidy jog. Found by rerunning the same segment-intersection scan used for the Spain fix above *before* writing the file, not after. Fixed by dropping the new arc's single redundant near-duplicate border point (the existing French Mediterranean endpoint already covers that exact spot) — one point, not a refit.
- Spliced both arcs into the `landmass` ring in place of the old whole-peninsula crude placeholder (which was never one contiguous approximation — separate short runs for each coast), keeping the still-crude heel/Otranto/Calabria/toe/Sicily-strait stretch between them untouched. `landmass`: 271 → 367 points.
- Verified: the same segment-intersection scan across the entire ring — zero crossings. Point-in-polygon check for all six Tyrrhenian/Adriatic gazetteer cities (milan, venice, genoa, florence, rome, naples) — all six land correctly inside, including the port cities (Genoa/Naples/Venice), which is stricter than the "may miss by a few tenths of a degree" standard accepted for ports elsewhere in this project. `tsc --noEmit` and `vite build` clean. Live in the browser: the Ligurian gulf's real angular shape, the Sorrento/Amalfi peninsula's jagged detail near Naples, and Venice's own coastal approach all render as a single coherent, non-chaotic peninsula silhouette.
- **Deliberately left for a future pass, flagged rather than folded in silently**: Calabria, the boot's toe, and the Adriatic coast south of Ancona (Puglia/the heel) — still the old crude placeholder, connecting the two new arcs with a couple of long straight jumps, the same character as every other not-yet-detailed stretch in this ring. Sicily (currently a separate crude 4-point island entry) untouched — out of scope for "Italy" as the mainland peninsula.
- **Unrelated, pre-existing issue noticed in passing, not fixed here**: a direct-DOM icon-overlap check at the default view found Bruges/Ghent/Antwerp's city-icon hitboxes touching by a couple of pixels — this doesn't involve `landmass` or any coordinate this session touched (city x/y positions are untouched), so it predates this work and is flagged for a separate pass, not folded into a map-geography commit.

### 2026-07-26 — Map follow-up: real coastline data for Iberia (~450 points, up from 6)

- Owner: "spain points need to be extended to 200 points." Iberia is the sparsest calibration case tackled yet — the gazetteer has exactly one real anchor on its mainland coast, Lisbon (Madeira is a separate remote island). Every prior region had at least two or three nearby cities to fit and cross-validate a transform; here there's only one, so the usual leave-one-out check isn't possible and had to be replaced with a different kind of validation (below).
- **First attempt failed — a real lesson, not just a footnote**: tried anchoring a 2-point transform on Lisbon plus the existing shipped France-Atlantic arc's own Hendaye endpoint, on the theory that forcing exact agreement at both ends would guarantee a clean seam. It backfired: Hendaye's inherited ptolemaic latitude (41.38) and Lisbon's own historically-recorded latitude (41.0) are almost identical despite sitting 4.7° apart in reality, which forced the fitted latitude coefficient down to ~0.08 — a degenerate, nearly-flat transform that compressed Portugal's real ~7° latitude range into a sliver and sent the new arc slicing back through the existing France arc's own coastline. Caught immediately by the same segment-intersection scan used for every fix this session (10 crossings, Lisbon itself failing point-in-polygon) — before it was shown to anyone, not after.
- **Fix: stopped trying to force agreement between independently-sourced ptolemaic truths.** Fit the Atlantic and Mediterranean coasts as two separate arcs (Iberia has two coasts facing different seas, same structural shape as Italy), each using the already-validated French-Mediterranean cluster's slope (lyon/geneva/genoa/florence/rome) but re-anchored on its own single real hard point — Lisbon for the Atlantic arc, Cerbère (the existing France-Mediterranean arc's own start point) for the Mediterranean arc — rather than trying to satisfy two conflicting anchors at once. This is the same "borrow a validated slope, re-anchor the intercept" technique used throughout this feature, just applied to two anchors instead of one shared line. Confirmed computationally against three other candidate combinations (reusing the England/Atlantic transform's slope instead, or forcing exact continuity at the internal Hendaye/Tarifa seams) — all three produced real self-intersections; only this combination came back clean.
- Extracted both arcs from the same `ne_50m_coastline` dataset by nearest-match to the real Hendaye, Tarifa (Strait of Gibraltar), and Cap Cerbère border coordinates: 170 points for the Atlantic coast (Hendaye to Tarifa, passing within 0.04° of Lisbon's real position), 87 points for the Mediterranean coast (Tarifa to Cerbère) — both at full native resolution.
- **Accepted seam gaps, not hidden**: this approach doesn't force perfect continuity at Hendaye, at the internal Atlantic/Mediterranean boundary (Tarifa), or anywhere else — each is a single straight jog, the same character every prior seam in this feature already has, just larger here (1.6–3.9°) because Iberia only has one real anchor per coast to work from instead of several.
- Verified: the segment-intersection scan across the whole 616-point ring — zero crossings. Lisbon lands within 0.007° of the drawn coastline (essentially exactly on it, the same "correct for a port city sitting right on the boundary" standard already accepted for Calais/Genoa/Naples — a strict point-in-polygon test isn't the right tool for a city exactly on the ring). `tsc --noEmit` and `vite build` clean. Live in the browser: Portugal's real coastal bulge and bays are now clearly recognizable, non-chaotic, and the seam jogs to the existing French coast read as simple straight lines, not tangles. Reran the direct-DOM icon-overlap check — same pre-existing Bruges/Ghent/Antwerp result as before, confirming nothing new broke.
- **Flagged for whoever picks this up next**: with only one real anchor per coast, this transform is far less constrained than Britain's, France's, or Italy's — it's internally consistent and geometrically sound, but there's no second data point to catch a systematic offset the way Rome caught one for Italy's Adriatic transform. If a genuine second Iberian reference point ever gets added to the gazetteer (a real historical Ptolemaic-Iberia coordinate, not a guess), it's worth re-deriving and re-validating this transform properly rather than assuming it's exactly right.

### 2026-07-26 — Map follow-up: real coastline data for Africa (North Mediterranean + West Atlantic coasts)

- Owner: "then africa needs work." Africa in this gazetteer has two real anchors, both far from each other and each other's coast: Alexandria (tagged `levant`, but geographically North Africa's Mediterranean coast) and Gambia (tagged `africa`, on the West Atlantic coast, a real Chapter 4 city) — plus Timbuktu (`africa`, deep inland Mali, useful for a slope fit but not itself coastal). No single transform could plausibly span both without repeating the Iberia lesson, so this split into two independently-anchored arcs the same way, this time confirmed against four slope candidates rather than assuming the first attempt was right.
- **North African Mediterranean coast, Alexandria to Ceuta**: only one real anchor (Alexandria) exists anywhere on this stretch, so — lacking a second Africa-region point to fit a proper slope the way Iberia used the French-Med cluster — used an identity slope (real degrees mapped 1:1, offset-only) re-anchored on Alexandria's own gazetteer position. Tested against three other slope candidates (the French-Med cluster's slope, the England/Atlantic slope, a flatter 0.7 scale); only the identity slope came back with zero self-intersections against the rest of the ring on the first pass.
- **West African Atlantic coast, Ceuta to Gambia**: better-anchored than the Mediterranean piece — fit an actual 2-point slope from Gambia and Timbuktu's own real modern/ptolemaic pairs (both genuine gazetteer entries, not a borrowed or guessed slope), then re-anchored the intercept on Gambia specifically, since it's the one real coastal city here. A small local pinch surfaced right at the Gambia end (the same kind of near-duplicate-point crossing found and fixed for the Tyrrhenian/French-Mediterranean seam) — resolved the same way, by trimming the arc's last two redundant points rather than accepting a self-intersection.
- Extracted both arcs from the same `ne_50m_coastline` dataset by nearest-match to the real Alexandria, Ceuta (Gibraltar-strait Morocco/Spain corner), and Gambia river-mouth coordinates: 238 points for the Mediterranean arc, 166 points for the Atlantic arc (from a 170-point raw extraction, minus the 3 points trimmed for the Gambia-end pinch and the shared Ceuta duplicate) — both full native resolution, replacing the old whole-continent placeholder's 14 crude points for these two stretches only.
- Verified: the segment-intersection scan across the resulting 1007-point ring — zero crossings. Gambia and Alexandria both land exactly on the drawn coastline (0.0° distance, both being their own arc's anchor point) — expected and correct for a port city on the boundary, the same standard as Lisbon/Calais/Genoa/Naples. Timbuktu (correctly inland, not on the coast) tests inside the landmass polygon. `tsc --noEmit` and `vite build` clean. Live in the browser: the West African coast's real Atlantic bulge (Morocco down through Senegal/Gambia) renders as a single clean, recognizable, non-chaotic shape. Reran the direct-DOM icon-overlap check — same pre-existing Bruges/Ghent/Antwerp result, confirming nothing new broke.
- **Deliberately left for a future pass, flagged rather than folded in silently**: the rest of Africa's coastline — the Gulf of Guinea eastward, the entire Indian Ocean and southern African coast, and the Red Sea — was never attempted even crudely in the original 62-point sketch (Ptolemy's own historical Geography didn't extend that far either), so there's nothing further south or east to extend yet without inventing a route the game has no reason to need. The Mediterranean arc's transform in particular rests on a single real anchor (Alexandria) with no second point to cross-check it against, the same caveat flagged for all of Iberia — treat it as internally consistent and geometrically sound, not as precisely calibrated.

### 2026-07-26 — Map follow-up: real coastline data for the rest of Italy (Calabria, the toe, Puglia, the heel)

- Owner: "please also fix the rest of italy" — closing the gap deliberately left open when the Tyrrhenian and Adriatic arcs shipped: the whole southern Italian coastline (Calabria's two coasts, the toe, Puglia, the heel) was still just 2 crude points connecting the two real arcs.
- **First attempt failed for a new reason, not a repeat of Iberia's**: tried forcing the new arc to land exactly on both existing endpoints (the Tyrrhenian arc's own last point and the Adriatic arc's own first point, both already-shipped ptolemaic values) via a 2-point fit. This produced a healthy, non-degenerate latitude coefficient (unlike Iberia's flat-latitude failure) but a wildly amplified longitude coefficient (4.41) — because the two endpoints sit close together in both real and ptolemaic space, while the actual coastline between them loops far away (around the whole toe and heel). The amplified longitude stretched that loop's bulge straight into the ptolemaic-space already occupied by the old crude Balkans placeholder further along the same ring, producing 6 real crossings. Caught by the same segment-intersection scan before anything shipped.
- **Fix**: stopped forcing the far (Ancona) endpoint. Anchored only on the near end — the Tyrrhenian arc's own already-shipped last point — and swept the longitude coefficient down from the Tyrrhenian arc's own validated slope (1.79) until the Balkans overlap cleared, landing on 1.5 (latitude coefficient kept at the Tyrrhenian's own validated 0.96). Zero self-intersections at that value. The Ancona end is left with an accepted seam gap (3.9°/-0.05°), the same character as every other seam in this feature.
- Extracted 91 points from the same `ne_50m_coastline` dataset, continuing directly from the exact raw point the shipped Tyrrhenian arc's own extraction stopped at (no gap, no re-matching needed) through to the same raw point the shipped Adriatic arc starts from — covering the Cilento coast, both sides of Calabria, the toe, the Gulf of Taranto, Puglia, and the heel in one continuous real trace, replacing the old 2-point placeholder. `landmass`: 1007 → 1096 points.
- Verified: the segment-intersection scan across the whole ring — zero crossings. All six Italian gazetteer cities (milan, venice, genoa, florence, rome, naples) still test correctly inside the landmass. `tsc --noEmit` and `vite build` clean. Live in the browser: the peninsula's silhouette now reads as one coherent shape with a real toe and heel, not a blunt triangle stump.
- **Sicily attempted and deliberately rolled back — no real anchor to validate against**: pulled Sicily's own real coastline (a separate 90-point line in the same dataset) and tried anchoring it off the new Calabria arc's own Strait-of-Messina point, reusing the same validated slope. It came back with 5 real crossings against the mainland coast, and — critically — there's no actual gazetteer city on Sicily to check the result against, unlike every other splice this session. Rather than hand-tune an unvalidatable transform until the crossings happened to disappear, left Sicily as its existing crude 4-point placeholder, the same call already made and documented for Sardinia/Corsica/Baleares. Flagging this explicitly rather than silently shipping a guessed shape.
- **Still open, for a future pass**: Sicily's own coastline (needs either a real anchor or an explicitly-accepted unanchored approach, decided deliberately rather than defaulted into), and the pre-existing Bruges/Ghent/Antwerp icon-overlap noted (not touched) in the earlier Tyrrhenian/Adriatic entry.

### 2026-07-26 — Map follow-up: real coastline for Cyprus and Trebizond's Black Sea coast

- Owner: "lets do 1 and northern europe please" (following a recommendation to prioritize regions with real anchor cities still sitting on crude geography) — this entry covers "1": Cyprus (Famagusta, Kouklia — both Chapter 3) and Trebizond (Chapter 2).
- **Cyprus — the best-anchored case since Venice**: unlike every other island touched this session, Cyprus has two real gazetteer cities on it. Fit a proper 2-point transform from Famagusta and Kouklia's own modern/ptolemaic pairs (no borrowed slope needed), replacing the island's existing 7-point approximation with 49 real points from the same `ne_50m_coastline` dataset. Both cities land within 0.008–0.022° of the new coastline. Zero self-intersections within Cyprus's own ring, zero crossings against the mainland.
- **Trebizond — a much narrower scope than the region's name suggests**: the crude mainland ring's Balkans-to-Egypt stretch (13 points, Trieste to Alexandria) only has one point that plausibly stands in for the Black Sea/Trebizond area at all, and the rest — Balkans, Greece, the Aegean, Anatolia's south coast, the broader Levant — has no real anchor whatsoever to fit or validate a transform against. Rather than guess a shape for a coastline with zero ground truth (the same discipline that ruled out extending Iberia's transform to Scotland, or forcing Sicily through), replaced only that one point: extracted 61 real points of Turkey's central Black Sea coast around Trebizond and anchored them with an identity slope (real degrees 1:1, offset-only — the same choice made for Alexandria's arc, for the same reason: exactly one real anchor available). Trebizond lands exactly on the new coastline (0.0°).
- Verified: the segment-intersection scan across the whole ring — zero crossings. `tsc --noEmit` and `vite build` clean. Live in the browser: Cyprus now reads as a real, recognizable shape (the Karpas peninsula's eastward finger is visible), and the Black Sea coast around Trebizond shows a real headland instead of a straight-line guess. Reran the direct-DOM icon-overlap check — same pre-existing Bruges/Ghent/Antwerp result, confirming nothing new broke.
- **Deliberately left for a future pass**: the Balkans, Greece, the Aegean, Anatolia's south coast, and the broader Levant coast (Lebanon/Israel/Gaza) between Trieste and Alexandria — still the old crude placeholder except for the one Trebizond-area point fixed here. None of these have a real gazetteer anchor at the moment; fixing them properly would need either new reference points or an explicit, flagged decision to use an unanchored approach, the same call already made and reversed for Sicily.

### 2026-07-26 — Map follow-up: real coastline for Scotland and the Danzig/Baltic coast

- Owner: "lets do 1 and northern europe please" — this entry covers "northern Europe": Scotland (finally revisited — flagged as unfinished all the way back at the original England/Wales pass) and the Danzig-area Baltic coast.
- **Scotland — the flagged limitation from the England/Wales pass finally addressed, with a real fix rather than the originally-assumed one**: back when England/Wales shipped, Edinburgh was suggested as "if a reliable modern/Ptolemaic pair can be sourced" for a future Scotland transform. It's in the gazetteer, so it looked like the obvious anchor. It isn't: anchoring on Edinburgh's own recorded ptolemaic position (using the England transform's own slope) produced 15 self-intersections — Edinburgh's gazetteer entry itself carries forward Ptolemy's well-documented historical error of bending Scotland northeastward, and a real, undistorted coastline can't be forced through a deliberately-distorted reference point without contradicting itself.
- **Fix**: stopped trying to anchor on Edinburgh at all. Anchored instead on the real geographic connection point — the Solway Firth border coordinate — matched directly to the *existing already-shipped* England/Wales arc's own start point, using the England transform's own validated slope. Zero self-intersections, and a normally-proportioned shape (unlike an earlier discarded attempt that forced exact agreement at both the Solway *and* Berwick ends simultaneously, which produced a healthy 0-crossing result but crushed Scotland's real ~4° latitude range into a 0.3° sliver for the same underlying reason Iberia's first attempt failed — accepted a seam gap at the Berwick end instead, same as everywhere else in this feature).
- Extracted 231 real points for Scotland from the same Great Britain coastline line the original England/Wales pass used (bbox lat 54.7–58.65, correctly excluding Shetland/Orkney), replacing the hand-placed 6-point Firth-of-Forth-through-Argyll placeholder. `britain` island ring: 233 → 457 points.
- **Danzig/Baltic coast — a single-anchor case like Trebizond, not a full Northern-Europe pass**: the crude landmass ring's opening 7 points (the very start of the ring, representing the huge Netherlands-to-Scandinavia gap that neither the Britain nor the France Atlantic work ever touched) have only one real anchor — Danzig, a real `baltic`-region gazetteer entry (not a playable city) — and it doesn't clearly correspond to any single one of those 7 points by longitude, only loosely by latitude. Replaced just the one point with the closest latitude match (`[36, 55]`) with 36 real points of the Baltic coast around Gdańsk, anchored with an identity slope (single anchor, same reasoning as Alexandria/Trebizond) after finding the initial extraction window clipped through the real, genuinely-looping Curonian Spit/Kaliningrad lagoon and had to be trimmed back to a simpler stretch to clear a self-intersection. `landmass`: 1156 → 1191 points.
- Verified: the segment-intersection scan across the whole ring (both `landmass` and the `britain` island) — zero crossings in each. `tsc --noEmit` and `vite build` clean. Live in the browser: all of Great Britain, Scotland included, now reads as one real, recognizable island with genuine sea lochs and a complex west coast, not a blunt hand-drawn stump. Reran the direct-DOM icon-overlap check — same pre-existing Bruges/Ghent/Antwerp result, confirming nothing new broke.
- **Deliberately left for a future pass**: the rest of the Netherlands/Germany/Denmark/Scandinavia coastal gap (still the old crude placeholder except for the one Danzig-area point fixed here), and Scandia/Thule (the crude 4-point island placeholders for Scandinavia and Iceland/Norway) — none of these have a real gazetteer anchor at the moment.

### 2026-07-26 — Phase 15: queued multi-hop voyages, chapter-close feedback, forward-looking objective hints

- Owner feedback, two complaints at once: "the game doesnt have advancements or a point yet... just accumulating wealth without point. Need to make it clearer how things can happen," and "cant leap frog cities even by boat, would be good to understand why this is." Answered the second directly from the actual code before proposing anything: `dispatchVessel`/`findRoute` require a *literal direct edge* in the `ROUTES` array — there has never been any multi-hop pathfinding anywhere in the sim, so a ship at Antwerp genuinely cannot dispatch straight to Venice (no direct edge) and must backtrack through Bruges first; `CityPreviewPanel.tsx` only ever said "Not reachable directly," with no explanation of the route graph or what to do instead.
- Ran a background design workflow (parallel design + adversarial critique, one front per problem) before writing any code, grounding both fronts in facts pulled directly from source rather than assumption: the full current `ROUTES` graph for the movement front; the verified history that chapter1/2/3_complete produce *zero* UI feedback today (each new chapter's own full-screen "ending" screen was historically just moved forward to the new frontier rather than anything being left behind at the old boundary) for the advancement front, plus the design doc's own Chapter 8 ("Epilogue screen prices the whole campaign: net worth, people kept, Conscience, secrets never sold") as precedent for what "scoring" vocabulary this project already intends — 4 chapters away, not something to pull forward as an ambient ticker now. The critique pass caught two real issues in the first-draft proposals before anything was built: a chapter-close card's "seen it" tracking needed to be *persisted* state, not a component-local ref (a save/reload between the flag flipping and the player clicking Continue would otherwise eat the card permanently), and an ambient progress bar's *fill-width math* must never bake in an unshipped chapter-8 total, even if the visible "of 8" text were dropped. Both fixes were built in from the start. Presented the resulting menu via `AskUserQuestion`; owner picked, in order, "Path preview + queue" for movement, "Chapter-close card + header label, plus a Chronicle log" for advancement, and — critically — "I also want forward-looking hints," not just retrospective acknowledgment.
- **Movement: real multi-hop pathfinding as a UI convenience, not a new dispatch mechanic.** New `planRoute(fromId, toId, landOnly)` in `sim/content.ts` — a plain Dijkstra over the existing `ROUTES` graph (weights = `distanceWeeks`), reusing `reachableFrom`'s own `landOnly` filter so a courier is never routed over a sea/river leg. Used *only* by the UI: `dispatchVessel`/`findRoute` in `sim/actions.ts` are completely untouched, so every per-leg system that keys off a specific route (`sim/insurance.ts`'s premium/risk pricing, `sim/expedition.ts`'s Gambia/Timbuktu disease clock) still fires exactly once per real dispatch, unchanged. `CityPreviewPanel.tsx` now computes a path when a clicked city isn't a direct neighbour and shows it explicitly ("Not reachable directly. Nearest path: Bruges → Dijon (3wk) → Geneva (2wk) → Milan (2wk) — 3 dispatches, 7 weeks of sailing. The Charetty ship will dock — and can trade — at each stop; you'll be prompted to continue on arrival."), plus a "Queue journey to Milan" button. A new optional `Vessel.plannedRoute?: string[]` (remaining route ids) is set explicitly on *every* `DISPATCH_VESSEL` call (undefined if not passed) — this is what makes a manual redispatch elsewhere correctly drop a stale plan rather than leaving a "Continue to X?" prompt pointing at a city the vessel is no longer chained toward, a real gap the design critique caught before it shipped. Two new actions: `CONTINUE_PLANNED_ROUTE` (resolves the next queued route id to a real destination from the vessel's *current* location, then calls the exact same `dispatchVessel` any manual voyage uses — same insurance quote, same risk roll, same expedition tracking) and `CANCEL_PLANNED_ROUTE` (drops the plan without moving the vessel — it stays a normal docked, tradeable, freely-redirectable vessel). `GameScreen.tsx`'s Fleet & Household list shows a "Continue on to Geneva? 2 legs remaining" prompt (with its own per-vessel insure checkbox, so a queued leg's insurance decision is never silently skipped) on any docked vessel with a non-empty plan.
- **Advancement: fixed a verified bug (chapter transitions were completely silent) plus one deliberately-scoped-out feature the owner also asked for the same day.** New `sim/objectives.ts` export `CHAPTER_TITLES` (the design doc's own §9 chapter names). New `GameState.lastAcknowledgedChapter?: number`, seeded at creation to match the campaign's actual starting chapter (`skipPrologue ? 1 : 0`) so no false "Chapter 0 complete" card appears for a campaign that skipped the prologue entirely. New `ACKNOWLEDGE_CHAPTER` action (`sim/actions.ts`) is the one exception added to `processAction`'s pending-event guard — it must go through even while the next chapter's own opening event is already queued in `pendingEvents`, which it typically is by design (that event's trigger fires the same tick the previous chapter's flag does), or the acknowledgment could never be dismissed at all; verified this exact interaction directly (dispatching `ADVANCE_WEEK` is still correctly blocked while an event is pending — the bypass is scoped to `ACKNOWLEDGE_CHAPTER` only). New `components/ChapterCompleteCard.tsx` (styled from `EventOverlay`'s own chrome) shows "Chapter N complete," that chapter's resolved objectives, and a Continue button; `GameScreen.tsx` renders it *instead of* the already-queued next event whenever `currentChapterNumber(state) > (state.lastAcknowledgedChapter ?? 0)`, so the card is a pure render-order insertion, not a change to any trigger logic. New `components/CampaignProgress.tsx` is a small always-visible header readout ("Chapter 2 — The Spring of the Ram" plus one filled segment per chapter reached so far) — deliberately renders *no* denominator, not even in the segment-count math, since Section 14 treats chapters 5-8 as unconfirmed, not a committed roadmap. New `components/ChronicleLog.tsx` (opt-in, a new header "Chronicle" button, hidden together with the objectives panel behind the existing "Hide chapter objectives" toggle) lists every already-closed chapter's resolved objectives, reusing `objectivesForChapter` re-read for a past chapter number — zero new GameState writes, same read-only-projection discipline `ObjectivesPanel` already established. Extracted a shared `ObjectiveRow` (plus `statusColor`/`statusSuffix`) out of `ObjectivesPanel.tsx` so the live panel, the chapter-close card, and the Chronicle all render a resolved thread identically rather than three subtly-drifting copies.
- **Explicitly not built: a net-worth/score readout.** The design critique flagged this hard: an always-visible derived "your number" becomes a de facto score just by being visible every session, which is exactly the win-condition-by-the-back-door the owner's own "no scripted victory" framing rules out. The design doc's real Chapter 8 epilogue (4 chapters away) is where that number belongs, as a one-time reveal — not as an ambient ticker running from Chapter 1. Left alone.
- **Forward-looking objective hints, the third confirmed decision.** New `Objective.revealFlag?: string` (`sim/types.ts`) and `ObjectiveProgress.revealed` (`sim/objectives.ts`: `status !== 'pending' || !revealFlag || state.flags[revealFlag]`) — an objective's `description` only shows once its own flag fires, or once the thread is no longer pending (complete/missed always reveals — there's nothing left to spoil once a thread resolves, only context to give, even if its own reveal flag never happened to fire, e.g. declining a commission before it's ever offered). Traced the actual Chapter 1 event chain before authoring anything, rather than guessing: found the cannon-shipment objective's existing description ("Buy at Milan, sail it to Naples via Genoa...") was a real, if minor, spoiler — it showed the *entire solution* from turn 1, before `ev_c1_026`'s commission-offer event has even fired, so `content/objectives/chapter1.json`'s `obj_c1_cannon` now gates it behind `revealFlag: "cannon_commission_accepted"`. `obj_c1_condotta` (previously blank — the panel showed literally nothing for it) got both a real description and a `revealFlag: "condotta_naples_active"` (Astorre's own offer event). Two other previously-blank objectives, `obj_c2_epilogue` and `obj_c3_epilogue`, got real descriptions with no gating (they're automatic aftermath beats with no real lead to hide — nothing to spoil). `ObjectivesPanel.tsx` shows "No lead yet — keep the story moving." in place of `description` while unrevealed.
- **Verification.** Scripted driver (`processAction`/`planRoute`/`objectivesForChapter` called directly, seeded past skipPrologue's own `pendingEvents` guard — the same recurring bug class this project's own drivers have hit before, caught again here and fixed the same way): 32/32 assertions covering `planRoute`'s shortest path for both a ship and a land-only courier, a disconnected-path null result, the full queue→arrive→continue→arrive chain (including a real mid-voyage scripted-event interruption drained automatically, not sidestepped), a manual redispatch correctly clearing a stale plan, `CANCEL_PLANNED_ROUTE` leaving the vessel untouched, `ACKNOWLEDGE_CHAPTER`'s persistence and its pending-event bypass (and that the bypass is scoped correctly — ordinary actions stay blocked), and `revealFlag` gating including the "resolved without ever revealing" edge case. Live in the browser (this session's own dedicated preview server, since another session's dev server was already running against the same port): queued a real Bruges→Dijon→Geneva→Milan voyage on a fresh campaign and watched two unrelated Chapter 1 events fire mid-journey without derailing the queue; confirmed the "Continue" prompt's next-city name and remaining-leg count at each stop, and that "Not yet" cleared the plan without moving the vessel; forced a real chapter1_complete transition via a save's own localStorage entry (same technique Phase 13's own verification used for save-compat testing) and confirmed the Chapter-close card shows the correct title/objectives, that clicking Continue persists (survives a full page reload without reappearing), and that the queued Chapter 2 opening event then renders normally afterward; opened the Chronicle log and confirmed it lists Chapter 0 (correctly empty, collapsed by default) and Chapter 1 (expanded by default, matching the live panel's own row rendering exactly); confirmed the header's ambient segment count (3 filled segments at chapter 2, not tied to any total). Zero console errors throughout. `tsc --noEmit` (client + api) and `vite build` clean. Test campaign abandoned afterward.
- **Scope boundaries, confirmed with the owner and worth restating**: no real numeric win/score condition was added or implied — objectives and the Chronicle remain a read-only display layer over existing flags, and the net-worth idea was explicitly deferred to the real Chapter 8 epilogue rather than built now. Movement stays a chain of real, individually-insured single-hop dispatches — a vessel still docks, and can trade, at every intermediate city; true non-stop multi-hop voyages (Option 4 from the design menu) were explicitly not built this phase and would need their own follow-up design pass (new insurance-repricing timing, expedition leg-boundary tracking, a save migration) if ever requested. **Do not start Chapter 5, build true non-stop multi-hop dispatch, or add any numeric net-worth/score display without being asked.**

### 2026-07-26 — Phase 15 follow-up: fixed the queued-journey "Continue" prompt going dead behind a scripted event

- Owner report right after Phase 15 shipped: "it is buggy, it stops and doesnt advance as soon as it hits another city despite being queued." Reproduced live rather than guessing: queued a real Bruges→Dijon→Geneva→Milan voyage, and the moment the ship's arrival at an intermediate city coincided with an unrelated Chapter 1 scripted event also being pending (common — Chapter 1 fires fairly often on `dateAfter`/`location` triggers, and this happened on nearly every "Advance one week" click during testing), clicking the visually-normal "Continue" button did *nothing*, with zero feedback.
- **Root cause**: `EventOverlay`/`ChapterCompleteCard`'s backdrops are `position: fixed, inset: 0, zIndex: 100` — genuinely full-viewport, sitting on top of the entire sidebar including the Fleet & Household vessel list. But the backdrop is a translucent `rgba(8, 6, 4, 0.78)`, not opaque, so the "Continue"/"Not yet" buttons underneath still render at close to normal brightness against this game's already-dark palette — nothing visually signals they're inert. A click there lands on the backdrop, not the button, and is silently swallowed. This is a real, confirmed defect, not a UX-taste question: the control looked clickable and wasn't, with no error and no explanation, exactly matching "despite being queued, it stops."
- **Fix, scoped to the one spot this actually bites**: `GameScreen.tsx`'s Continue/Not yet block now checks `pendingEvent || showChapterCompleteCard` first — while either is true, it renders a plain-text line ("Continuing on to Milan waits on the matter above being resolved first.") instead of the buttons, so there's no dead-looking control to click and the plan's survival stays visible. Deliberately did *not* add `CONTINUE_PLANNED_ROUTE` to `processAction`'s pending-event bypass (the way `ACKNOWLEDGE_CHAPTER` is) — dispatching a vessel is a real commercial action, and the design doc's own principle ("personal events interrupt commercial play, not run alongside it") says it *should* stay blocked until the event resolves; the bug was purely that the UI didn't reflect that truthfully.
- **Verified live, the exact failure mode reproduced naturally** (not forced): queued Bruges→Dijon→Geneva→Milan again; an unrelated event landed exactly as the ship reached Geneva; confirmed the new explanatory line shows instead of dead buttons; resolved the event; confirmed the real "Continue"/"Not yet" buttons reappear and are genuinely clickable, dispatching the final leg to Milan correctly. No scripted-driver assertion added for this one — the bug and the fix are both pure JSX render-order/conditional logic (no sim/reducer change), so live browser verification is the meaningful check here, not a driver over `processAction`. `tsc --noEmit` and `vite build` clean. Test campaign abandoned afterward.
- **If a future session adds another per-vessel or per-city action button anywhere in the sidebar, check this same class of bug** — anything rendered outside the modal's own card but still under its full-viewport backdrop needs to either hide itself or visibly disable itself while `pendingEvent`/`showChapterCompleteCard` is true, rather than relying on the backdrop's opacity to look "obviously" inert.

### 2026-07-26 — Map follow-up: real coastline near Constantinople and Rhodes

- Owner: "please go ahead and get new reference data to ensure they are robust" — following up on the flagged Balkans/Aegean/Levant gap. Before researching brand-new sources, checked whether the gazetteer already had unused real anchors nearby: it did. Constantinople and Rhodes (both real, historically-sourced `aegean`-region entries) were sitting unused the whole time the Trebizond fix was being scoped down to "just one point" — this entry uses them properly.
- **Tried a combined 2-point fit between Constantinople and Rhodes first, and it failed for a now-familiar reason**: their real modern longitudes are almost identical (29.0° vs 28.2°) while their recorded ptolemaic longitudes differ by 2.3° — the same kind of numerically unstable pairing that ruled out Venice+Milan for Italy and London+Edinburgh for Scotland (two points too close together on one axis relative to their real distance apart, here driven by how much more the two cities differ in latitude than in longitude). Treated each as its own single-anchor case instead, same as Trebizond and Alexandria.
- **Constantinople**: replaced the single closest-matching old crude point (`[54, 41.8]`, the ring's own approach to the Bosphorus/Black Sea entrance) with 59 real points of the Sea of Marmara/Bosphorus coast, anchored with an identity slope. A tight local self-crossing surfaced right at the far end of the extraction window (a narrow inlet near the strait) — resolved by trimming the window's last point, the same fix pattern as the Gambia and Tyrrhenian seam pinches.
- **Rhodes**: replaced the single closest-matching old crude point (`[57.5, 37.5]`, the ring's approach to south-west Anatolia) with 40 real points of the Turkish mainland coast nearest Rhodes (Rhodes itself is an island a short distance offshore — this is the mainland side, matching what a `landmass` ring entry can represent), also identity-anchored. Zero crossings on the first attempt.
- Verified: the segment-intersection scan across the whole ring — zero crossings after each splice. Alexandria and Trebizond (both already-shipped anchors from earlier entries) still test correctly on the coastline. `tsc --noEmit` and `vite build` clean. Live in the browser: both new stretches read as coherent local detail, no tangling with the already-real Trebizond arc between them.
- **Still not a full Balkans/Aegean/Levant redo, and flagged as such**: this fixes two more local points (bringing the total real-anchored points on this whole Trieste-to-Alexandria stretch to Trebizond + Constantinople + Rhodes, three), but the Balkans' own west coast (Albania, Montenegro, Croatia south of Trieste), continental Greece, the Aegean islands, and the Levant proper (Lebanon, Israel, Gaza) are still the old crude placeholder. Genuinely fixing those needs their own real anchors — see the next entries for the research done toward that.

### 2026-07-26 — Map: real Ptolemaic data for Sardinia, Corsica, and Sicily (no transform needed)

- Owner: "please go ahead and get new reference data to ensure they are robust." Sicily, Sardinia, and Corsica were three of the crude 4-point island placeholders flagged repeatedly this session as having no real gazetteer anchor to fit or validate a transform against (Sicily explicitly attempted and rolled back for exactly this reason during the Italy work).
- **A better source than expected: Ptolemy's own text, not just a modern anchor point.** Researched via LacusCurtius (Bill Thayer, University of Chicago) — a scholarly, corrected transcription of Ptolemy's *Geography*, Book III, Chapters 2–4 (Corsica, Sardinia, Sicily respectively), each a complete coastal gazetteer in Ptolemy's own recorded degrees. This is categorically better than every other splice this session: those all took *modern* coastline data and passed it through a *fitted* transform to guess where Ptolemy would have placed it. This is Ptolemy's *own* coastal survey, already in his own coordinate system — no transform, no fitting, no guessing required, and it's the single most "robust" (independently verifiable, historically primary) source used in this whole feature.
- Parsed 40 coastal points for Sardinia, 37 for Corsica, and 53 for Sicily directly from the source text (degrees/minutes converted to decimal), including Thayer's own scholarly corrections where the original 1932 edition had a transcription error (Corsica's Casalus Bay latitude, Sardinia's Juliola Town longitude) — used the corrected values, not the erroneous originals.
- **A real parsing bug caught by the same verification discipline used all session**: the name "Himera" appears twice in Ptolemy's Sicily text (a river mouth on the north coast, and an unrelated town on the south coast) — a naive first-occurrence text search assigned the south-coast town the north coast's coordinates, and the segment-intersection scan caught it immediately (a nonsensical backward jump in the coastal sequence). Fixed by searching sequentially from each previous match's position rather than from the start of the text every time.
- All three islands plot as clean, simple (non-self-intersecting) polygons directly in Ptolemy's own coordinates — zero self-intersections, no transform-induced distortion to worry about.
- **Two regressions surfaced and fixed, both caused by this session's own earlier work, not pre-existing**: adding real, precisely-anchored data next to already-precisely-anchored data exposed two genuine overlaps that generic crude boxes had never triggered before. (1) The crude `rhodos` island (a 4-point placeholder) now overlapped the real Rhodes-mainland coastline added in the previous entry, since both were anchored on the same single gazetteer point. Fixed by adding Rhodes island's own real coastline (18 points, same LacusCurtius-adjacent `ne_50m_coastline` source used all session) and nudging its anchor 0.2° south — Rhodes genuinely sits south of that stretch of Turkish mainland, so this is a correction toward reality, not an arbitrary fudge. (2) The crude `thule` placeholder now overlapped the real Scotland extension shipped in the Northern Europe entry, since Scotland's real northern extent reaches further than the old hand-placed version did. Nudged Thule 1.5° further north — consistent with Thule's own traditional role as "the furthest north," not a contradiction of it.
- **Also re-tuned the North African Mediterranean arc's slope** (Alexandria-to-Ceuta, from the Africa entry): its original identity slope (1.0) placed Tunisia's coast in the same ptolemaic-space neighborhood as Sardinia and Sicily's own newly-added, more-authoritative real positions, creating 4 crossings. Confirmed computationally across six slope candidates (1.0 down to 0.5); 0.8 and below all clear both islands, so re-fit at 0.7 (matching a candidate already validated against the rest of the ring during the original Africa work) and re-verified the whole ring from scratch.
- Verified exhaustively, not just the usual single-ring scan: the segment-intersection check across the *entire* `landmass` ring, *every one of the 15 islands' own rings*, and *every pairwise combination of ring against ring* (120 pairs) — zero self-intersections and zero cross-conflicts anywhere in the whole dataset. `tsc --noEmit` and `vite build` clean. Live in the browser: Sardinia and Corsica read as real, recognizably elongated shapes near their actual relative positions; the wider Mediterranean view is coherent with no tangling anywhere. Reran the direct-DOM icon-overlap check — same pre-existing Bruges/Ghent/Antwerp result, confirming nothing new broke.
- **A mid-task mistake worth recording honestly**: while first attempting the Rhodes-island fix, mis-identified a middle element of the already-shipped 40-point Rhodes-mainland arc as if it were a standalone single point, and replaced it with a fresh 40-point arc — fragmenting the existing good arc into three pieces instead of cleanly editing one. Caught immediately (a self-intersection count that should have been unaffected jumped to 8-12), and fixed by reverting the whole file to the last clean commit and redoing every step from there with exact-match boundary verification (confirming a slice's first and last elements precisely before touching it) rather than assuming a single coordinate match meant a single-point target.

### 2026-07-26 — Map: real Ptolemaic data for the Netherlands/Germany/Denmark coast

- Owner: "please go ahead and get new reference data to ensure they are robust" — the last of the three regions flagged after the Northern Europe entry. Checked whether Ptolemy's own *Geography* covers this stretch the way it covered Corsica/Sardinia/Sicily: it does. Book II, Chapter 10, "Greater Germany," is a complete coastal survey from the Rhine mouth around the Cimbrian peninsula (modern Denmark/Jutland) to the Vistula mouth (Poland) — the entire gap, in one primary source, in Ptolemy's own coordinates.
- **Source and copyright note**: this chapter's LacusCurtius page (`.../2/10/limited.html`) presents a bilingual Latin/English edition and, per the site's own copyright key, isn't marked as the plain public-domain reproduction the Corsica/Sardinia/Sicily source was — the translation and editorial commentary appear to draw on a copyrighted modern edition. Only the factual data was used here — the place names (historical facts, not creative expression) and Ptolemy's own ~1,900-year-old numeric coordinates (not copyrightable regardless of which modern edition transcribes them) — not any of the surrounding translated prose or editorial notes.
- **Denmark's own historical distortion, the same phenomenon as Scotland's**: the source's own editorial note (not reproduced here, only its factual implication) confirms Ptolemy draws the Cimbrian peninsula rotated roughly 60° clockwise from its real orientation, attributed to the same root cause as Scotland's own bend — Roman forces never reached the region, so Ptolemy had only sailors' accounts to work from, not land survey. Using his data directly (no transform, same technique as Corsica/Sardinia/Sicily) means this authentic distortion carries through automatically — which is correct for this map's stated purpose, not a defect to correct.
- Parsed 16 coastal points (river mouths along the North Sea coast, then the whole Cimbrian peninsula circuit, then back along the Baltic coast to the Vistula), skipping the inland river-source entries Ptolemy also records for the same rivers. Zero self-intersections as a standalone path.
- **Consolidated rather than patched around the earlier single-anchor Danzig fix**: that fix (from the Northern Europe entry) covered only the immediate Gdańsk-area coast using Danzig's own gazetteer position as a single anchor. This new data covers the same stretch — and much more — natively, so rather than reconciling two overlapping, differently-sourced representations of the same few miles of coast, replaced the Danzig fix entirely along with the two remaining old crude points, dropping in this one continuous 16-point arc in their place (reversed to match the ring's own west-to-east winding at this point). `landmass`: 1288 → 1263 points (net: -61 old/single-anchor points, +16 authentic ones — fewer points, but every one now historically sourced instead of guessed or narrowly anchored).
- Verified: the segment-intersection scan across the whole ring plus cross-checks against all 15 islands — zero self-intersections, zero cross-conflicts. Calais (the nearest existing real anchor) still sits the same ~0.3° from the coastline it always has — unaffected, not a new regression. `tsc --noEmit` and `vite build` clean. Live in the browser: a real, recognizable Jutland-peninsula silhouette (bent, per Ptolemy's own error) extending down from the north, connected cleanly to the already-real French/Belgian coast with no tangling.
- **Deliberately left for a future pass**: the Netherlands' and Belgium's own immediate coast between the Rhine mouth and Calais reads as a single accepted seam gap rather than real detail (no further Ptolemaic source was checked for it this round), and Scandinavia itself is still the crude 4-point `scandia` placeholder — interestingly, this same Germania chapter also describes Ptolemy's own "Scandiae" islands with real coordinates, which would be the natural next source to use if that placeholder is ever revisited.
- **This closes out all three "new reference data" follow-ups from this round** (Levant/Cyprus was done with existing unused gazetteer anchors in an earlier entry; Sardinia/Corsica/Sicily and this Germania/Denmark piece both used freshly-researched primary-source data). Remaining known gaps in the whole map, for whenever picked up next: the Balkans' west coast, continental Greece, the Aegean islands, and the Levant proper; Scandinavia's own island shape; and the still-unaddressed Bruges/Ghent/Antwerp icon-overlap (unrelated to any geography work, flagged repeatedly, never fixed).
